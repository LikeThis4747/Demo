# PCG Grid-WFC V3.2 实施交接

## 当前状态

- V3.2 已通过最新独立审计并获用户允许写入正式 DailyPlan。
- 本轮只修改文档，没有修改 `Source/Demo/PCG` 或 UE 资产。
- 下一次用户会明确要求开始实现；必须从 DailyPlan 检查点 0 开始，不能直接重写源码。

## 必读最少文件

1. `DOC/DailyPlan/2026-07-24-PCG-Grid-WFC-V3.2实施方案.md`
2. `claude/docs/2026-07-24-pcg-grid-wfc-v3_2-code-proposal.md`
3. `claude/reviews/2026-07-24-pcg-grid-wfc-v3-draft-review.md`
4. `claude/tasks/active/TASK-20260723-002-PCG整关场景.md`
5. `DOC/Design/PCG/SCIFI_HYDROLAB_MODULE_TABLE.md`

## 已冻结决定

- 600 cm 单层逻辑 Tile，代码固定覆盖 `OpeningMask 0..15`。
- WFC 保留最小熵、权重观察和传播；删除回溯、重试、换 Seed 和 SkeletonFallback。
- 删除 A*、Socket/Portal/Closure/Cap、逻辑 Catalog 和旧抽象空间图。
- Flow/Core 只保留 CompletionRule、K/N、Start/Objective/Exit Landmark、ProgressBand 和确定性 Hash。
- Grid 先造 Start→Exit 主骨架，再将全部 Objective 接入；Optional 路口无配额。
- HydroLab 表现基线：LargeFloorB、WallH、CeilingC、WallTrimG、PillarC。

## 实施前风险

- 工作区包含大量 UE 5.8 升级与其他任务的 dirty 文件；只修改 DailyPlan 列出的 PCG 文件和项目自有生成资产，不 reset、不清理、不顺手合并无关改动。
- 审计仍留在 `claude/reviews/` 根目录，因为代码建议尚未真正落实；完成全部高回报项后再由实施 AI 标记 `Done-` 并归档。
- UE 5.8 基线构建或当前 `Demo.PCG` 若失败，先区分升级问题，不能把错误归因于尚未开始的 V3.2。

## 精确下一步

1. 用户明确说“开始实现”。
2. 检查 Git 范围与 UE 是否关闭。
3. 完整构建 UE 5.8 `DemoEditor Win64 Development` 并运行当前 `Demo.PCG`。
4. 基线通过后按正式计划连续实施；每个检查点完成后报告实际差异与验证结果。仅在范围变化、第三方素材修改、额外删除或升级冲突时暂停询问。

## 验证状态

- 文档代码围栏、正式路径与旧 V3.1 历史引用已完成一致性检查；现存 V3.1 字样只用于说明被 V3.2 取代的历史结论。
- 尚未执行任何 V3.2 C++ 构建、自动化、Blueprint 编译、PIE 或玩家验收。
