# 给 Code Review AI 的审查任务

请对本目录的 PCG 多层关卡拟实现补丁做严格静态审查。不要直接应用补丁，不要修改 `Source/`、`Content/`、`Config/` 或 UE 资产；只把审查报告写到 `claude/reviews/` 根目录，文件名不要加 `Done-` 前缀。

## 必读内容

1. `DOC/DailyPlan/2026-08-03-PCG多层关卡正式实施计划.md`
2. `claude/tasks/active/TASK-20260802-001-PCG多层WFC新方案讨论.md`
3. 本目录 `README.md`、`INTEGRATION_REVIEW.md`、三个 review notes
4. 按 README 顺序阅读五个补丁，以及当前工作树中补丁涉及的真实 `.h/.cpp`
5. `DOC/AI_WORK_GUIDELINES/PROJECT_ARCHITECTURE_RULES.md` 与 `DOC/AI_WORK_GUIDELINES/AI_WORKFLOW.md`

当前 `ZeroEscapeGameMode.h/.cpp` 已包含用户未提交的 ResultMenu 改动。这些改动属于审查基线，不是补丁可以覆盖的旧代码；请专门核对三方合并结果是否完整保留结算界面逻辑。

提交本轮复审前已有以下隔离证据：五片补丁顺序检查、实际应用与应用后 `git diff --check` 通过；UE 5.8 UHT 和 Demo 模块编译通过并生成 DLL；`Demo.PCG` `24/24`、`Demo.GameFlow.AsyncSetupGate` 与 `Demo.GameFlow.AutomaticRetryPolicy` 合计 `2/2` 通过。请继续审查逻辑与架构，不要把这些证据误写成正式资产、真实 `RecastNavMesh`、PIE 或玩家验收。

## 重点审查

- 五个补丁能否按声明顺序机械应用；新增头文件、测试、include、UHT 宏、UE5.8 API 和模块依赖是否完整。
- 2/3/4 层权重、玩家/追猎者在一楼、Exit 在顶层、每对相邻层的必需双层楼梯，以及“贯通三层的楼梯间最多一座且不能替代必需楼梯”是否真正成为硬合同。
- 必需端点是否先最大化空间跨度、边缘偏好只作次级排序；额外楼梯是否逐座最大化到全部现有端点的最近距离，且概率/数量受 DataAsset 与整栋上限控制。
- 高天花板房间是否支持整栋最低数、目标权重、每层上限与间距；任意层可为零，顶层合法；只有真实上层存在时才保留净空。
- 结构四向旋转后，可走格、实体格、净空格、内部无向边、开口和落脚平台是否规范排序、去重、同向变换并保持单一所有权；混合大小写 `FName` 是否使用统一稳定排序。
- 每层是否继续调用现有二维 WFC，而不是暗中把公共 Grid/WFC 接口改成三维；结构是否正确投影为 Required、Outside 与固定开口边。
- 整栋候选、回溯和求解树预算是否由所有楼层与全部整栋重试共同扣减，任何重试都不能刷新；全部乘法、权重总和、坐标差与数组下标是否先安全校验，损坏 DataAsset 是否 fail-closed。
- 相同 Seed 的结果是否可能受 `TMap/TSet` 迭代、资产数组重排、`FName` 进程索引、浮点近似 Hash 或共享随机流消费顺序影响。
- 最终整栋通行图是否覆盖玩家、Exit、每层和全部必需/额外楼梯落脚点；每层路线跨度验收与整栋连通验收是否各自有意义，而非互相矛盾或重复遍历。
- Builder 是否避免普通格、完整楼梯和高厅生成双地板、双墙、补顶或封口；隐藏坡面是否延续 Level0 的碰撞/导航职责，且没有一次性探针、临时 Actor、全局导航 Build 或 Recast 全局参数放宽。
- 导航完成事件不带 PCG 操作编号。请检查代码是否如实只用 OperationId 防旧 Timer/最终报告，而没有虚构事件归属；提交期间事件、目标导航数据、单 Actor 单局、重复回调、超时、EndPlay 和清理是否 fail-closed。
- 代表点是否有真实的 20 点/19 路径硬上限，投射高度是否避免吸附到错误楼层，路径查询是否使用真实追猎者代理；代码不得用固定毫秒阈值判废一张连通地图。
- GameMode 是否请求前绑定并锁输入，只在最终导航成功后依次放置玩家/追猎者、Exit、显式调用 Population、绑定死亡与胜负并开放输入；主菜单请求和直接运行 `L_Game` 是否走同一流程。
- 自动重试是否只接受换 Seed 可能改变的 `Stage + Failure` 组合；固定配置、实例化和导航准备错误是否仍 fail-closed。GameInstance 中的次数与 PendingRequest 是否原子推进并在新显式请求时归零；最多 3 次后是否停止；下一 Seed 是否可复现且不改变难度。
- 重试是否始终通过蓝图显式配置的 `TSoftObjectPtr<UWorld> GameLevel` 创建新 World，且没有写死 `L_Game` 路径、同 Actor 原地重生成、新状态子系统或加载界面。`ClearGeneratedScene` 是否如注释所述只清理而不重新开放 Generator。
- Population 的零候选/零目标是否成功跳过；配置、候选查询、类加载或 Spawn 失败是否清掉本轮全部对象。失败回主菜单、重复回调和 EndPlay 是否不留下半局 Actor 或委托。
- 请确认被保留的两级 Spawn 预算确实分别约束单条规则和跨规则累计总量；不要仅因常数相同就要求删除其中一层。Planner 目前只增加分节注释，除非发现具体职责错误或无法测试的耦合，不以文件行数本身要求扩张为多文件重构。
- 自动化是否只证明纯合同，不把纯单元测试冒充真实 `RecastNavMesh`、角色胶囊、追猎者或玩家验收。

## 报告格式

按严重程度列出可操作问题。每条给出补丁名、目标源码路径、精确片段/行号和违反的已确认规则；优先报告编译失败、错误地图、非确定性、预算失效、半局提交和导航假成功。

最后单列：

1. 可直接采纳的代码修正
2. 必须由用户决定的玩法取舍
3. 只能在正式落盘后通过构建、UE 自动化、资产迁移或 PIE 证明的事项

不要因为它是拟实现稿就降低审查标准，也不要要求它在尚未授权的阶段提供正式资产修改或玩家验收结果。
