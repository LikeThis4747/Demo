# Demo 夜间只读审计 — 2026-08-08

## 今日完成与验证

- 上一夜间快照后有 4 个白天提交：建立 HeavyImpact 起身实现基线、保存制导机关调参基线、实现玩家/追猎者共享倒地起身恢复桥，并记录 A Pose 修复计划。
- 当前磁盘上另有 6 个未提交文件：两份项目 AnimBP，以及 HeavyImpact 恢复组件、调参和自动化测试的 4 个 C++ 文件。C++ 增量为倒地二次接触增加最小 Chaos 冲量门槛，并把恢复 Capsule 路径起点抬到骨盆上方以减少贴地假重叠。
- 现有 `UnrealEditor-Demo.dll` 时间为 2026-08-07 16:41:42，晚于上述 C++；现有编辑器日志记录 `Demo.Physics.HeavyImpact.*` 5/5 成功。
- 现有运行日志已出现玩家与追猎者的真实 `committed → Downed → recovery completed`；同时仍出现少量“最终骨盆附近没有可信空闲 Capsule Sweep 起点”的阻塞记录，不能视为墙边/堵塞边界已验收。
- 官方 UE5.8 MCP 与本地 UE Editor MCP 均在线；Level0 打开、PIE 已停止。两份 AnimBP 父类均为项目的 `HeavyImpactAnimInstance`，编译状态 `UpToDate`。
- 蓝图/关卡只读审计发现：玩家 AnimBP、追猎者 AnimBP 与 Level0 当前均为编辑器内脏状态。本轮未保存资产；Git 快照只能备份磁盘版本，不能包含尚未保存的编辑器内状态。

## Git 快照

- `origin` 已在提交前核验为 `git@git.woa.com:shiqiqiwang/Demo.git`，分支为 `main`。
- 主快照提交与普通推送结果：待提交后回填。
- 禁止的 reset/clean/restore/checkout/rebase/merge/stash/amend/force-push 均未执行；未访问 GitHub 或其他外部平台。

## 明日优先任务

1. P0：先由用户决定并保存或放弃 Level0 与两个 AnimBP 的编辑器内脏状态，再做玩家/追猎者真实命中的连续画面验收。
2. P0：复测墙边、墙角、完全堵塞、解除堵塞与倒地二次接触；确认阻塞时保持倒地重试，空间恢复后可正常起身。
3. P1：在具备 Recast 的正式追逐环境中确认追猎者起身后恢复追逐；Level0 当前日志的 Recast 缺失不能替代此项。
4. P2：HeavyImpact 画面通过后，再从摆锤、冲锤、制导机关中只选一个接入正式一局。

## 可玩内容新 idea

- “重冲击逃脱窗口”：把一个已验收冲锤放进必经短廊，玩家可诱导追猎者吃到真实重冲击并获得数秒逃脱窗口；复用现有倒地/起身状态，不新增第四种机关。

## 风险与优化排序

- 阻塞交付：编辑器内三项未保存资产状态尚未纳入磁盘快照，次日首先处理归属与保存选择。
- 高回报：把真实起身恢复与追猎导航串成一次完整可玩验收，价值高于继续扩展机关或抽象公共层。
- 高回报：验证 `MinimumDownedReimpactImpulse=1000` 是否能过滤残余接触，同时保留真正二次命中的打断。
- 可延后：UI、美术统一和更大范围 Physics Control 重构。

## AI 经验

- `UpToDate` 只说明当前加载蓝图的编译状态，不能替代 `is_dirty` 与磁盘快照核对。
- 真实恢复成功日志与墙边/堵塞验收是两类证据，不能互相代替。
- 夜间快照遇到编辑器内脏资产时只能备份磁盘版本，绝不能为追求干净状态自动保存。
