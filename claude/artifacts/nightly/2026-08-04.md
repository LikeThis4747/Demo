# Demo 夜间只读审计 — 2026-08-04

## 今日完成

- 2026-08-03 白天完成正式一局三阶段闭环：主菜单进入游戏、胜负判定、结算/重开/选关/回主菜单与暂停恢复；项目记忆记录用户已验收该流程。
- 多层 PCG 已正式落入当前工作区：完整结构预放置、逐层二维 WFC、楼梯/高厅表现、动态导航等待与路径检查、跨 World 有限换 Seed 重试、Population 与 GameFlow 接入。
- 当前构建产物与日志显示 2026-08-03 21:55 完成 Demo 模块构建，并执行 `Demo.*` 自动化 23 项；日志逐项均为 `Success`，队列总结为 `23 tests performed`。
- 代表性白天证据记录 Seed 12345 首次共享预算失败后以派生 Seed 2044445496 成功生成 3 层，并完成 11 点投射、10 条路径与玩法对象装配；这不是本轮新 PIE，也不替代玩家/追猎者验收。

## 本轮只读审计

- 开始时 `main` 与 `origin/main` 分歧为 `0/0`，但工作区包含尚未提交的多层 PCG 正式实现、GameFlow/UI 收口、4 个 UE 资产及相关计划/报告/记忆变更；Source/Demo 当前共 85 个源码/Build 文件。
- C++ 静态复核确认新增职责集中在多层布局 Planner、结构表现 Builder、运行时导航门、GameMode 异步开局/自动重试、Population 候选消费与 23 项合同测试；`git diff --check -- Source/Demo` 未发现空白错误，仅报告既有行尾转换提示。
- 本地 UE Editor MCP 返回 `pong=false`，UE5.8 官方 MCP 工具当前未暴露；两者不满足联合可用条件，因此**蓝图审计未执行**。本轮不推断蓝图、关卡、DataAsset、资产引用或配置状态。
- 提交历史中存在白天提交 `e44723ef`（消息为从 GitHub URL 合并）；它发生在本轮之前。本轮未访问、调用或推送 GitHub，最终仅允许在 `origin` 精确等于内部工蜂地址后普通推送。

## 明日优先任务

1. 玩家从主菜单开始，连续实走双层楼梯与贯通三层的楼梯间，检查起收步、转角、脚底、护栏、相机与高厅封闭；这是当前最重要的可玩验收。
2. 用真实追猎者完成正式追逐中的跨层上/下楼，并复核正常游玩期间是否出现 `CrowdManager` 找不到 `RecastNavMesh`；仅开局路径查询成功不能替代此项。
3. 连续验证生成失败换 Seed、暂停/恢复、胜负结算、同 Seed 重开与新 Seed 下一把，再执行 Easy/Normal/Hard 各至少 300 个纯数据 Seed 统计。

## 风险与优化（按优先级）

1. **玩家与追猎者跨层体验未验收**：当前只能标记“已实现、待验收”；先完成真实移动，再决定是否调楼梯碰撞、导航或结构参数。
2. **多层首轮参数缺少分布证据**：单次成功不能证明难度、楼层、楼梯/高厅分布和失败率合理；用 900 Seed 数据后再调 DataAsset。
3. **工作区变更面较大**：约 3795 行新增、2752 行删除并包含 UE 资产；快照用于备份，不等于代码审查或玩家验收通过。
4. **任务记录需要白天归档**：`L_Game玩家未生成排查` 已标记 done 但仍在 active；SFCorridors 退场任务不是当前可玩主线，建议白天归档/暂停，保持唯一关键任务。

## 可玩内容 idea

- 先做“跨层追猎压力跑”：玩家必须携带一个现有磁性物穿过至少一座楼梯到达最高层 Exit；携带会限制操作节奏，追猎者可跨层追击。该 idea 复用磁力、追猎者、楼梯与 Exit，不先增加新框架，且能同时暴露当前最关键的导航、碰撞和节奏问题。

## AI 经验

- 自动化路径存在性检查只能证明导航图连通，不能替代真实角色跨层移动与玩家手感。
- 大规模生成实现应先保留可恢复失败边界，再用批量 Seed 数据校准参数，而不是用单次成功调值。
- 夜间快照只证明变更已备份；构建、测试、资产回读和玩家验收必须分别陈述。
