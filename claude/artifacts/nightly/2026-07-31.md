# Demo 夜间报告 — 2026-07-31

## 今日完成与证据

- 当日没有 Git 提交；工作区保留白天未提交内容：8 个主菜单/正式开局流程 C++ 文件、`UMG` 模块依赖、`GAME_LOOP_PLAN.md`、`Level0` 三层楼梯塔、`r.VirtualTextures=False` 及相关任务/记忆更新。
- C++ 静态审计确认阶段一骨架已覆盖 `GameInstance` 的 Seed/难度传递、主菜单 Widget 逻辑、主菜单 GameMode，以及正式 GameMode 的 PCG 生成与玩家/追猎者初始摆放。
- 新 C++ 文件时间为 2026-07-30，现有 Demo 模块二进制仍为 2026-07-29；本轮没有构建、UHT、自动化或 PIE 证据，不能把阶段一记为可玩。
- HydroLab 三层楼梯塔任务卡记录最终为 211 Actor / 13 文件夹、四个 600 cm 接口与静态通道检查；本轮未实时复核该资产。
- 蓝图审计未执行：本地 UE Editor MCP `pong=false`，保存日志显示连接数达到上限；官方 UE5.8 MCP 未暴露。未推测蓝图、关卡、DataAsset 或引用结果。

## Git 快照

- 初始状态：`main...origin/main`，10 个已跟踪修改、9 个未跟踪文件；2026-07-31 无当日提交。
- `git remote get-url origin` 精确返回 `git@git.woa.com:shiqiqiwang/Demo.git`。
- 主快照 `204851b`（`Nightly snapshot 2026-07-31`）已普通推送到 `origin/main`；1 个 LFS 对象约 4.1 MB 上传成功。

## 明日优先任务

1. **P0 可玩推进**：先构建新 GameFlow/UI C++，再在 UE 中装配并验证 `GameInstance`、主菜单 GameMode/Widget、正式 GameMode、`L_MainMenu` 与 `L_Game`，跑通“选 Seed/难度 → 生成 → 玩家和追猎者就位”。
2. **P0 运行验收**：确认正式 GameMode 使用已装配输入/磁力资源的玩家蓝图，并验证错误路径不会留下“玩家已移动但追猎者未生成”的半初始化局面。
3. **P1 场景验收**：玩家、Recast 和真实追猎者连续走完 V5 与三层楼梯塔；随后恢复当前 18 项 Demo.PCG 与至少 10 Seed 基线。
4. **P1 流程闭环**：阶段一通过后再接生命归零失败、同 Seed 重开与 Exit 成功结算，不提前扩展通用任务框架。

## 风险与优化

- **阻塞交付**：新阶段一代码尚未构建，且本轮无法确认所需 UE 资产与项目设置是否存在或引用正确。
- **高回报**：正式 GameMode 的原生玩家类没有输入 DataAsset 装配；若蓝图未覆盖 `DefaultPawnClass`，角色会生成但无法正常输入/磁力交互。
- **高回报**：当前开局流程先移动玩家、后校验/生成追猎者；后半段失败时可能留下部分生效状态，白天应先复现再决定是否调整。
- **白天确认**：`.gitignore` 新增 `DOC/PPT/` 会影响后续新 PPT 文件进入快照；需确认这是否为有意仓库策略。
- **可延后**：菜单视觉润色、复杂难度联动和通用任务系统都应晚于最小一局闭环。

## 新玩法 idea

- 把主菜单 Seed 选择直接变成可复现的“逃生挑战码”：玩家可重开同一 Seed 对比三层路线与追猎压力，先复用现有生成/追击能力，不引入新系统。

## AI 经验

- C++ 类存在不等于流程可玩，跨关卡闭环必须同时有当前构建、UE 资产装配和 PIE 证据。
- UE MCP 不可用时必须明确停止蓝图审计，不能从 C++ 或二进制资产名推断装配结果。
- 多步骤开局编排应先验证全部必需依赖，再改变玩家位置或生成持久对象，避免失败后留下半初始化状态。
