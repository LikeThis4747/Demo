# Demo 夜报 — 2026-07-29

## 今日完成

- Level0 已形成 HydroLab WFCBrickStudyV3 静态原型：7 类独立积木、兼容链和完整可玩组合；任务记录包含磁盘重载、静态几何和短时 Simulate 证据，用户实走与 AI/NavMesh 验收仍未完成。
- 追猎者新增近战攻击/方向受击蒙太奇、受击停顿、`AttackApproachRadius`、限时 `AttackProjectile` Tag 与 Camera 通道忽略；玩家 BP、追猎者 BP、locomotion AnimBP 均为 `UpToDate`。
- 三张旧追猎者任务卡已归档；当前 active 为 SFCorridors 筛选与 HydroLab 错层房间装配验证。
- 本轮未构建、未运行 PIE/自动化、未保存或修复任何 UE 资产。官方 UE5.8 MCP 未暴露，DataAsset 精确属性与通用引用审计未执行。

## 明日优先任务

1. **P0 — Level0 可玩导航**：补建并可视化确认 Recast，玩家连续走完入口、兼容链、主房间、StairsC/Deck135，再让追猎者实际寻路覆盖同一路线。
2. **P1 — 最小失败闭环**：接通“生命归零 → GameFlow 失败 → 同 Seed 重开”，优先完成一局可失败、可重开的闭环。
3. **P1 — 追猎者战斗验收**：用当前 UE5.8 完整构建，随后 PIE 验证攻击距离、攻击蒙太奇、三方向受击停顿、至少 10 次连续命中及恢复。
4. **P2 — 资产收敛**：SFCorridors 继续只读依赖闭包；新 MeleeAnimation 包先确认产品所需资产，再讨论精确退场清单，不自动删除。

## 风险与优化（按优先级）

- **P0 导航未成立**：日志明确出现 `Unable to find RecastNavMesh instance`；短时 Simulate、静态射线与 Actor 存在都不能替代玩家胶囊和 AI 寻路证据。
- **P1 新战斗链未建立当前运行证据**：C++ 与资产已落盘，但本轮没有构建/PIE；`UpToDate` 只证明 Blueprint 当前编译状态。
- **P1 重复投掷 Tag 边界**：每次投掷创建独立 Timer 清除同一 Actor Tag；若旧 Timer 到期前再次投掷，旧 Timer 可能提前清除新一轮有效 Tag，需先复现再修。
- **P1 MCP 稳定性**：`open_asset_editor` 有 UObjectArray 断言/SEH 崩溃保护记录，并出现递归 `FlushRenderingCommands` 警告；白天避免高频切图/开资产与批量保存交叠。
- **P2 活跃任务失焦**：两张 active 卡并存，且追猎者新改动没有独立验收卡；应在白天由用户确认唯一关键路径后收敛记录。

## 可玩内容 idea

把 V3 完整组合直接做成一局“错层追逐”：入口取得磁力物，投掷命中追猎者触发方向受击与短暂停顿，玩家借 StairsC/Deck135 拉开距离后冲向 Exit；被追上或踩中地刺则进入失败并同 Seed 重开。它复用今日已有场景与战斗资产，不先扩展新系统。

## AI 经验（最多 3 条）

- 600 cm 求解 Cell、可独立成立的 Module 与最终 Composition 必须分层验证，不能把孤立盒子当成可玩模块。
- Blueprint `UpToDate`、短时 Simulate 和静态射线都不是 Recast、AI 寻路或玩家连续实走的替代证据。
- 限时 Actor Tag 若允许同物体重复激活，清理机制必须区分激活代次，避免旧 Timer 清掉新状态。

## Git 备份

- 快照前工作树：23 个已跟踪变更条目、18 个未跟踪条目；`git diff --check` 无空白错误，仅有 4 个 LF→CRLF 提示。
- 远端核验、提交与推送结果在本次流程完成后回填；只允许 `git@git.woa.com:shiqiqiwang/Demo.git`。
