# Demo 夜间只读报告 — 2026-08-18

## 今日完成与验证

- 2026-08-17 白天完成楼梯普通顶灯恢复与入口/出口蓝色点光引导、追猎者攻击录制开关、Level0/正式出口蓝色传送门，以及 Gameplay HUD 的资源条、操作提示和顶部光团目标行。
- Git 记录显示上一夜快照后共有 9 个提交；当前工作树干净，`main...origin/main` 无可见偏差。DemoEditor Development 模块于 23:05 生成，白天任务卡记录 HUD/传送门 PIE smoke；本夜未运行构建、自动化或 PIE。
- 蓝图审计已执行：官方 MCP 与本地 UE Editor MCP 均在线；当前关卡为 `/Game/Levels/Level0`、PIE 已停止；`WBP_GameplayHUD`、`BP_ZeroEscapeExitVolume`、`BP_ZeroEscapePlayerController` 均为 `UpToDate`，关键资产与 Level0 均非 Dirty。出口依赖现有 Portal 材质，HUD 父类为 `ZeroEscapeGameplayHUDWidget`。

## 明日最优先任务

1. 在 `L_Game` 正式 PIE 先验收最终 HUD：顶部目标行当前 Canvas 锚点为左上角 `(0,0)`、水平对齐为 `0.5`，存在半行落到屏幕外的高概率风险；最终两笔 HUD 提交后没有新的 PIE 证据。
2. 录制完成后把 `DA_Pursuer.Enable Attacks` 从当前 `false` 恢复为正式值，并在同一局验证攻击、受击与追逐没有互相屏蔽。
3. 用同一正式 PIE/Development 包跑通动态 Recast、真实追猎者跨层追逐、机关、光团、出口、死亡与重开；再做目标机启动检查。

## 新内容 / 玩法建议

- 把“光团达标”做成一次统一的可玩反馈：顶部计数由红转蓝、现有传送门短闪，并补一声短促出口解锁音；只复用现有状态与表现入口，不新增任务系统。

## 风险与优化优先级

- **P0 阻塞交付**：顶部目标行最终布局未经过 PIE；先确认锚点、全分辨率安全区和暂停/结算遮挡。
- **P0 阻塞交付**：追猎者攻击 DataAsset 仍为录制用关闭状态，正式验收/打包前必须显式恢复并回读。
- **P0 阻塞交付**：正式完整一局、动态导航、真实跨层追逐与目标机运行仍缺同一轮证据。
- **P1 高回报**：保存日志仍报告 `/Engine/EngineMeshes/Humanoid` 缺失依赖，目标机重点检查追猎者骨骼/动画。
- **P2 可延后**：`claude/tasks/active/` 仍有 34 张卡且存在重复编号；白天按 Owner/验收状态归档，避免遮蔽真正 P0。

## Git 快照

- 推送前将按规则核验 `origin` 必须精确为 `git@git.woa.com:shiqiqiwang/Demo.git`。
- 快照与远端哈希结果将在完成普通推送后回填。

## AI 经验

- UI 最后一笔资产提交后必须重新做一轮 PIE，较早截图不能证明最终锚点和安全区正确。
- `UpToDate` 与非 Dirty 只证明资产可编译且已保存，不能替代运行时可见性和交互验收。
- 录制用 DataAsset 开关必须进入正式验收清单，避免调试状态随快照进入交付版本。
