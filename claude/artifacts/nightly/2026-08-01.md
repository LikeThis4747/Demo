# Demo 夜间只读维护 — 2026-08-01

## 今日完成与验证

- 自上一轮夜间快照后新增白天提交 `15fba6cb69bec116f8729a9b10bb4751a103fec3`（`main menu`），覆盖主菜单/正式关卡、GameInstance、GameMode、Widget、配置、相关 C++ 与任务记录；本轮开始时 `main` 比 `origin/main` 超前 1 个提交，另有已保存但未提交的 `Content/Levels/L_Game.umap` 修改。
- 当前 `UnrealEditor-Demo.dll` 时间为 2026-07-31 20:57:05，晚于本轮相关 GameFlow/UI 源码；现有编辑器日志确认该 DLL 已加载。此证据只说明已有白天构建产物，不等同于本轮重新构建或完整 UHT/自动化回归。
- UE5.8 官方 MCP 与本地 UE Editor MCP 均在线；PIE 当前未运行。官方 MCP 读取确认：`L_Game` 使用 `BP_ZeroEscapeGameMode`，启用 AI/Navigation，Generator 为 `ExplicitOnly`，Populator 已绑定该 Generator 与 `DA_Population_Default`；`L_Game` 与 `L_MainMenu` 均无未保存脏状态。
- `BP_ZeroEscapeGameMode`、`BP_MainMenuGameMode`、`BP_ZeroEscapeGameInstance`、`WBP_MainMenu` 均为 `UpToDate`。正式 GameMode 的 Default Pawn 已指向 `BP_ZeroEscapeCharacter`，追猎者类指向 `BP_Pursuer`；WBP 的 BindWidget 所需控件均存在，目标关卡为 `L_Game`。
- 现有白天日志（非本轮新 PIE）确认：从主菜单以 Seed 12345 进入 `L_Game` 后只出现一次该 Seed 的 PCG 成功结果，随后放置 4 个地刺与 8 个磁性物体，并生成 `BP_ZeroEscapeCharacter` 与 `BP_Pursuer`。此前“默认 Seed + 菜单 Seed 重复生成”和 Populator 未绑定的日志只出现在修正前。
- 当前没有 2026-08-01 白天提交；本轮未运行构建、自动化测试或 PIE，未修改项目代码、资产、配置或规范。

## Git 快照

- origin：待快照前核验。
- commit：待执行。
- push：待执行。
- 最终工作区/LFS/分支状态：待执行后复核。

## 明日最优先任务

1. 完成最小一局验收：从主菜单选择 Seed/难度进入游戏，实际操作玩家，确认 PCG、陷阱/磁性物体、追猎者、Exit 与生命归零链路连续成立。
2. 复核出生距离：配置下限为 1200 cm，但最新成功日志的实际玩家—追猎者二维距离为 1138 cm；检查碰撞调整和候选格实际位置，确定是否影响开局公平性。
3. 在当前源码上重新建立 DemoEditor 构建、18 项 `Demo.PCG` 自动化和至少 10 个 Seed 的玩家/导航/真实追猎者基线。

## 新内容 / 玩法建议

- 最小“磁性保险丝撤离”目标：玩家必须把现有磁性物体中的任务保险丝送到 Exit 附近终端后才能撤离；它直接复用磁力、陷阱、追猎者和出口，不先扩建新框架即可让一局从“跑到出口”升级为“携物穿越压力区”。

## 风险与优化

### 阻塞交付

- 尚无玩家实际完成“菜单 → 游戏 → Exit/死亡 → 结算或重开”的整局验收；当前只能确认开局生成与放置链路。
- 当前版本未重跑 18 项 `Demo.PCG`、至少 10 Seed、Recast 连续覆盖与真实追猎者移动，不能用单 Seed 12345 的成功日志替代。

### 高回报

- 先把胜利、失败和同 Seed 重开接到现有 GameFlow，再扩展楼梯塔或新房间；这能最快形成可反复试玩的一局。
- 把开局“计划距离”和生成后的“实际距离”同时断言/记录，避免碰撞调整后悄然缩短追猎者缓冲距离。

### 可延后

- SFCorridors 资产退场、三层 WFC 扩展、低价值目录/命名整理继续延后，待最小一局与回归基线稳定后处理。

## AI 经验

- 同时读取当前资产属性与时间排序日志，才能区分“修正前失败记录”和“磁盘中已保存的当前配置”。
- PCG 自动触发与 GameMode 显式触发必须只有一个权威入口，否则菜单参数会造成同关卡重复生成。
- 出生点的规划距离必须用最终 Actor 位置复核，因为碰撞生成或传送调整可能改变理论间距。
