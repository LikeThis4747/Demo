# Demo 夜间只读报告 — 2026-08-19

## 今日完成与验证

- 上一夜快照后共有 13 个白天提交：HUD 目标行居中与事件更新、出口→追猎者→玩家开场镜头、关底胜负过场、Level0 楼梯导航坡面、玩家跳跃削弱及 SciFiHydroLab Presentation 基线。
- 当前未提交实现继续加入出口能量不足提示、“开始逃亡”提示、0.35 视角倍率、关闭 Motion Blur，以及关卡、追猎者、摆锤和 HUD 资产调整。
- 已有白天证据：Shipping 二进制于 22:12 生成；23:16 Cook、Pak、Stage 全部成功，UAT `ExitCode=0`；PIE 日志记录一次收集 `2/2` 光团后 `ZE_ROUND_RESULT result=Win`。本夜未重跑构建、自动化或 PIE。
- 蓝图审计未执行：本地 UE Editor MCP 在线，但官方 UE5.8 MCP 未暴露；按项目规则未用本地 MCP 替代官方字段、父类、WorldSettings、Actor UPROPERTY 或资产配置回读。

## 明日最优先任务

1. 先确认并恢复官方 UE5.8 MCP 工作流：当前磁盘 `Demo.uproject` 已移除 `ModelContextProtocol`，与项目“官方 MCP 自动启动”规则冲突；若只是规避 Cook 端口冲突，应优先使用 Cook 的禁用参数，而不是长期移除项目插件声明。
2. 在同一正式 PIE/Shipping 运行中证明动态 Recast 与真实追猎者跨层追逐；当前保存日志仍反复出现 `Unable to find RecastNavMesh`。
3. 跑通机关、光团、锁门提示、出口、死亡/重开，并在目标机检查开场镜头、HUD、安全区、视角倍率、胜负过场及追猎者骨骼/动画。

## 新内容 / 玩法建议

- 把现有开场镜头做成 10 秒内的“出口—追猎者—玩家”逃亡宣告：镜头落回玩家时显示“开始逃亡”，出口未解锁时用现有红字与传送门闪烁反馈；只组合已存在的流程和 UI，不新增任务系统。

## 风险与优化优先级

- **P0 阻塞交付**：`Demo.uproject` 当前不再声明官方 MCP 插件，本夜官方 MCP 不可用；先恢复工具链一致性，再做资产级验收。
- **P0 阻塞交付**：PIE 日志持续缺少 RecastNavMesh；一次胜利日志不能证明真实追猎者跨层追逐。
- **P1 高回报**：Cook 仍报告 `/Engine/EngineMeshes/Humanoid` 缺失依赖；虽然本次打包成功，目标机上的追猎者骨骼/动画仍需实测。
- **P1 高回报**：日志反复出现 UIOnly 聚焦不可聚焦 Widget 错误；菜单键盘/手柄焦点与暂停/结算切换需验证。
- **P2 可延后**：`claude/tasks/active/` 任务卡过多，并留有临时脚本/图片；由对应白天 Owner 核对后再归档或清理，夜间不处理。

## Git 快照

- 待执行：必须先确认 `origin` 精确等于 `git@git.woa.com:shiqiqiwang/Demo.git`，再按授权执行 `git add -A`、`Nightly snapshot 2026-08-19` 与普通 `git push origin main`。

## AI 经验

- Cook/Pak/Stage 成功只证明产物生成，不证明目标机启动、动态导航或真实 AI 追逐。
- 临时规避编辑器插件端口冲突应限于 Cook 命令参数，不能静默改变日常编辑器工具链基线。
- 日志里的胜利结果必须与同轮 Recast、真实追逐和玩家体验证据一起看，不能单独当作整局验收。
