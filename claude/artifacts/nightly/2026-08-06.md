# Demo 夜间只读维护报告 — 2026-08-06

## 今日完成与证据

- 自上一轮夜间快照 `248f78eccfa4e6cc2902477fc8c79498ed61ba66` 后共有 5 个白天提交，当前 HEAD 为 `29e900bd0e9ad3bd5eab0c02d8bb7a8914f915bd`。
- Level0 常驻物理摆锤原型已落盘：独立 Actor、调参 DataAsset、装配蓝图和测试房；白天完整构建、补能标定、20 kg 磁性物碰撞与用户初步手感验收已有记录。
- 共享重冲击原型的 C++ 已落盘：准备请求/接口、Physics Control 状态组件、玩家与追猎者适配、磁力中断、摆锤 ETA 预测和三组自动化测试源码。
- 重冲击当前仅有 UHT 与 `-Module=Demo -NoLink -NoEngineChanges` 编译证据；`git diff --check` 通过。没有完整 DLL 链接、自动化、PCA/DA/Blueprint 装配或重冲击 PIE 证据。
- 双 MCP 均在线；官方 MCP 确认当前 Level0 打开、PIE 未运行，Level0、L_Game、摆锤蓝图/DA、玩家与追猎者蓝图均非脏；本地 MCP 确认三份相关蓝图为 UpToDate。
- 资产回读确认 `/Game/ZeroEscape/Physics/HeavyImpact` 下两份 PCA 与 HeavyImpact DataAsset 尚不存在；当前 Editor 加载的旧 DLL 中，玩家/追猎者 CDO 还没有 HeavyImpact 字段，追猎者仍暴露旧 `PhysicsControlHitResponse` 实例。

## 明日优先任务

1. **P0 — 完成可运行基线**：用户安全关闭 PID 13728 的 Editor 后，完成 `DemoEditor Win64 Development` 完整链接；不得杀进程、热更或触碰 `D:\UE5_8` 的用户 OIT 改动。
2. **P0 — 完成资产装配**：重启 Editor 后人工创建并 Compile 两份 PCA，再创建玩家/追猎者 HeavyImpact DataAsset，装配角色 CDO 与摆锤 500 cm 预测距离，精确编译/保存并回读。
3. **P0 — 验证核心效果**：先跑 `Demo.Physics.HeavyImpact.*`，再跑相关 GameFlow/Demo 回归；PIE 覆盖玩家/追猎者、误预测、连续冲击、墙边/斜坡/角落与 Downed 二次碰撞。
4. **P1 — 回到可玩内容**：重冲击原型通过后，再决定摆锤在机关房中的路线职责，并继续全部楼梯/旋转组合、真实追猎者完整跨层追逐与整局联合回归。

## 风险与优化（按优先级）

- **P0 阻塞**：当前 `UnrealEditor-Demo.dll` 时间为 20:25，早于 21:51 后的重冲击源码；运行中的 Editor/Live Coding 锁住 DLL，当前资产与蓝图审计只能证明旧模块状态。
- **P0 缺口**：PCA/HeavyImpact DA 不存在；即使蓝图显示 UpToDate，新状态组件也无法初始化，不能把 C++ 无链接编译当成功能完成。
- **P1 玩法边界**：首版 Downed 没有起身桥，必须先验证真实落点、相机、碰撞与整局失败/恢复语义，避免原型让正式一局永久卡死。
- **P1 机关边界**：当前摆锤仍是 Level0 独立测试房，不是 PCG Population 已投放内容；机关房型与“追猎者一次迟滞后必定通过”的规则仍未确定。
- **可延后**：900 Seed 校准、UI 美术和 Physics Asset 艺术化质量分布应排在重冲击运行闭环与整局联合回归之后。

## 推进可玩内容的新 idea

**一次性追逐断点房**：把摆锤放在窄通路与侧向避险凹室之间；玩家可诱导摆锤首次重击追猎者换取固定短窗口，触发后机关自动进入不会再次阻断 AI 的通行状态。它直接服务追逐节奏，并明确满足“只迟滞一次、之后必定通过”，但需等重冲击 Downed/恢复语义通过验收后再讨论实现。

## AI 经验（最多 3 条）

- Blueprint 显示 UpToDate 只证明当前已加载类可编译，必须结合 DLL 时间与 CDO 字段确认新模块是否真正加载。
- `-NoLink` 编译只能证明源码/UHT 阶段，不能替代完整链接、自动化与 PIE。
- 依赖 PCA 的状态组件在 PCA/DA 缺失时必然停在初始化门禁，资产存在性应先于效果调参检查。

## Git 快照

- 待本报告与 Project Memory MCP 更新完成后，核对唯一允许的内部工蜂 origin，再执行普通快照提交与 `git push origin main`。
