# Demo 夜间报告 — 2026-07-25

## 今日完成与验证

- PCG V3.2 已把旧 Graph/Socket/Portal/Catalog/A*/回溯 Solver 链替换为构造性 Progression、Grid Layout、16 Mask 无回溯 WFC、600→300 cm 结构展开与 Runtime HISM；代码审计显示旧 `ZeroEscapeLayoutSolver` 删除，新 `ZeroEscapeGridLayoutSolver` / `ZeroEscapeWfcSolver` 接管职责。
- 最新只读日志发现 `Demo.PCG` 13 项且 13/13 Success；Seed Sweep 覆盖 3 难度 × 3 Flow × 32 Seed。NewWindow PIE 证据为 27 Cells、444 Instances、5 HISM，Harness Teleport/Transfer 成功。
- Demo 已升级到 UE 5.8；内部工蜂实施前快照为 `667c49e39e3d70970a398d04f61bab6df20f004d`。
- UE Editor MCP 本轮 `pong=false`，官方 UE5.8 MCP 工具未暴露：**蓝图审计未执行**，未保存任何蓝图、关卡或资产。

## Git 快照

- 快照前 `main...origin/main` 为 0/0，工作树有大量待备份变化。
- `origin` 已精确核验为 `git@git.woa.com:shiqiqiwang/Demo.git`；本报告完成后仅执行 `git add -A`、`Nightly snapshot 2026-07-25` 提交和普通 `git push origin main`，最终哈希与推送结果见自动化最终回执。

## 明日最优先任务

1. 用户确认是否允许仅在第三方根材质 `/Game/SciFiHydroLab/Materials/Parents/M_HydroLab` 启用 `Used with Instanced Static Meshes`；获准后用正常渲染 NewWindow PIE 复验零 Usage 警告。
2. 用户实际检查材质、接缝、碰撞、净空并走通 Start→Exit；该门禁通过前不切换到追猎者实现。
3. 验收通过后，复用生成结果中的 Gameplay Anchor 接入最小追猎者与出口触发，先形成单局闭环。

## 新内容 / 玩法建议（提案，未采用）

- “磁力保险丝逃生”：Objective 房生成可搬运保险丝；送入插槽恢复出口供电，同时触发追猎者进入地图。它复用磁力抓取、PCG Objective Anchor 和出口状态，能用较少新增系统形成“探索—搬运—追逃—撤离”闭环。

## 风险与优化（按优先级）

- **P0 交付阻塞**：四个 HydroLab 材质实例缺 `InstancedStaticMeshes` Usage，当前会回退默认材质；等待第三方根材质最小修改许可。
- **P0 验收缺口**：构建、13/13 自动化、288 Seed Sweep 与 PIE 日志均不替代正常材质、接缝、碰撞、净空和玩家走通。
- **P1 仓库卫生**：工作树包含 `$null)`、`tmp/` PDF 渲染物、嵌套 `Content/Content/`、多个工程/素材导入项；夜间不清理，白天应先确认来源与保留范围。
- **P1 任务治理**：两个活动任务共用 `TASK-20260724-001`，且素材筛选/追猎者任务会分散 PCG 验收；建议保留 PCG 为唯一关键路径并在白天重编号或归档。
- **可延后**：在玩家走通前，不继续抽象 PCG 通用框架，也不投入低回报素材精修。

## AI 经验

- UE MCP 注册存在不等于编辑器在线，`pong=false` 时必须明确写“蓝图审计未执行”。
- 生成器的自动化与 Seed Sweep证明算法契约，正常视口 PIE 才能证明材质、碰撞和通行体验。
- 第三方素材的单点设置阻塞应先请求最小直接授权，不用项目副本或映射层绕开。
