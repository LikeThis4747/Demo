# Demo 夜间只读报告 — 2026-08-20

## 今日完成与验证

- 2026-08-19 白天形成 6 个后续提交：局内 BGM 生命周期、菜单灵敏度/音乐/音效设置、磁力/追猎者/脚步音效、三层楼层目标方向箭头，以及答辩状态保存；C++ 变化为 20 个文件、648 行新增、3 行删除。
- 现有构建证据：`UnrealEditor-Demo.dll` 为 15:29，Shipping 二进制为 15:42，Staged 包为 15:45，日志记录 `BUILD SUCCESSFUL`。17:03 仍有 DA、GameMode Blueprint 和 `Demo.uproject` 提交，当前 `Demo.uproject` 另有恢复 `ModelContextProtocol` 的未提交改动，因此现包不代表最终工作树。
- 两个 UE MCP 今夜均在线。Level0 当前打开且 PIE 停止；Level0、L_Game、正式 GameMode/HUD、主菜单、追猎者/磁力 DA 均非 Dirty。正式 GameMode 的 BGM、磁力音效、追猎者命中/起手音效均已装配，追猎者攻击开关为 true，主菜单目标关卡为 L_Game。
- 未执行构建、自动化、PIE、蓝图编译或资产保存；未自动修复任何问题。

## Git 快照

- 待执行：仅在 `origin` 精确等于 `git@git.woa.com:shiqiqiwang/Demo.git` 后执行 `git add -A`、`Nightly snapshot 2026-08-20` 和普通 `git push origin main`。

## 明日最优先任务

1. 在最终正式 L_Game/Shipping 中完整跑一局：动态 Recast、真实追猎者跨层追逐、楼层箭头、音频、机关、光团、出口、死亡/重开。
2. 复核 HeavyImpact 重复 preparation timeout、penetrating start 恢复，以及追猎者隐藏重放置。
3. 用当前最终资产/配置重新打包，并在目标机验证 BGM、菜单音量、磁力/攻击/脚步音效与设备切换。

## 新内容 / 玩法建议

- 把现有楼层箭头与 BGM 做成“三层追逃节奏”：每次跨层只增加一次短促音乐层或提示音，顶层光团/出口解锁再切入终局层，直接强化可玩目标而不新增系统。

## 风险与优化优先级

- **P0 阻塞交付**：最新 13:38 PIE 实际运行 `Level0 + BP_ZeroEscapePrototypeGameMode`，Level0 的 `bEnableNavigationSystem=false`；同轮结束仍报告缺少 Recast，因此不能作为正式 L_Game/真实追逐验收。
- **P0 阻塞交付**：同轮反复出现 HeavyImpact 准备超时和“penetrating start”恢复警告，并触发追猎者隐藏重放置；先复现和玩家验收，再决定是否修复。
- **P1 高回报**：音频接入后编辑器日志反复出现 WASAPI 设备丢失、空设备交换和 48k/44.1k 重采样；需在目标机做可听性与设备切换检查。
- **P1 高回报**：15:45 包早于 17:03 资产/config 保存；最终交付包需重做并核对哈希/时间戳。
- **P2 可延后**：`f6093e6b` 一次 checkpoint 涉及 2329 个文件，且活跃任务卡、临时脚本仍较多；白天由各 Owner 核对归属后再整理，夜间不删除。

## AI 经验

- 静态 Recast Actor 存在不代表导航系统已启用，必须同时核对 WorldSettings 和同轮运行日志。
- Shipping/Pak 成功只覆盖生成时点，之后的资产与配置提交必须重新打包才能进入最终验收。
- 音频功能完成至少需要引用回读、真实播放和设备切换三类证据。
