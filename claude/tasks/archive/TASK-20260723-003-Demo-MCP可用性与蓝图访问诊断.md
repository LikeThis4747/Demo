# TASK-20260723-003 Demo MCP 可用性与蓝图访问诊断

> 状态：已完成并归档（2026-07-24 整理）。检查点全部完成，实施结果见文末。

- Owner: Codex `/root`
- 状态: 已完成 — Demo 项目级 MCP 已恢复，工具注册验证完成
- 范围: 只读核对 Demo 的 Project Memory MCP、UE Editor MCP、当前 Codex 工具注册和蓝图访问能力
- 不在范围: 修改 UE 资产、项目代码、全局 MCP、停止或删除现有服务
- 已知配置:
  - `D:\UE5projects\Demo\.vscode\mcp.json`（只读）
  - `C:\Users\shiqiqiwang\.codex\config.toml`（只读）
- 修改文件: `D:\UE5projects\Demo\.codex\config.toml`

## 检查点

- [x] 核对 Demo `.vscode/mcp.json`
- [x] 核对相关进程是否运行
- [x] 核对当前任务可调用工具
- [x] 核对 Codex MCP 注册状态与项目 MCP 手册
- [x] 尝试 UE Editor 健康检查与蓝图只读查询
- [x] 给出是否恢复及最小配置方案

## 当前证据

- Demo 三项 MCP 配置仍存在，未被清空。
- Demo Memory/UE Editor MCP Python 进程正在运行。
- 当前 Codex 任务未注册 UE Editor 或项目 Memory MCP 工具，暂不能执行 `ue_ping` 或蓝图查询。
- 根因：Demo 仅有 `.vscode/mcp.json`，缺少 Codex Desktop 使用的项目级 `.codex/config.toml`；全局配置中的同名服务仍指向已禁用的 ue5-warrior。
- 直接 MCP 客户端验证：`ue_ping` 成功，`editor.list_assets` 从 `/Game` 返回 16 个蓝图类资产。
- `blueprint.describe_full(BP_MagneticProp)` 成功，编译状态 `UpToDate`，父类 `/Script/Demo.MagneticPrototypeProp`，返回 2 个图和 4 个节点。
- Demo Memory MCP 成功列出 6 个工具，并通过 `memory_get` 读取三份启动上下文。
- 建议：创建 Demo 项目级 `.codex/config.toml`，以同名表覆盖全局禁用项并指向 Demo；需用户明确授权后实施。

## 实施结果

- 用户已明确允许恢复 Demo 项目级 MCP。
- 已创建 `.codex/config.toml`，启用 Demo 的 `project-memory-mcp`、`ue-editor-mcp-logs`、`ue-editor-mcp`。
- TOML 解析通过。
- 在 Demo 根目录执行 `codex mcp list`：三项均为 `enabled` 且路径/`--root` 均指向 Demo；RenderDoc 两项保持 `disabled`。
- 待 Codex 重启或新建任务后，使用注册工具再次执行 Memory 启动读取、`ue_ping` 与 `blueprint.describe_full`。
