# TASK-20260723-001 — 优先级与任务上下文收敛

- Owner：Codex / root
- Status：done
- Stage：已完成
- Created：2026-07-23
- Updated：2026-07-23

## 目标与验收

- 目标：确认 PCG → 追猎者 → 生成地图玩法闭环的依赖顺序；将磁力调优降为后续方向；核验 Demo MCP；收敛活跃任务上下文。
- 验收：`claude/tasks/active/` 只保留当前关键路径任务；历史、暂停和后续优化记录可追溯；不修改玩法代码、资产或项目配置。
- 非目标：不实现 PCG、追猎者或磁力修复；不终止外部进程；不修改 Codex 全局配置。

## 修改范围

- 允许修改：本任务卡、`claude/tasks/active/` 与 `claude/tasks/archive/` 中的 AI 任务记录。
- 共享/潜在冲突：不修改其他 AI 当前拥有的玩法文件；只移动非当前关键路径记录，并收敛保留卡的优先级文字。
- 并行拆分/依赖：任务卡与 MCP 均只读审计可并行；最终由 root 统一调整任务记录。
- 用户已授权范围：用户明确要求检查工具风险并调整协作风险，避免活跃卡过多影响上下文。

## 计划与检查点

- [x] 核验项目内 MCP 配置、全局 Codex 配置、当前工具暴露与相关进程。
- [x] 分类活跃任务卡，以单一 PCG 当前卡承载关键路径。
- [x] 将其余历史/暂停/后续优化卡移出 active，并验证没有信息丢失。

## 验证

- 命令/场景：`git status --short`、任务卡清单/大小、MCP 配置与进程只读检查。
- 结果：
  - `.vscode/mcp.json` 的 Memory 与两项 UE MCP 均正确指向 `D:/UE5projects/Demo`，UE 运行端注册 166 个动作且只读 ping 成功。
  - 当前 Codex 会话未暴露自定义 MCP；全局 `C:/Users/shiqiqiwang/.codex/config.toml` 仍保留指向 `ue5-warrior` 且 `enabled=false` 的旧配置，因此根因位于 Codex 加载层，不在 Demo 服务端。
  - 发现 5 个 `CodeBuddy CN` 实例重复启动 5 套 Demo MCP，共 20 个 Python 进程；连同父进程约占 2.64 GB 工作集。它们不是当前 Codex 启动的，本轮未擅自终止。
  - 活跃任务由 12 张收敛为 1 张，重复的 `TASK-20260722-002` 已将蓄力记录更正为 `TASK-20260722-006`。
- 用户操作（时机、步骤、预期结果）：无需操作。
- 用户验收：待验收

## 阻塞、风险与下一步

- 阻塞：当前 Codex 会话未暴露 Demo Memory MCP 或 UE MCP 工具。
- 风险：误把尚未完成的功能写成已完成；归档时保留原任务卡内容，不更改功能验收事实。
- 下一步：只保留 PCG 整关场景当前卡；完成该检查点后再新建追猎者实施卡。MCP 恢复需另行授权修改 Codex 配置并完全重启，随后依次验证工具暴露、`ue_ping`、Memory 工程根回读和 `memory_guard_check`。
