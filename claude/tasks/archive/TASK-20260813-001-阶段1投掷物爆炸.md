# TASK-20260813-001 — 阶段1投掷物爆炸

- Owner：Codex / 当前会话
- 任务类型：实现
- 实现文件写入权：已认领（2026-08-14，直到本任务完成技术验证或主动交接）
- Status：completed（待归档）
- Stage：实现、完整构建、资产装配、自动化回归与用户 PIE 击飞验收均已完成
- Created：2026-08-13
- Updated：2026-08-14

## 目标与验收

- 目标：按已确认的普通/爆裂两态实现爆裂投掷：持有时按 E 激活但不扣次数，满 1 秒后可按 E 取消；只有爆裂状态正式投掷成功建立事务后扣 1 次，首次合格碰撞对玩家与追猎者产生范围 Heavy、伤害和增强碎片。
- 验收：普通投掷完全不变；激活/取消不扣次数；爆裂正式投掷只扣一次并只爆一次；未命中超时不返还；玩家和追猎者各至多收到一次同源距离衰减伤害与径向 Heavy；目标处于任意 Heavy 阶段仍可被另一场爆炸再次推动且最终正常恢复；爆裂碎片增强但不产生二次伤害；全部可调手感来自现有 `DA_MagneticGrabTuning` 独立参数组。
- 非目标：本步不制作红光、爆炸特效、音效和 HUD；不实现阶段一的光球生成、出口锁或阶段二守场；不把范围冲击伪装成现有“真实刚体接触” Heavy Impact，也不修改项目碰撞通道。

## 修改范围

- 允许修改：用户已授权修改 DailyPlan 所列的既有 C++、输入资产和现有磁力 DataAsset；实现未扩展到其他玩法文件。
- 共享/潜在冲突：`TASK-20260814-001-HeavyImpact误触发与恢复卡死诊断.md` 已完成共享 Heavy C++ 修正、构建和自动化，正在等待用户 PIE 复验；本功能必须沿用其不可回滚和唯一恢复基线合同。当前工作区另有其他任务持有的文档、Memory 改动和未跟踪任务卡。
- 并行拆分/依赖：本功能依赖现有正式投掷 Hit、破碎 next-tick 替换和 Heavy 恢复状态机；按“配置输入 → 两态扣次 → 径向 Heavy → 爆炸/伤害/碎片 → 资产验证”单一顺序实施，不并行修改共享文件。
- 用户已授权范围：已授权按 DailyPlan 的最小闭环修改列出的 C++、输入资产和磁力 DataAsset；不得扩展到其他玩法代码、关卡、第三方资产或未确认的安全性重构。设计冲突必须先报告。
- 并行纯文档（只读、不暂存、不提交）：PCG 任务持有 `DOC/README.md`、`DOC/Design/PCG/HAZARD_PLACEMENT_RULES.md`、`claude/tasks/active/TASK-20260814-002-PCG机关节奏放置方案评审.md`；规则调整任务持有 `AGENTS.md`、`DOC/AI_WORK_GUIDELINES/AI_WORKFLOW.md`、`DOC/AI_WORK_GUIDELINES/GIT_INTERNAL.md`、`claude/templates/TASK_TEMPLATE.md`、`memory-bank/daily.md`、`memory-bank/decisions.md`、`claude/tasks/archive/TASK-20260814-003-AI并行任务规则调整.md`；两天收口盘点持有 `claude/tasks/active/TASK-20260814-003-两天游戏完整性收口盘点.md`。

## 计划与检查点

- [x] 阅读 `DOC/Design/ENDGAME_GAMEPLAY_DESIGN.md` 与项目架构/工作流/MCP 规范。
- [x] 完整审计磁力抓取、正式投掷、破碎、输入、能量与 Heavy Impact 真实代码和 UE 资产装配。
- [x] 收敛普通/爆裂两态、投出才扣次数、玩家与追猎者均受影响、伤害、碎片增强和现有 DA 参数组。
- [x] 创建 `DOC/DailyPlan/2026-08-14-阶段1爆裂投掷实施方案.md`，列出完整文件/资产范围、检查点、回退与验收。
- [x] 在对话中展示拟实现代码，等待用户审阅。
- [x] 根据用户意见删除径向入口的统一 Busy 拒绝，修订为六种 Heavy 状态均可受爆炸、但只保留一份恢复基线的状态化方案与代码预览。
- [x] 用户已审阅修订方向并明确授权功能落盘；Heavy Impact 诊断任务没有新的共享实现文件改动。
- [x] 实现基线核验通过：实现范围无既有改动、暂存区为空；本地 HEAD、`origin/main` 与工蜂远端 `main` 均为 `8f727421fb1cdb580f19846a67d1305b270b06a5`；其他 Owner 的纯文档保持未暂存。
- [x] 落盘最小 C++ 闭环：普通/爆裂两态与投出扣次、独立径向 Heavy、范围伤害、单次爆裂破碎倍率、追猎者生命组件和输入引用。
- [x] `DemoEditor Win64 Development -NoLink` 通过；UHT 与 18 个本任务源文件编译成功。
- [x] 官方 MCP 创建并保存 Boolean `IA_MagneticExplosionMode`；`IMC_ZeroEscape` 保留原三条映射并只追加 `E`，保存后回读未脏。
- [x] 编辑器关闭后完成正式链接、重启与官方 MCP 资产装配。
- [x] 重跑全部 5 项 `Demo.Physics.HeavyImpact.*`：5/5 Success，0 失败、0 警告。
- [x] 用户 PIE 验证爆炸击飞效果可用，并授权完成收尾；下一步转入爆炸表现制作。

## 验证

- 命令/场景：`DemoEditor Win64 Development` 完整构建；官方 MCP 资产回读、角色蓝图编译与 CDO 组件检查；`Demo.Physics.HeavyImpact.*` 自动化；用户 PIE 投掷验证。
- 结果：完整构建成功。输入动作、E 映射、输入 DA 与磁力 DA 回读正确且均已保存；`BP_Pursuer`、`BP_ZeroEscapeCharacter` 编译成功；Heavy 自动化 5/5 通过。PIE 日志确认普通投掷仍走 Light 与 `350 cm/s` 碎片，爆裂投掷可同时伤害并击飞玩家/追猎者，爆裂碎片为 `875 cm/s`。
- 编辑器备注：PIE 运行中工具编译蓝图曾因现有 `HealthComponent::BeginPlay` 重复 `AddDynamic` 触发 handled ensure；编辑器未退出，用户确认不为仅编辑器热编译场景调整运行时代码。后续只在停止 PIE 后编译蓝图。
- 用户验收：2026-08-14 用户确认爆炸击飞效果可用，并要求完成收尾。

## 阻塞、风险与下一步

- 阻塞：无。
- 已知边界：接触式 `PrepareForImpact` 的 Busy 合同保持原样；径向入口允许所有 Heavy 阶段追加冲量。追猎者生命归零后果、能量光球和爆炸表现不在本任务内。
- 下一步：单独规划并制作爆炸状态红光与爆炸瞬间特效，复用本任务预留的爆裂状态变化事件。
