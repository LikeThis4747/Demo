# TASK-20260816-005 — PCG 楼梯蓝灯与磁力拾取闪光

- Owner：Codex `/root`
- Status：active
- Stage：待用户验收
- 任务类型：实现
- 实现文件写入权：Codex `/root` 独占
- Created：2026-08-16
- Updated：2026-08-17

## 目标与验收

- 目标：保留 PCG 楼梯全部普通顶灯，在楼梯下层入口外侧和上层出口外侧额外放明显的蓝色点光源；自由状态的磁力投掷物进入现有可拾取候选范围后闪蓝光，拿到手上立即停止。
- 验收：用户在正式 PCG 关卡中检查楼梯引导方向、两类灯亮度，以及磁力物靠近/拿起时的蓝光切换。
- 非目标：不改 WFC、楼梯拓扑、拾取判定、投掷/爆裂逻辑，不新增常驻 Tick，不扩展成通用灯光或交互提示系统。

## 修改范围

- 允许修改：
  - `Source/Demo/Private/PCG/Presentation/ZeroEscapeStructureBuilder.h`
  - `Source/Demo/Private/PCG/Presentation/ZeroEscapeStructureBuilder.cpp`
  - `Source/Demo/Public/PCG/ZeroEscapeRuntimeLevelGenerator.h`
  - `Source/Demo/Private/PCG/ZeroEscapeRuntimeLevelGenerator.cpp`
  - `Source/Demo/Public/Data/Magnetism/MagneticGrabTuningData.h`
  - `Source/Demo/Public/Components/Magnetism/ElectromagneticGrabComponent.h`
  - `Source/Demo/Private/Components/Magnetism/ElectromagneticGrabComponent.cpp`
  - `/Game/ZeroEscape/Data/Magnetism/DA_MagneticGrabTuning`
  - `/Game/ZeroEscape/Interaction/Magnetism/FX/MI_MagneticPickupBlueOverlay`
- 共享/潜在冲突：工作区现有 PPT 脚本、输出与任务卡属于其他文档任务，本任务不修改、不暂存、不提交。
- 用户已授权范围：允许自行完成 C++/蓝图/资产改动，要求严格控制改动量，不要求测试，等待用户验收。

## 计划与检查点

- [x] 记录基线：`a8801f6c32cf50c7378cd29f5e4b3448f0fa009e`；本地、`origin/main` 与内部远端一致。
- [x] 回读正式楼梯灯位、磁力蓝图父类与抓取范围（当前 `GrabRange=1800cm`）。
- [x] 根据用户现场反馈撤销对楼梯固定顶灯的错误替换，全部恢复为原普通灯；蓝色入口/出口灯改由最低层和最高层实际结构 Opening 额外生成。
- [x] 根据第二次现场反馈取消入口向下聚光：入口/出口都改为蓝色点光源，移到连接楼梯的普通格中心并降低高度，同时提高强度和半径。
- [x] 用现有候选选择函数和单个低频 Timer 控制蓝色 Overlay；抓取前清除，不新增 Actor/Component Tick。
- [x] 保存 C++ 与两项资产，保持状态为“待用户验收”。

## 验证

- 命令/场景：按用户要求不运行自动化、PIE 或正式测试；只执行 `DemoEditor Win64 Development` 必要编译，以及官方 MCP 资产保存/回读。
- 结果：2026-08-17 入口/出口灯位修正后 Development Editor 编译成功；`GrabRange=1800cm`，蓝色 Overlay 引用与颜色 `(0.03, 0.30, 1.0)` 已保存且两项资产均非 Dirty。
- 用户验收：待验收

## 阻塞、风险与下一步

- 阻塞：无。
- 风险：点光源强度和半径仍需用户实玩后确认；本轮蓝灯位于楼梯外侧接近区，不占用固定顶灯位。
- 下一步：完成最小实现并交给用户进正式 PCG 关卡验收。
