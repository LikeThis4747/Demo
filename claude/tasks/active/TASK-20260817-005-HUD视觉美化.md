# TASK-20260817-005 — HUD 视觉美化

- Owner：CodeBuddy 当前会话
- 任务类型：实现（修改 UMG 蓝图资产）
- 实现文件写入权：占用 `Content/ZeroEscape/UI/WBP_GameplayHUD.uasset`
- Status：active
- Stage：基线已记录，等待用户确认方向后落盘
- Created：2026-08-17
- Updated：2026-08-17

## 目标与验收

- 目标：美化游戏内 HUD（能量条/血条/按键块/底板），去掉 `####` 调试文本条，统一工业科幻风配色。
- 验收：PIE 运行截图确认 HUD 干净、进度条正确随数值变化、无重复显示；蓝图编译保存通过。
- 非目标：不改任何 C++ 逻辑；不改 HUD 的数据绑定。

## 修改范围

- 允许修改：`Content/ZeroEscape/UI/WBP_GameplayHUD.uasset`（通过 ue58-official-mcp `set_properties` 设默认样式，不改变量）。
- 只读输入：现有 widget 树结构（已取证）。
- 并行边界：不修改其他会话拥有的资产。

## 美化方案（工业科幻风，纯 Slate RoundedBox，无贴图）

1. **隐藏调试文本条**：`EnergyMeterText`、`HealthMeterText` 设 Collapsed（消除 `####`）。
2. **进度条**：能量条青色发光填充、血条红色发光填充，背景深色半透明，RoundedBox 圆角。
3. **按键块**（LMB/RMB/E）：亮青实色块 → 深色半透明底 + 亮色描边圆角，文字缩小一号。
4. **HUDPanel**：改深色半透明圆角底，统一容器感。
5. **保留数据绑定**：只改默认样式，不改动 `EnergyBar`/`HealthBar`/`ChargesText` 等变量的运行时更新。

## Git 基线

- HEAD：`a158a934a46039e59803d88179b55233563d5965`
- 既有未提交改动均为文档类（DOC/claude/memory-bank/.ai-context），与 HUD 资产无重叠；`WBP_GameplayHUD.uasset` 不在未提交列表，说明当前磁盘=HEAD 干净状态。

## 阻塞、风险与下一步

- 风险：set_properties 对 `RoundedBox` 圆角颜色的写法需验证；若官方 MCP 设不动某属性，回退到 ue-editor-mcp `widget.set_properties`。
- 下一步：用户确认配色方向（默认工业科幻）→ 落盘 → PIE 截图验证。
