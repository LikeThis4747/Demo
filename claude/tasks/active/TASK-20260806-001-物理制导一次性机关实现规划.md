# TASK-20260806-001 — 物理制导一次性机关实现规划

- Owner：Codex `/root`
- Status：active
- Stage：独立 V1 已落盘并通过静态验证与 Demo 模块构建；等待用户 PIE 验收
- Created：2026-08-06
- Updated：2026-08-06

## 目标与验收

- 目标：为“壁挂式一次性物理制导机关”形成可实施方案：正面墙或拐角墙摆位承担主要瞄准，机关锁定首次进入的目标，在可调推进时间内由真实施力驱动并做小角度修正；首次有效碰撞后立刻停止制导但继续推进至计时结束，之后完全交给 Chaos 反弹与滑行。
- 验收：C++ 与资产落盘；两个 Blueprint 编译、保存且 EventGraph 为空；Level0 正面墙与拐角墙实例完成静态净空核对；关闭编辑器后仅构建并链接 Demo 模块；PIE 与手感验收由用户次日执行。
- 非目标：不实现玩家/追猎者受击表现；不接入 PCG Population；不做循环发射、对象池、伤害、联网、自动纠偏复位或为假设中的碰撞抖动预写过滤框架；本次不抽取 HeavyImpact 预测公共层。

## 修改范围

- 实际新增 C++：`Source/Demo/Public/Actors/Hazards/ThrustGuidedHazardLauncher.h`、`Source/Demo/Private/Actors/Hazards/ThrustGuidedHazardLauncher.cpp`、`Source/Demo/Public/Actors/Hazards/ThrustGuidedHazardProjectile.h`、`Source/Demo/Private/Actors/Hazards/ThrustGuidedHazardProjectile.cpp`、`Source/Demo/Public/Data/Hazards/ThrustGuidedHazardTuningData.h`、`Source/Demo/Private/Data/Hazards/ThrustGuidedHazardTuningData.cpp`。
- 实际新增资产：`/Game/ZeroEscape/Hazards/Physics/BP_ThrustGuidedHazardLauncher`、`/Game/ZeroEscape/Hazards/Physics/BP_ThrustGuidedHazardProjectile`、`/Game/ZeroEscape/Hazards/Physics/Data/DA_ThrustGuidedHazard_Default`、`/Game/ZeroEscape/Hazards/Physics/Materials/PM_ThrustGuidedHazard`、`/Game/ZeroEscape/Hazards/Physics/Materials/M_ThrustGuidedHazard_Body`；修改 `/Game/Levels/Level0`，复用 `PCG_Test/Room900` 放置两个测试实例。
- 共享/潜在冲突：`IHeavyImpactReceiver` 与 HeavyImpact 类型只读；不改 `PhysicsControlHitResponseComponent`、玩家/追猎者 Blueprint、动画/PCA/受击 DataAsset、PCG 生成代码或现有摆锤/冲锤资产。
- 并行拆分/依赖：依赖另一活动任务提供稳定的重冲击输入合同，但本机关只上报真实接触数据与本次发射唯一标识，不依赖其内部状态；实施前还依赖所有并行任务确认当前改动归属并完成全工作区 Git 基线。
- 用户已授权范围：已明确允许开始实现；V1 先独立完成，功能稳定后再评估 HeavyImpact 预测公共层。

## 计划与检查点

- [x] 固化当前策划规则与非目标。
- [x] 读取项目记忆、架构/工作流、双 MCP 规范、物理候选 Idea 与并行任务边界。
- [x] 审计现有 Hazard、物理投掷、重冲击接口和 PCG/Level0 结构。
- [x] 用官方 UE MCP 核对 Level0、既有 Hazard Blueprint/DataAsset 与测试空间。
- [x] 定稿状态机、制导与推进实现、碰撞语义、参数和文件级架构。
- [x] 吸收正面墙/拐角墙摆位：Muzzle +X 为发射基准，TriggerAnchor 可独立摆放，制导只补偿路线内横移。
- [x] 输出六文件完整代码预览、Blueprint 装配、Level0 双场景验证矩阵、风险分级和实施门槛，等待用户确认。
- [x] 用户审阅后决定暂不抽公共层，并明确授权开始实现。
- [x] 实施前完整基线 `5983d11ff9c8d9158188e77b627b1e6dbca6fd04` 已推送到内部工蜂；本地/远端一致且工作区干净。
- [x] 新增六个独立 C++ 文件；无链接编译和正式 Demo 模块链接均通过。
- [x] 创建/配置 DataAsset、Launcher/Projectile Blueprint、物理材质和简易可视组件；Blueprint 以警告视为错误编译通过。
- [x] 在 Level0 的 `PCG_Test/Room900` 静态布置正面墙与拐角墙版本，完成出生净空与发射走廊射线核对并保存。
- [x] 正常关闭 UE 编辑器，以 `-Module=Demo -NoEngineChanges` 完成最终 Demo 模块构建；未编译引擎。
- [ ] 用户次日在 PIE 中验收玩家/追猎者触发、首碰失导、连续反弹与手感。

## 验证

- 命令/场景：先执行 Demo 模块无链接编译；资产创建后由官方 UE MCP 回读父类、CDO、空 EventGraph、组件与材质配置；对两个 Muzzle 做 200 cm 出生净空和完整房间发射走廊静态射线；保存后正常关闭编辑器，再执行 `Build.bat DemoEditor Win64 Development Demo.uproject -Module=Demo -NoEngineChanges -NoHotReloadFromIDE -WaitMutex`。
- 结果：六个 C++ 文件通过 UHT/编译和正式 DLL 链接；最终关闭编辑器后的 Demo 模块构建 `Result: Succeeded`，目标已最新、0 个动作，没有编译引擎。Launcher/Projectile Blueprint 父类正确、EventGraph 为空、以警告视为错误编译通过；DataAsset、物理材质和 CDO 引用回读正确。Level0 中两个实例位于 `Hazards/Physics/ThrustGuidedHazardTest`，前 200 cm 净空，完整走廊分别约 730/760 cm 后命中远端墙。局部材质编译辅助工具虽返回 0 error/0 warning，但在渲染线程产生一次 handled ensure；随后官方资产验证与最终 Demo 构建均通过，运行期仍以用户 PIE 为准。
- 用户操作（时机、步骤、预期结果）：次日在 Level0 PIE 中分别从正面路线和拐角路线进入触发区，并让玩家/追猎者各触发一次；预期锁定首个角色、预警后发射、首次碰撞失导但推进计时继续，之后由 Chaos 多次反弹。
- 用户验收：待验收

## 阻塞、风险与下一步

- 阻塞：当前无静态交付阻塞；PIE 与玩法手感验收按用户要求延期。
- 风险：制导过强会看起来像脚本转向，制导过弱则首段无威胁；拐角 TriggerAnchor 若放在墙后会产生隔墙锁定或出生撞墙；高转速和高速接触可能产生 CCD/求解不稳定；“首次有效碰撞”必须排除发射器/出生重叠；受击接口不能把每个 Chaos 回调都直接当成一次重击。
- 下一步：完成本任务完整内部提交/推送与远端核验；用户次日 PIE 后再根据命中率、制导观感和 Chaos 反弹稳定性调参。
- 用户追加交付要求：AI 自行关闭 UE 编辑器完成正式构建和完整提交；不执行 PIE。提交释放后通知“物理受击交互方案”任务先读 Review，再实现、完整构建并提交。

## 2026-08-06 现场 Review 布局修正

- 用户指出原两个实例错误地放在远离当前物理机关测试区的 `PCG_Test/Room900`；该位置不利于发现和横向对照，必须迁出。
- 真实坐标复核：`PlayerStart` 位于约 `(51960, 28380, 92)`；摆锤房 `PendulumTestRoom` 结构中心约 `(52000, 30300)`；冲锤低顶走廊位于摆锤房画面左侧（世界 +X）。
- 新布局：在摆锤房画面右侧（世界 -X）新建独立低天花板 90° 转角走廊，与左侧冲锤房、中间摆锤房形成并排对照；走廊复用既有 HydroLab 300 cm 模块，不修改第三方资产。
- 新走廊冻结范围：主体约 `X=49300..50800`、`Y=29700..30900`、`Z=0..300`；南侧进入，沿纵向直段进入北侧转角，再向世界 -X 转出。与摆锤房西缘保留约 900 cm 间距，对称于冲锤房与摆锤房的分隔。
- 迁移既有两个 Launcher：一个位于纵向直段尽头正面墙，朝世界 -Y 发射；一个位于转角东侧墙，朝世界 -X 发射。拐角版本单独后移 TriggerAnchor，避免目标刚转弯便贴近 Muzzle 触发。
- 本次只修改 `/Game/Levels/Level0` 与任务/计划/日报记录，不修改六个机关 C++ 文件、机关 Blueprint/DataAsset/材质、玩家起点、摆锤房、冲锤房或 PCG 代码。
- 修正完成并形成新的完整提交/推送、远端核验与干净工作区后，再通知“物理受击交互方案”任务读取其最新未归档拟实现代码 Review，随后开始实现起身代码。

### 布局修正落盘与静态复核

- 已在 `Hazards/Physics/ThrustGuidedHazardTestCorridor` 创建 45 个房间构件：14 个地板、14 个低顶、14 段墙和 3 盏灯；连同迁入的 2 个 Launcher，根文件夹递归回读共 47 个 Actor。
- 结构包围盒回读约为 `X=49300..50804`、`Y=29700..30904`、`Z=-4..304`，与冻结的 300 cm 高 L 形走廊范围一致；未改 PlayerStart、摆锤房、冲锤房和第三方素材本体。
- 旧文件夹 `Hazards/Physics/ThrustGuidedHazardTest` 已无 Actor 并由编辑器自动移除；没有在旧 `PCG_Test/Room900` 留下机关副本。
- 直段 Launcher 回读为 `(50500, 30900, 150)`、Yaw `-90°`；转角 Launcher 为 `(50800, 30600, 150)`、Yaw `180°`；转角 `TriggerAnchor.RelativeLocation.X` 为 `1100 cm`。
- 纵向/横向走廊中心线以及两个 Muzzle 前 200 cm 静态射线均无命中；Level0 已保存。本轮未运行 PIE，运行时触发与反弹仍留给用户验收。
- UE 编辑器已正常关闭；仅执行 `DemoEditor Win64 Development -Module=Demo -NoEngineChanges -NoHotReloadFromIDE -WaitMutex`，结果 `Succeeded`、目标已最新、0 个动作，没有编译引擎。
