# TASK-20260806-001 — 物理制导一次性机关实现规划

- Owner：Codex `/root`
- Status：active
- Stage：当前 `900 cm / 18° / 50 kg / 8 s` 基础机关版本已获用户验收；准备先提交验收版，再进行旧代码与文档权威入口清理审计
- Created：2026-08-06
- Updated：2026-08-10

## 当前目标

把上一轮“短时推进、飞行中制导”的机关改成：

```text
发射前预测移动交点
    -> 炮管真实转向
    -> 从刚体真实质心施加一次冲量
    -> 发射后只由 Chaos 计算重力、碰撞、反弹和休眠
```

默认直行路线应有较高命中可能；玩家在预警期急停、后退、横移或在下游放置物理资源都可以改变结果。数学无精确解不作为取消射击的理由。

拟实现代码：

- `D:\UE5projects\Demo\claude\docs\2026-08-10-狭窄走廊预判抛射Chaos机关拟实现代码.md`

成熟参考：

- GDC 2017《Math for Game Programmers: Predictable Projectiles》
- UE5.8 `UGameplayStatics::SuggestProjectileVelocity`
- UE5.8 `UPrimitiveComponent::AddImpulse`
- UE5.8 `UGameplayStatics::PredictProjectilePath`（仅开发期观察，不是发射门槛）

## 当前冻结方案

### 玩法与布局

- 一次性锁定首个进入角色，预警初值 `0.55 s`。
- 转角 Launcher 移到玩家转弯后正面的尽头墙，不再从背后追逐远离目标。
- Trigger 按真实玩家胶囊第一次 BeginOverlap 位置回读；默认该位置到 ProjectileSpawnPoint 约 `900～1100 cm`。
- 保留约 `10 m/s` 的慢速可读基线；预警期急停、后退或横移是有效躲避。
- 第一轮关闭 HeavyImpact 准备，只验收弹道、可读性和 Chaos；弹道通过后再独立决定轻/重受击。

### 弹道与机械

- `ReferenceRange=900 cm`、`PreferredLaunchAngle=18°` 只用于按世界重力推导约 `1225 cm/s` 的设计初速，不是运行门槛或 Trigger 距离。
- 目标从胶囊中心上移约 `50 cm`；预警期间用 `0.05 s` Timer 更新。
- 水平限制约 `30°`、向上约 `18°`、向下约 `15°`，相对 BeginPlay 缓存的固定中性炮轴。
- 炮管约 `90°/s` 真实转动；发射帧同步采样，最终冲量严格沿实际炮管方向。
- Muzzle 只表示出口平面；`ProjectileSpawnPoint` 是弹体质心。弹道 Start、出生检查、SpawnTransform 和 Chaos 冲量共享这一位置。
- 移动目标预测最多 8 次；时间、目标点、质心点和方向同时收敛，且未发生 Yaw/Pitch 限制时才标记 `PredictedIntercept`。
- 回退：移动交点低弧 → 当前/最后位置低弧 → Closest 候选 → 固定中性前向 + 8°。所有候选都经过同一机械限制。

### 弹体与堵膛

- 紧凑简单胶囊：半径 `22 cm`、半高 `45 cm`、质量 `50 kg`。
- 弹体正式进入 `Ballistic` 后启动 `8 s` Actor LifeSpan，到期整体清除；仍然不启用 Tick。
- `QueryAndPhysics`、简单碰撞、CCD；CCD 只降低风险，不承诺绝不穿透。
- 同一个 Chaos 刚体从出生到休眠；首碰只切日志/表现阶段和阻尼，不接管速度。
- 权威出生检查是在最终 ProjectileSpawnPoint 做完整胶囊 Overlap；不能从 Muzzle 中心 Sweep 一个完整胶囊。
- BeginPlay 的 Launcher/WorldStatic 结构重叠是装配错误并提前 Disabled。
- 运行时极端堵住出生体积时消费本发并播放 `BlockedDischarge`；不生成重叠弹体，也不向堵塞物伪造直接冲量。出生区之外的资源由真实弹体撞击。

## 当前修改范围（用户确认并通过 Git 门禁后）

### C++

1. `D:\UE5projects\Demo\Source\Demo\Public\Data\Hazards\ThrustGuidedHazardTuningData.h`
2. `D:\UE5projects\Demo\Source\Demo\Private\Data\Hazards\ThrustGuidedHazardTuningData.cpp`
3. `D:\UE5projects\Demo\Source\Demo\Public\Actors\Hazards\ThrustGuidedHazardLauncher.h`
4. `D:\UE5projects\Demo\Source\Demo\Private\Actors\Hazards\ThrustGuidedHazardLauncher.cpp`
5. `D:\UE5projects\Demo\Source\Demo\Public\Actors\Hazards\ThrustGuidedHazardProjectile.h`
6. `D:\UE5projects\Demo\Source\Demo\Private\Actors\Hazards\ThrustGuidedHazardProjectile.cpp`

不新建 Runtime 类，不迁移现有 UCLASS 路径，不抽 HeavyImpact 公共层。

### 资产与关卡

- `/Game/ZeroEscape/Hazards/Physics/BP_ThrustGuidedHazardLauncher`
- `/Game/ZeroEscape/Hazards/Physics/BP_ThrustGuidedHazardProjectile`
- `/Game/ZeroEscape/Hazards/Physics/Data/DA_ThrustGuidedHazard_Default`
- `/Game/Levels/Level0`：只修改本机关测试走廊中的转角 Launcher/Trigger 摆位及必要机关组件装配，不改玩家起点、摆锤房、冲锤房、PCG 代码或第三方资产

共享只读：

- `IHeavyImpactReceiver` 与 HeavyImpact 类型/实现
- 玩家/追猎者 Blueprint、动画、PCA、受击 DataAsset
- 轻受击任务的代码、资产、Review 和记录

## 当前非目标

- 不做循环发射、对象池、伤害数值或联网。
- 不做发射后追踪、推进、姿态转矩或目标引用。
- 不使用 `UProjectileMovementComponent` 与 SimulatePhysics 切换方案。
- 不用运行时最短飞行时间、最大速度或预测路径净空决定是否值得发射。
- 不为尚未复现的碰撞抖动预写过滤框架。
- 不接入 PCG Population；只记录未来 PCG 摆位合同。

## 实施门禁

- 用户已明确授权本轮 Source、Blueprint、DataAsset 和 Level0 机关范围落盘；磁力碎裂仅限用户当前资产制作，不授权本任务修改其 C++/Blueprint 逻辑。
- 当前工作区存在另一任务的 `claude/reviews/README.md` 与轻受击 Review 改动，归属不在本任务；本任务不得读取后擅改、提交或覆盖。
- 用户明确授权后，必须先与并行 Owner 收口所有改动，再完成一次全工作区 commit、内部工蜂 push、远端包含核验和 `git status --short` 为空。
- Push 前核对 `origin` 精确为 `git@git.woa.com:shiqiqiwang/Demo.git`。
- 任一门禁失败时暂停 Source/Blueprint/DataAsset/Level0 修改。

## 当前计划与检查点

- [x] 阅读项目记忆、架构规则、现有六文件、Level0 Launcher/Trigger 真实装配和旧 Review。
- [x] 确认旧推进版的核心失败是可读性、命中与穿透表现，不能继续只调 `ThrustStrength`。
- [x] 选择成熟的“发射前预判 + 一次冲量 + 纯 Chaos”模型。
- [x] 完成第一版拟实现代码。
- [x] 二次审查发现并修正：质心起点、单一 30° 锥、触发距离误读、无收敛迭代、堵膛伪造冲量、60 kg、CCD 绝对承诺。
- [x] 第三次复核修正：600 cm 慢速基线只命中继续迎面移动目标；急停定义为躲避；实际仰角改为约 8～14°；完整胶囊只在最终质心 Overlap；限角解不得标精确；收敛加入质心/方向；HeavyImpact 关闭时完全不启动准备链。
- [x] 重写活动任务卡，移除“当前计划与旧推进实施记录混排”的授权和验收矛盾。
- [x] 用户确认审查修订版与三个默认决定，并明确授权开始实现。
- [x] 执行完整 Git 基线门禁：`168316b1d3834e4ce1afc32239b54c4a457f079f` 已推送，远端 `main` 与本地一致，首次 Source 修改前工作区干净。
- [x] 修改六个 C++ 文件并完成 UHT 与 Demo `-NoLink` 编译；2026-08-10 四个编译 action 全部成功。
- [x] 静态复核修正两项实现 High：先进入 Armed 再启 Trigger 查询；机械转动改为固定中性轴 Yaw/Pitch 平面限步，避免四元数插值中途越过 18° 低顶上限。
- [x] 正常关闭 UE Editor，只完整链接 Demo 模块，不编译 UE 引擎；`UnrealEditor-Demo.dll` 于 2026-08-10 18:58 完成更新。
- [x] 用 UE5.8 官方 MCP 装配/回读 Launcher、Projectile、DataAsset 和 Level0；两个 Blueprint 以警告视为错误编译、保存成功。
- [x] Level0 转角实例改为西端半幅端墙上的正面迎击：Launcher `(49300,30400,150) / Yaw 0`，TriggerAnchor Local=`(600,200,0)`；出口灯移出弹道，北侧 300 cm 通路保留且整条可通行宽度均会触发。
- [x] 官方 MCP 回读机关三个资产和 Level0 均为非 Dirty；当前会话未发现机关 Blueprint 编译 Warning/Error。
- [x] 用户首次 PIE 发现两个机关完全不触发；日志锁定为 Level0 旧实例仍保存 `Muzzle -> SceneRoot`，在 BeginPlay 父级校验处自禁用。
- [x] 用官方 MCP 将两个 Level0 实例正式改为 `Muzzle -> AimPivot`；C++ 同时增加旧关卡实例父级自愈和详细失败诊断。
- [x] 触发修复通过 Demo `-NoLink` 与 Demo-only 正式链接；定点 PIE 覆盖两个实例 `Armed -> BeginOverlap -> Warning -> Fire`，无 disabled/failed 日志，运行后资产非 Dirty。
- [x] 用户 PIE 反馈弹体速度偏慢、重量感偏轻，并确认 `ReferenceRange=900 cm`、质量 `50 kg`、发射后 `8 s` 自动清除的增量方案。
- [x] 增量实现前重新执行完整基线门禁：`010018097d07ef072c943399721c38f22d848bf2` 与工蜂远端 `main` 一致，origin 正确且工作区干净。
- [x] 新增并校验 `ProjectileLifetimeSeconds`，默认 DataAsset 回读为 `900 cm / 50 kg / 8 s`；Demo `-NoLink`、正式链接和最小 PIE 生命周期验证通过。
- [x] 用户已确认当前基础机关版本验收通过；模型更换、表现细节和轻受击接入留作后续独立增量。
- [ ] 先提交并推送当前验收版；完成旧代码/文档残留清理讨论后再决定任务归档。

## 当前验证计划

### 构建与静态

- 六文件中无 Thruster、Projectile Tick、飞行中目标、Powered 状态或姿态转矩残留。
- `IsConfigured()` 完整覆盖所有新字段和关系。
- DataAsset 字段、Blueprint 组件树/父类、两个 Level0 实例、真实 BeginOverlap 路径位置逐项回读。
- 只构建 Demo 模块；记录 `-NoLink` 与最终链接结果。

### 用户 PIE

以拟实现代码文档第 14 节为唯一当前验收依据，重点是：

- 近距静止目标没有固定约 55 cm 的系统偏移。
- 默认 9～11 m 触发后继续前进时，看得到预警、实体和尾迹，并有较高命中可能。
- 预警期急停/后退/横移能躲。
- 转角取得炮口视线后才触发。
- 下游资源由真实弹体撞击。
- 墙、顶、地面和后续碰撞由同一 Chaos 刚体连续处理。
- 薄墙连续多发专项；只有实测后才能判断穿透问题是否收敛。

## 风险与需报告的问题

- `SuggestProjectileVelocity` 的 Closest 候选可能接近高角度，必须统一限制到向上 18°。
- 300 cm 天花板下 18° 只有约 20～24 cm 粗略余量；可视网格不得超出胶囊，最终以 BP 枢轴和房间内表面回读。
- Scene Query 看不到 `PhysicsOnly`/`NoCollision` 外壳；Launcher、墙和 PCG 装配必须 `QueryAndPhysics + Block PhysicsBody`。
- 角色胶囊会比角色中心更早 BeginOverlap；转角 Trigger 的 260 cm 横向半宽不能伸过拐角造成隔墙触发。
- 如果用户要求“9～11 m 处触发后立刻停住也必须被命中”，当前 10 m/s 慢速基线不满足；必须提高速度或缩短触发距离，并重新权衡可读性。

## 旧推进方案历史——不再作为当前实施或验收依据

- 旧 V1 曾获用户授权并实施：六个 C++ 文件、Launcher/Projectile Blueprint、DataAsset、材质和 Level0 低顶 L 形测试走廊。
- 旧基线与实现过程曾通过 Demo `-NoLink` 和 Demo-only 完整链接；未编译 UE 引擎。
- 已处理的旧 Review 位于 `claude/reviews/archive/Done-2026-08-08-壁挂式物理制导机关稳定性重构稿-review.md`。
- 旧转角实例当前为 `(50800,30600,150) / Yaw 180° / TriggerAnchor.X=1100`，实质是从背后追逐远离玩家；本轮计划会改为正面迎击。
- 用户现场结果是：即使降低 `ThrustStrength`，仍然看不清、乱飞、穿墙、打不中；因此旧“推进一段时间、首碰后停推”的用户 PIE 指令已经作废。
- 旧提交/构建记录只用于追溯，不代表新预判抛射方案已授权、已实现、已构建或已验收。
