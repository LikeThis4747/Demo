# TASK-20260806-001 — 物理制导一次性机关实现规划

- Owner：Codex `/root`
- Status：active
- Stage：讨论方案
- Created：2026-08-06
- Updated：2026-08-06

## 目标与验收

- 目标：为“一次性物理制导机关”形成可实施方案：机关锁定首次进入的目标，在可调推进时间内由真实施力驱动并追踪；首次有效碰撞后立刻停止制导但继续推进至计时结束，之后完全交给 Chaos 反弹与滑行。
- 验收：给出核心循环、状态唯一管理者、物理制导算法、C++/蓝图/DataAsset 边界、完整拟新增/修改文件与资产路径、受击接口合同、Level0 验证矩阵、PCG 后续接入边界、性能与回退风险，并在落盘前向用户展示拟实现代码。
- 非目标：本轮不修改功能 C++、Blueprint、关卡或配置；不实现玩家/追猎者受击表现；不接入 PCG Population；不做循环发射、对象池、伤害、联网、自动纠偏复位或为假设中的碰撞抖动预写过滤框架。

## 修改范围

- 允许修改：本任务卡；讨论期如需保存较长代码预览，只允许新增 `claude/docs/` 下本任务专属草稿。
- 预计实施范围（尚未授权，路径待现状审计后定稿）：`Source/Demo/Public/Actors/Hazards/`、`Source/Demo/Private/Actors/Hazards/`、`Source/Demo/Public/Data/Hazards/`、`Source/Demo/Private/Data/Hazards/`、`/Game/ZeroEscape/Hazards/Physics/` 与 `/Game/Levels/Level0` 的独立测试区。
- 共享/潜在冲突：`IHeavyImpactReceiver` 与 HeavyImpact 类型只读；不改 `PhysicsControlHitResponseComponent`、玩家/追猎者 Blueprint、动画/PCA/受击 DataAsset、PCG 生成代码或现有摆锤/冲锤资产。
- 并行拆分/依赖：依赖另一活动任务提供稳定的重冲击输入合同，但本机关只上报真实接触数据与本次发射唯一标识，不依赖其内部状态；实施前还依赖并行起身/动画资产工作完成全工作区 Git 基线。
- 用户已授权范围：已允许开始规划实现方案；尚未授权功能落盘。

## 计划与检查点

- [x] 固化当前策划规则与非目标。
- [x] 读取项目记忆、架构/工作流、双 MCP 规范、物理候选 Idea 与并行任务边界。
- [ ] 审计现有 Hazard、物理投掷、重冲击接口和 PCG/Level0 结构。
- [ ] 用官方 UE MCP 核对 Level0、既有 Hazard Blueprint/DataAsset 与测试空间；若编辑器未运行则报告阻塞，不使用本地 MCP 静默兜底。
- [ ] 定稿状态机、制导与推进实现、碰撞语义、参数和文件级架构。
- [ ] 输出代码预览范围、验证矩阵、风险分级和实施门槛，等待用户确认。

## 验证

- 命令/场景：只读源码、资产和关卡审计；规划阶段不构建、不启动 PIE、不保存 UE 资产。
- 结果：进行中。
- 用户操作（时机、步骤、预期结果）：方案确认和代码预览后再说明；当前无需操作。
- 用户验收：待验收

## 阻塞、风险与下一步

- 阻塞：当前工作区含另一任务持有的动画资产与文档改动；任何功能落盘前必须确认归属、完成完整内部 Git commit/push、核验远端并恢复干净工作区。
- 风险：制导过强会看起来像脚本转向，制导过弱则首段无威胁；高转速和高速接触可能产生 CCD/求解不稳定；“首次有效碰撞”必须排除发射器/出生重叠；受击接口不能把每个 Chaos 回调都直接当成一次重击。
- 下一步：完成实时代码/资产审计后，向用户提交最小 V1 实现规划。
