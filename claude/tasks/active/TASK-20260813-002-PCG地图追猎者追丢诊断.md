# TASK-20260813-002 — PCG 地图追猎者追丢诊断

- Owner：Codex（当前会话）
- Status：active
- Stage：待用户验收
- 任务类型：实现
- 实现文件写入权：Codex `/root` 独占
- Created：2026-08-13
- Updated：2026-08-14

## 目标与验收

- 目标：从《挑战性设计》的游戏性目标出发，核对当前追猎者 AI、生成流程与运行时导航，说明追丢玩家的实际原因与边界。
- 验收：分别给出策划目标、当前 AI 决策链、PCG/导航链、已确认原因、待 PIE 复现项和下一轮讨论问题，并标明证据来源。
- 非目标：不修改 C++、Blueprint、DataAsset、关卡、配置或正式策划案；不把静态检查当作运行验收；本轮不确定实现方案。

## 修改范围

- 允许修改：仅本任务卡，用于记录只读诊断检查点。
- 共享/潜在冲突：`DA_Pursuer.uasset`、`ENEMY_DESIGN.md`、`CHALLENGE_DESIGN.md`、追猎者攻击任务卡及动画资产当前有其他改动，本任务只读且不接管其归属。
- 并行拆分/依赖：无并行编辑；结论依赖当前 C++、UE 资产真实配置与现有 PIE 日志交叉核对。
- 用户已授权范围：阅读策划案并诊断当前 AI 逻辑及追丢原因；未授权功能实现。

## 计划与检查点

- [x] 读取启动记忆、当前错误、项目架构与双 MCP 规范。
- [x] 读取 `DOC/Design/CHALLENGE_DESIGN.md` 的追逐设计目标。
- [x] 完整核对追猎者控制器、配置、角色与生成/导航源码。
- [x] 通过官方 UE5.8 MCP 优先核对 DataAsset、Blueprint、关卡与运行时导航相关配置；本地 MCP 补充编辑器状态和日志。
- [x] 汇总“设计上的追丢”和“技术上的无法到达”，与用户开始方案讨论。

## 验证

- 命令/场景：源码逐文件阅读、资产只读回读、现有 PIE 日志核对；如无必要，本轮不启动 PIE。
- 结果：当前控制器每 0.1 秒直读玩家位置并持续全局追逐；旧的 1500/2000 cm 察觉与主动放弃限制已由 `a522bff` 移除。折返楼梯的新卡点来自直线攻击距离与 `MoveToActor` 胶囊重叠到达判定把上下层误当作已经足够接近；控制器仍未记录移动完成/失败或无进展恢复。
- 导航证据：生成器会在进入 Ready 前投影代表点并验证非局部路径；现有日志 10 次 `ZE_PCG_RESULT` 均成功。日志中 42 次 `Unable to find RecastNavMesh...` 均紧随 World teardown/cleanup，不能作为游玩中导航消失的证据。当前仍缺少 Population 完成后的真实移动追逐与路径失败遥测，因此导航问题保留为待 PIE 复现项，而非本轮首要已证实原因。
- 用户操作（时机、步骤、预期结果）：当前无需用户操作；若官方 MCP 不在线，按项目规则先汇报再决定是否只用本地 MCP。
- 用户验收：待验收

## 阻塞、风险与下一步

- 阻塞：暂无。
- 风险：生成后的静态路径抽样成功不等于移动目标追逐全程成功；资源/机关在导航验证后填充，且当前控制器没有路径结果、无进展或恢复日志，真实寻路故障仍可能与主动放弃同时出现。
- 下一步：先与用户确认游戏性验收口径——平路首次形成攻击压力所需时间、玩家一次成功机关/击退应换得的喘息秒数、以及 AI 多久无路线进展才进入公平恢复；方案确认前不改功能文件。

## 2026-08-14 楼梯隔层攻击最小实现增量

- 用户已授权：以 70 cm 为近战高度差上限；高度接近时保留楼梯挥斧，超过上限时继续严格寻路，不新增状态机、DataAsset 或行为树。
- 唯一实现文件：`Source/Demo/Private/AI/PursuerAIController.cpp`。
- 实现基线：`593eb3e673dfb6f4f3cba5d64358dbed593ecd14`，已推送内部工蜂并由 `ls-remote` 核验远端 `main` 同哈希。
- 并行纯文档改动：`claude/tasks/active/TASK-20260814-006-爆裂投掷特效方案.md`、`claude/tasks/active/TASK-20260814-007-PCG投掷物出现率与远距组合奖励.md`；本任务不修改、不暂存、不提交。
- [x] 高度差不超过 70 cm 时保留现有近战与普通停靠逻辑。
- [x] 高度差超过 70 cm 时不发起近战，改用不计双方胶囊重叠的严格位置寻路。
- [x] `git diff --check` 通过；`DemoEditor Win64 Development` 完整编译、链接成功。
- [x] `Demo.Combat.PursuerAttack.PredictionAndBallistics` 自动化 1/1 Success。
- [ ] 用户在原折返楼梯复验：隔层时持续爬楼，高度接近后仍会挥斧。
