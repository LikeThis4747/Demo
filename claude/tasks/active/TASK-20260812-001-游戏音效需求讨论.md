# TASK-20260812-001 — 游戏音效需求讨论

- Owner：Codex `/root`
- Status：active
- Stage：正式素材清单已落盘，等待用户确认首批灰盒范围
- Created：2026-08-12
- Updated：2026-08-12

## 目标与验收

- 目标：基于当前正式玩法、已落地机关与真实 UE 资产，整理一份可直接用于音效搜集和灰盒接入的需求表。
- 输出需区分：当前已经存在的触发事件、建议补充的声音表现、素材时长/循环方式、空间化与优先级。
- 同时给出现成素材的搜索方法、授权检查要点，以及适合/不适合 AI 生成的音效类型。
- 用户已明确要求把对话中的需求整理成正式文档；本轮仍不修改 C++、Blueprint、关卡、音频资产、配置或第三方素材。

## 修改范围

- 新增正式清单：`DOC/Design/GAME_AUDIO_REQUIREMENTS.md`。
- 更新目录索引：`DOC/README.md`。
- 维护本任务卡：`claude/tasks/active/TASK-20260812-001-游戏音效需求讨论.md`。
- 只读输入：正式策划/排期、相关交付记录、当前任务、UE 资产注册表与当前关卡/蓝图配置。
- 不占用当前追猎者攻击实现正在修改的源码和资产。

## 计划与检查点

- [x] 读取项目热记忆、错误记录、当前上下文和架构规则。
- [x] 确认官方 UE5.8 MCP 与本地 UE Editor MCP 在线。
- [x] 核对正式游戏循环、现有机关、磁力、追猎者、UI/胜败事件。
- [ ] 通过官方 MCP 盘点现有 SoundWave/SoundCue/MetaSound 等音频资产与当前关卡关键对象；首次列工具集成功，后续工具说明调用连续 HTTP 断开，未擅自改用本地 MCP 代替正式回读。
- [x] 形成“触发时机—建议时长—听感/空间效果—优先级”表。
- [x] 给出现成素材检索词、来源与授权检查方法，以及 AI 生成路线。
- [x] 按用户要求将需求表、检索词、授权与 AI 生成要求整理到正式设计文档，并更新 `DOC/README.md` 索引。

## 验证

- 当前证据：官方 MCP `list_toolsets` 成功；本地 MCP `ue_ping` 返回 `pong=true`。
- 源码/内容只读证据：已有菜单/暂停/胜负、磁力抓取投掷与破碎、追猎者近战和跑跳下砸、地刺、冲锤、摆锤、发射器、站立/重受击与出口事件；源码中未检出正式 `USound`/`PlaySound`/`AudioComponent` 接线。
- 内容目录存在 HydroLab 风扇、室外风、门、水管与音乐等第三方 SoundCue/SoundWave，以及陷阱包圆锯声音；它们仅是候选库存，尚未通过官方 MCP 核实引用与实际播放状态。
- 关键现行时序：近战命中 0.45 秒、恢复 0.6 秒；跑跳起手 0.65 秒、飞行 0.65 秒、恢复 0.75 秒；地刺升降 0.4 秒、伸出 1.5 秒、隐藏 2.0 秒；冲锤预警 0.7 秒、伸出 0.25 秒、缩回 0.8 秒、等待 1.8 秒；发射器预警 0.55 秒。
- 外部资料核对：Epic UE5.8 音频导入/MetaSounds、Fab Standard License、Sonniss GameAudioGDC、Freesound/Pixabay 授权，以及 ElevenLabs/Adobe Firefly 音效生成能力和商用边界。
- 文档验证：正式清单明确区分当前事件时序与建议素材长度，区分已发现候选库存与已接入事实，并保留官方 MCP 回读待办。
- 用户验收：待用户确认需求表与第一批灰盒音效范围。

## 阻塞、风险与下一步

- 阻塞：不影响本轮需求讨论；正式盘点现有音频资产绑定仍需官方 MCP 恢复。
- 风险：当前追猎者攻击仍在实现中，具体动画帧与命中窗口可能变化；表内将把建议时间标为“相对事件/动画节点”，避免把临时秒数当作已落地事实。
- 下一步：用户确认 P0 清单后，再按“讨论 → 确认 → 接入预览 → 明确授权 → 实施”规划灰盒音频装配；本任务当前不实施接入。
