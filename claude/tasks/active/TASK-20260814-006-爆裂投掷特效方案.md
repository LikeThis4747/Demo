# TASK-20260814-006 — 爆裂投掷特效方案

- Owner：Codex / 当前会话
- 任务类型：实现
- 实现文件写入权：当前会话独占；其他会话已确认暂停实现写入
- Status：active
- Stage：火星修订已构建并装配；等待用户从玩家视角验收
- Created：2026-08-14
- Updated：2026-08-15

## Git 基线门禁

- 2026-08-15 已核验工作区干净。
- 本地 `HEAD`、`origin/main` 与工蜂远端 `refs/heads/main` 均为 `d35adf7640bebfd187fb7b9f4a410017947a6047`。
- `origin` 精确为 `git@git.woa.com:shiqiqiwang/Demo.git`。
- 共享工作区另一会话已暂停所有会影响构建、运行或编辑器行为的写入，并明确避开本任务文件。

## 目标

- 为已验收的爆裂投掷补充两段清晰表现：持有时按 E 后持续闪红；首次合格碰撞时播放类似手榴弹的火光、冲击与烟雾。
- 第一版的爆炸范围提示应与实际 `ExplosionRadius` 同源缩放；当前默认半径为 350 cm，以橙黄火星最远扩散距离自然暗示约 3.5 m，不绘制范围圈或方向箭头。

## 当前边界

- 本阶段允许修改本任务列明的 C++、磁力 DataAsset，并新增一个项目自有红光材质。
- 不改变爆炸半径、伤害、Heavy Impact、碎片速度、扣次和普通投掷行为。
- 不新增 Actor、Component、Interface 或第二份爆炸 DataAsset；复用现有爆裂状态和命中收口点。
- 只引用、不修改现有项目火星与 `P_Fire` 粒子资产。

## 计划

- [x] 回读现有爆裂状态与爆炸触发接口。
- [x] 审计项目内可复用 Niagara、材质和粒子资源及引用边界。
- [x] 明确红光提示、爆炸火光/烟雾的最小资产结构和可调参数方向。
- [x] 用户确认视觉方向：按 E 后明显闪红；爆炸包含火光、烟雾，并可读出 Heavy 作用范围。
- [x] 执行 Git 基线门禁并取得唯一实现写入权。
- [x] 写入 `DOC/DailyPlan/2026-08-15-爆裂投掷特效第一版.md`。
- [x] 在对话展示拟实现代码，取得落盘确认。
- [x] 落盘红光、爆炸表现接入与第一轮 DataAsset 装配。
- [x] 根据用户截图移除产生白色箭头/轨迹的两个 UE Niagara 默认模板，改为四周散射火星。
- [x] 编辑器关闭后完整构建，并装配/回读新的火星 DA 字段。
- [x] 只做编译与资源配置核对；爆炸视觉 PIE 由用户测试。
- [ ] 用户完成视觉与手感验收后收尾提交、推送并释放写入权。

## 2026-08-15 火星修订证据

- 用户截图确认第一轮白色箭头/轨迹不符合玩家视角；已移除 `SimpleExplosion` 与 `RadialBurst` 的运行时字段、引用和 Niagara 模块依赖。
- `DemoEditor Win64 Development` 完整构建成功，284/284 actions 完成。
- `/Game/ZeroEscape/Data/Magnetism/DA_MagneticGrabTuning` 已装配 `P_SpikeWheel_Sparks_Forge`；`ExplosionRadius=350 cm`。
- 用户第二轮截图确认火星和整体爆炸团过小。现将火星参考半径调为 `30 cm`（有效缩放约 `11.7` 倍）、发射窗口最终调为 `0.25 s`；火焰/烟雾继续使用 `P_Fire`，缩放调为 `2.5`、发射窗口最终调为 `0.40 s`。
- 官方 MCP 回读上述字段一致，DataAsset 已保存且非 Dirty；本次修订未启动 PIE，视觉验收由用户执行。

## 只读审计结论

- `BP_MagneticProp` 的可见刚体为 `StaticMeshComponent`，支持 UE 的 Overlay Material；可以用项目自建的透明无光照材质覆盖整件道具，利用材质 `Time/Sine` 自行闪烁，不增加 Tick。
- 持有阶段已有 `OnExplosionModeChanged`，事件携带精确持有刚体；正式投掷阶段由 `UMagneticObjectComponent::ArmThrownImpact/DisarmThrownImpact` 管理；真实爆炸点已在 `TriggerExplosion(ExplosionOrigin)` 收口。
- 第一轮复用的 UE Niagara 默认模板在玩家画面中显示为白色箭头/示意轨迹，不符合手榴弹爆炸观感，已明确弃用；不能再把技术范围示意直接当作玩家特效。
- 项目已有橙黄火星粒子 `/Game/ZeroEscape/Hazards/Physics/FX/P_SpikeWheel_Sparks_Forge`，其参考边界约为 100 cm，可只读复用并按实际爆炸半径缩放；烟雾继续复用 `P_Fire`。

## 本次最小实现范围

- 持有时：在磁力物可见网格上设置项目自有的红色脉冲 Overlay；取消、普通放下、重抓、超时或爆炸收口时恢复此前 Overlay。
- 投出后：仅爆裂投掷保留红光；普通投掷不进入任何新表现路径。
- 爆炸时：在真实 `ExplosionOrigin` 同时生成按 `ExplosionRadius` 缩放的橙黄火星和短时发射的火焰/烟雾；火星参考半径 100 cm，默认 350 cm 自动得到 3.5 倍缩放。范围通过火星散射自然表达，不生成几何外圈或箭头。
- 参数继续放进现有 `UMagneticGrabTuningData`，不创建第二套配置来源。

### 计划修改的实现文件

- `Source/Demo/Public/Data/Magnetism/MagneticGrabTuningData.h`
- `Source/Demo/Private/Data/Magnetism/MagneticGrabTuningData.cpp`
- `Source/Demo/Public/Components/Magnetism/MagneticObjectComponent.h`
- `Source/Demo/Private/Components/Magnetism/MagneticObjectComponent.cpp`
- `Source/Demo/Private/Components/Magnetism/ElectromagneticGrabComponent.cpp`

### 计划新增或修改的资产

- 新增：`/Game/ZeroEscape/Interaction/Magnetism/FX/M_MagneticExplosionArmedOverlay`
- 修改：`/Game/ZeroEscape/Data/Magnetism/DA_MagneticGrabTuning`
- 只读引用：`/Game/ZeroEscape/Hazards/Physics/FX/P_SpikeWheel_Sparks_Forge`
- 只读引用：`/Game/AssortmentOfTraps/Demo/StarterContent/Particles/P_Fire`
