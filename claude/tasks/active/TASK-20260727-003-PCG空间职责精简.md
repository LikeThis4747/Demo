# TASK-20260727-003 — PCG 空间职责精简

- Owner：Codex / root（本对话）
- Status：done
- Stage：已完成
- Created：2026-07-27
- Updated：2026-07-27

## 目标与验收

- 目标：把当前 PCG 收敛为空间生成最小核心；移除尚未确定的 K-of-N、CollectAll、多 Flow 玩法规则及其重复数据链，保留 WFC、回溯、Count/Connected/MaxConsecutive、中立房间、Start/Exit 与逻辑路线长度控制。
- 验收：先在对话中给出逐文件删改清单和拟实现代码；用户再次明确授权后才修改源码。实施后要求 C++ 构建、PCG 自动化、Seed Sweep、PIE 生成与总行数/净变化报告。
- 非目标：本检查点不实现室内光源、小地图、正式 GameFlow、追猎者导航、奖励/陷阱/敌人分配，也不修改第三方素材。

## 修改范围

- 允许修改：`Source/Demo/Public/PCG/`、`Source/Demo/Private/PCG/`、PCG 项目自有 DataAsset/Blueprint 配置及相关 PCG 测试。
- 共享/潜在冲突：`TASK-20260727-001` 正在修改 `Demo.Build.cs`、Pursuer 与 Physics 文件；本任务不得触碰这些文件，不占用其 UE 编辑器验证阶段。
- 并行拆分/依赖：先完成只读引用审计和代码预览；源码实施必须等待用户明确授权，并在开始前重新核对 Pursuer 工作树与 UE 状态。
- 用户已授权范围：已明确要求“开始删改代码”；当前已实施源码和测试精简，尚未修改 PCG 资产。

## 计划与检查点

- [x] 建立现有 K-of-N、Flow、Progression、Objective、Hash、验证与测试引用图。
- [x] 确定最小公共数据：Seed、Difficulty、Cells、Start/Exit、中立房间 Anchor、指标和失败报告。
- [x] 列出逐文件删除、修改、保留内容及预计行数变化。
- [x] 在讨论稿中展示关键类型、生成入口、最终路线验证和测试的拟实现代码。
- [x] 用户确认后写入当日 DailyPlan。
- [x] 删除 Flow、K-of-N、Objective、Landmark、玩法进度 Hash 与重复 Grid 请求链。
- [x] 保留 WFC、时间序回溯、Count/Connected/MaxConsecutive、Start/Exit、中立房间与一遍最终 BFS。
- [x] 精简自动化测试，并保留 WFC 回溯边界、完整连通性、确定性和 288 Seed Sweep。
- [x] UE5.8 `DemoEditor Win64 Development` 完整构建。
- [ ] Automation 与批量 Seed 检查（用户本轮明确要求跳过，改由手动测试）。
- [x] 迁移 V5 Generation DataAsset，并完成一次用户手动 PIE 运行。

## 验证

- 命令/场景：规划阶段只读检查 C++ 引用、代码规模、Git 工作树、现有测试边界，以及 `/Game/ZeroEscape/Generation` 下两份 Blueprint 和测试关卡的真实资产状态。
- 结果：生产代码和测试已完成第一轮删改；整个 `Source` 已无旧 Flow/K-of-N/Objective/Progression 类型引用。PCG C++ 从 9,034 行降至 7,576 行，净减少 1,458 行；`git diff --check` 通过。`DemoEditor Win64 Development` 完整构建成功，12/12 编译与链接步骤通过，总耗时 52.72 秒。Generation Profile 已保存为 V5、`RoomCount=3`。用户手动 PIE 留下 `schema=5 success=1`：Seed 12345、Normal、69 Cells、3 Rooms、1126 Instances、5 HISM、222.890 ms；Harness 传送到生成起点成功，PCG 类别无 Warning/Error。按用户要求未运行 Automation 或批量 Seed 检查。
- 用户操作（时机、步骤、预期结果）：用户已完成一次正常主视口运行；仍需用户说明视觉、路线和实际行走是否接受。
- 用户验收：通过。用户在主视口完成实际运行后确认“目前正常”；本卡空间职责精简范围结束。

## 阻塞、风险与下一步

- 阻塞：无。
- 风险：真实 DataAsset 仍保存旧 V4 序列化值，需要构建成功后用 MCP 显式迁移到 V5；不得触碰并行 Pursuer/Physics 文件。
- 下一步：由独立任务 `TASK-20260727-005` 讨论室内光源与小地图；本卡不再扩张职责。
