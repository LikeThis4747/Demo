# TASK-20260813-006 — PCG 种子稳定性与结构出现率诊断

- Owner：Codex（当前会话）
- Status：active（实现完成，待用户验收）
- Stage：构建与自动化完成 / 待最终提交推送和用户验收
- Created：2026-08-13
- Updated：2026-08-13

## 目标与验收

- 目标：还原当前 PCG 的固定种子、失败重试、楼层/楼梯/高厅结构和摆锤投放规则，解释用户体验到的换种、卡顿与稀有结构缺失，并结合公开一手资料提出稳定种子与无终局失败的改进方向。
- 验收：公开 Seed 不自动改变；质量指标从搜索开始提供倾向但不终局拒绝；正式 Profile 的 Easy/Normal/Hard 各 300 Seed 规划成功率 100%、重放 Hash 一致；每栋至少两个高厅且至少一个非顶层；构建和相关自动化通过，最终手感由用户验收。
- 非目标：不修改楼梯开口组合、机关与资源规则、高厅灯光、Loading UI、关卡或第三方素材；不把单个代表 Seed 当作全体 Seed 的统计结论。

## 修改范围

- 允许修改：`DOC/DailyPlan/2026-08-13-PCG固定Seed与软质量目标收敛.md` 与拟实现文档列出的 PCG、GameFlow、测试文件及正式 Generation Profile；不新增生产文件。
- 共享/潜在冲突：PCG 源码、正式 Generation/Presentation/Population DataAsset、主菜单与 GameMode 蓝图均只读；不接管现有追猎者追丢诊断任务的文件归属。
- 并行拆分/依赖：两个只读子任务分别完成现有代码修改面审计与外部一手资料研究；没有并行编辑功能文件。
- 用户已授权范围：用户已明确允许按收敛后的拟实现方案直接实施、关闭/打开 UE 编辑器、完整提交并推送当前工作区；当前会话是本次 PCG 实现唯一 Owner。

## 计划与检查点

- [x] 还原菜单随机种子到本局有效种子的传递与展示链。
- [x] 枚举可恢复失败、派生种子算法、重试次数和等待上限。
- [x] 回读 Difficulty、楼层、双层楼梯、贯通三层楼梯、高厅与摆锤的真实配置。
- [x] 分析困难档、结构预算、WFC 与导航验证之间的失败耦合。
- [x] 调研固定种子、确定性生成、约束求解失败修复的一手资料。
- [x] 输出当前规则、问题判断、推荐产品语义和待授权实施范围。
- [x] 写出拟实现代码，审视并修正长直线引导、同层收敛、可选结构降级与 Recast 保证边界。
- [x] 按模块估算功能代码和测试的新增、修改与删除数量。
- [x] 删除外层自动换 Seed/重载与 Version 字段，并保留既有随机序列兼容。
- [x] 将密度、覆盖、直线和距离质量规则软化到候选权重/评分。
- [x] 实现最多三候选择优、完美候选提前停止和同层确定性硬合法兜底。
- [x] 高厅先于可选楼梯放置，并保证每栋至少两个且至少一个非顶层。
- [x] 修正整栋候选与可选楼梯回滚时的固定预算可用量。
- [x] 更新并回读正式 Generation Profile。
- [x] 完成构建、核心自动化与三档共 900 Seed 压测。

## 验证

- 命令/场景：源码逐符号只读核对；官方 MCP 优先回读资产与蓝图；本地 MCP 补充日志；公开资料检索。
- 结果：基线提交 `6c0b6c880e12238379657f1ffb89045674f145d7` 已推送并核验；DemoEditor 构建通过；PCG Unit 10/10、WFC 8/8、Population 8/8、GameFlow 1/1、Navigation Gate 1/1 均通过；正式 Profile 三档各 300 Seed、每个重放两次的压力测试通过，生成失败/Seed 漂移/Hash 漂移/高厅合同违约均为 0。
- 用户操作（时机、步骤、预期结果）：次日从主菜单连续尝试困难档随机 Seed，重点观察加载卡顿、地图密度、长直线比例、两个以上高厅及至少一个非顶层高厅；再用同一 Seed 重进确认地图一致，并完成真实追猎者跨层/Recast 验收。
- 用户验收：待验收

## 代码规模与终审

- 功能生产代码 17 个既有文件，Git 原始统计 `+696/-439`，净增 257 行；测试 5 个既有文件 `+319/-136`；新增/删除生产文件、生产类和外部依赖均为 0。
- 原始新增数超过计划中的 480 行警戒线。提交前终审确认：Planner 的 `+172/-180` 主要是原地规则改写；Grid 中真正的确定性兜底约 194 行，低于单层收敛 260 行止损线；其余为密度引导、四项软评分和预算验收，没有第二套求解器、评分注册系统或可安全删除 50 行以上的绕路代码。
- 三路专项审计和一轮最终整体审计已处理预算复用、高厅配置合同、动态权重溢出和完美候选无收益搜索；最终未发现 P0/P1 阻断。

## 阻塞、风险与下一步

- 阻塞：暂无。
- 风险：正式 Profile 的 900 Seed 规划已全过，但自动化不能替代真实一局的动态 Recast、全楼梯和地图观感；非法配置、代码不变量或导航系统故障仍显式失败，只是不再更换公开 Seed。
- 下一步：完成最终功能提交、推送与远端哈希核验；保持任务 active，等待用户次日验收。楼梯开口、机关资源、高厅灯光和 Loading UI 继续排除。
