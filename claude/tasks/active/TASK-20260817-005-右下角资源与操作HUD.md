# TASK-20260817-005 — 右下角资源与操作 HUD

- Owner：Codex `/root`
- Status：waiting_user_acceptance
- Stage：实现与 PIE 冒烟已完成，等待用户画面验收
- 任务类型：实现
- 实现文件写入权：Codex `/root` 独占
- Created：2026-08-17
- Updated：2026-08-17

## 目标与验收

- 目标：在现有 Canvas 准星之外，增加一个轻量 UMG 局内 HUD，固定在右下角，显示左键投掷、右键按住吸取、E 赋能爆炸、爆裂次数、蓝色充能进度和红色生命进度。
- 假设：蓝色能量条显示现有爆炸投掷的下一次充能进度；`1/3` 显示当前可用次数/最大次数，不新增独立能量玩法。
- 验收：正式 `L_Game` 进入后 HUD 出现在安全区内；生命、爆裂次数和充能进度能随运行时状态变化；不覆盖准星、暂停菜单和结算菜单；Widget/Blueprint 无编译错误或未保存 Dirty 状态。
- 非目标：本轮不加入目标提示、出口方向、追猎者警告、独立教学关、额外字体、第三方 UI 素材或 CommonUI 架构。

## 修改范围

- 允许修改：
  - `Source/Demo/Public/UI/ZeroEscapeGameplayHUDWidget.h`
  - `Source/Demo/Private/UI/ZeroEscapeGameplayHUDWidget.cpp`
  - `Source/Demo/Public/GameFlow/ZeroEscapePlayerController.h`
  - `Source/Demo/Private/GameFlow/ZeroEscapePlayerController.cpp`
  - `/Game/ZeroEscape/GameFlow/BP_ZeroEscapePlayerController`
  - 新建 `/Game/ZeroEscape/UI/WBP_GameplayHUD`
- 共享/潜在冲突：现有 `AZeroEscapeHUD` 只保留 Canvas 准星；不改其他会话持有的 PCG、出口、楼梯灯、追猎者或分析文档文件。`DOC/README.md` 当前由文档任务占用，本任务不修改。
- 用户已授权范围：先提交已确认的既有实现改动，再开始本 HUD 实现；允许开关 UE；用户负责最终 PIE/画面验收。

## 计划与检查点

- [x] 记录实现前 HEAD、完整工作区状态并提交/推送既有实现基线。
- [x] 通过官方 UE MCP 核对 PlayerController CDO、Pause Widget 与现有 UMG 资产。
- [x] 创建并布置 `WBP_GameplayHUD` 右下角控件树；进度条保留 `UProgressBar`，并增加字符段备用显示保证画面可见。
- [x] 在现有 PlayerController 增加最小 Widget 创建入口并设置 Blueprint 默认类。
- [x] 官方 MCP 回读 Widget 树、默认值、编译与保存状态。
- [x] DemoEditor Development 构建，并在 `L_Game` 启动 PIE 做烟雾验证。
- [ ] 等待用户正式画面验收，不提前标记完成。

## 验证

- 命令/场景：`DemoEditor Win64 Development` 构建；官方 UMG/Blueprint 回读；`L_Game` PIE smoke。
- 结果：构建成功；`WBP_GameplayHUD` 父类为 `/Script/Demo.ZeroEscapeGameplayHUDWidget`，编译成功且保存后非 Dirty；PlayerController CDO 的 `gameplayHUDWidgetClass` 指向 `/Game/ZeroEscape/UI/WBP_GameplayHUD.WBP_GameplayHUD_C`；PIE 右下角可见操作提示、次数、蓝色能量段和红色生命段。
- 用户操作：用户进入 `L_Game`，观察右下角布局、生命条、充能条、次数变化和暂停/结算遮挡。
- 用户验收：待验收。

## 阻塞、风险与下一步

- 阻塞：无；官方 MCP 重启期间曾超时，用户已明确允许直接使用本地编辑器 MCP 继续，之后官方 MCP 已恢复。
- 风险：健康组件已有读接口但没有变化委托；首版由 Widget 使用低频 Timer 读取现有组件，避免加入常驻玩法 Tick。蓝色进度条结合可用次数比例与现有充能进度，旁边字符段用于分辨率下的稳定可读反馈。
- 下一步：用户在当前 PIE 画面验收右下角位置、可读性和与暂停/结算的遮挡关系；确认后再关闭任务卡。
