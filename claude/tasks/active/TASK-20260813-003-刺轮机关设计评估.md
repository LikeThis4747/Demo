# TASK-20260813-003 — 刺轮机关设计评估

- Owner：Codex `/root`
- Status：active
- Stage：移动与轨迹技术验证完成；火星视觉修正待复验
- Created：2026-08-13
- Updated：2026-08-14

## 目标与验收

- 目标：在 Level0 实现一个项目自有的刺轮初版，沿配置的 1～3 个连续逻辑格轨迹循环运动，并对玩家提交一次 Stop 与伤害，验证局部路线压力和防重复命中。
- 验收：C++ 构建通过；刺轮 Blueprint/DataAsset 编译保存；Level0 单实例能在独立空房间内按一格闭环路线持续移动，配置仍保留 1～3 格模板；一次完整接触只结算一次，离开且超过局部门禁后才能再次命中；不阻挡 Pawn、不影响导航。
- 非目标：不接入 PCG/Population，不实际组合冲锤/摆锤/投掷物，不实现追踪、销毁、物理破坏、多刺轮协同或玩家受击动画；不修改追猎者、玩家、现有机关和第三方素材。

## 修改范围

- 允许新增：`Source/Demo/Public/Actors/Hazards/SpikeWheelHazard.h`、`Source/Demo/Private/Actors/Hazards/SpikeWheelHazard.cpp`、`Source/Demo/Public/Data/Hazards/SpikeWheelHazardTuningData.h`、`Source/Demo/Private/Data/Hazards/SpikeWheelHazardTuningData.cpp`。
- 允许新增资产：`/Game/ZeroEscape/Hazards/Physics/BP_SpikeWheelHazard`、`/Game/ZeroEscape/Hazards/Physics/Data/DA_SpikeWheelHazard_Default`、`/Game/ZeroEscape/Hazards/Physics/FX/M_SpikeWheel_Sparks_Forge`、`/Game/ZeroEscape/Hazards/Physics/FX/P_SpikeWheel_Sparks_Forge`、`/Game/ZeroEscape/Physics/Impact/DA_SpikeWheelStandingImpact`。
- 允许修改：`/Game/Levels/Level0`、本任务卡和对应 `DOC/DailyPlan/`。
- 共享/潜在冲突：只读引用第三方 `SM_SawBlade` 与圆锯音效；不得修改或复制第三方 `BP_SawBlade`。Population、玩家 Stop 动画和追猎者任务保持只读。
- 并行拆分/依赖：当前只有本会话实施功能；玩家 Stop 动画由另一任务负责，刺轮来源 Profile 首版关闭动画开关。
- 用户已授权范围：2026-08-13 已明确允许按拟代码方案开始实现；销毁暂缓，追猎者反应固定为 None。

## 计划与检查点

- [x] 完整阅读 `CHALLENGE_DESIGN.md` 并提取刺轮与机关组合约束。
- [x] 核对当前 PCG/Population 的地图格、楼层、通路和机关投放合同。
- [x] 通过 UE MCP 确认刺轮素材与现有机关资产。
- [x] 比较固定、巡航、物理滚动等刺轮形态及蓝图/C++ 最短路线。
- [x] 输出压力设计、组合遭遇、PCG 规则、成本/风险和推荐优先级供用户讨论。
- [x] 用户确认 1～3 格轨迹模板、Level0 单实例和最小 C++ + Blueprint 装配方案。
- [x] 新增并构建刺轮配置与 Actor C++。
- [x] 使用官方 UE5.8 MCP 创建、配置、编译并保存刺轮资产。
- [x] 在 Level0 独立空房间放置一个一格展示实例并完成技术 PIE/边界验证。
- [x] 按用户反馈把轮心降到地面，只露上半圆；补充三种一格轨迹，并让 Actor 格坐标参与实例级确定性选路。
- [x] 复制项目自有的 Cascade 粒子与着色材质，只改项目副本；把 `Spark burst` 和 `Sparks` 两个发射器改为红黄打铁火星，烟雾保持原材质。
- [ ] 按用户截图反馈恢复火花原始 `1.0` 组件缩放，只保留红黄色修改；不得再改变原粒子的尺寸、数量、速度或拖尾形态。

## 验证

- 命令/场景：`DemoEditor Win64 Development -Module=Demo` 项目模块编译/链接；官方 UE5.8 MCP 资产回读、蓝图 Warning-as-error 编译；Level0 Simulation 与玩家接触 PIE。
- 结果：实施基线 `bb7de1a7c6d56b110f98237c5b409625a1bf8f43` 已推送并核验。新增 C++ 通过 UHT、编译和链接；蓝图及两个 DataAsset 保存后均无脏包。初始两格实例的移动碰撞体持续沿样条移动；玩家每次有效进入提交 Stop 并扣 20 点生命，持续接触不逐帧重复，实测下一次路线回碰约 5.33 秒；追猎者返回 Ignored 且伤害为 0。
- 房间展示调整：根据用户反馈，实例已移至冲锤测试区最前方的独立空房间，位置 `(53500, 30000, 0)`，改用一格闭环路线以完整留在约 `600×600 cm` 的室内净范围；运行时位置持续变化且无 `LogSpikeWheel` 警告。
- 2026-08-14 反馈修正：`WheelCenterHeightCm=0`，运行画面只露轮体上半圆；一格模板现为 `OneCell_OffsetLoop`、`OneCell_DiagonalM`、`OneCell_CrossLoop`，总配置仍覆盖 1～3 格。基础 Seed 会混入 Actor 所在逻辑格坐标，同一地图中不同实例可稳定选到不同模板/镜像/方向/相位，不会让整局共用一种路线。
- 火花表现：Blueprint 的 `GroundSparks` 使用项目副本 `P_SpikeWheel_Sparks_Forge`；`Spark burst` 与 `Sparks` 发射器使用项目自有加色材质，颜色为亮黄橙核心与橙红尾部，第三方粒子、材质和 `BP_SawBlade` 均未修改。2026-08-14 用户截图确认当前 `0.45` 组件缩放过小、近乎不可见，并明确要求只换颜色而不改变形态；该视觉项未通过验收。
- 既有技术证据：Demo `-NoLink` 编译和关闭编辑器后的 Demo-only 正式链接均成功；冷启动后 Blueprint 为 `0 error / 0 warning`，火星材质为 `0 error / 0 warning`。Simulation 中 `HurtZone` 在 0.8 秒内由 `(-179.47, 142.54, 0)` 移至 `(-124.28, -113.06, 0)`，半径 `65 cm`、Overlap 开启，且无 `LogSpikeWheel` 警告；这些证据不等于火星视觉验收。
- 边界说明：玩家受击动画不在本任务范围内；PIE 结束时仍出现项目已知的 `RecastNavMesh`/`UCrowdManager` 警告，与刺轮无直接关系。
- 用户操作（时机、步骤、预期结果）：AI 完成技术验证后，由用户最终验收路线压力和穿越手感。
- 用户验收：待验收

## 阻塞、风险与下一步

- 阻塞：暂无；共享 Heavy 诊断任务卡已由其 Owner 独立提交，刺轮功能基线可继续。
- 风险：Level0 手工一格空房间只验证 Actor 行为，不证明 Population 可安全选择跨格足迹；路线是否形成合适的停顿压力仍需用户亲自体验。
- 下一步：由用户在已对准视口的空房间内验收路线覆盖、速度和穿越手感，再决定是否只调 DataAsset 数值或进入后续 PCG 放置设计。
