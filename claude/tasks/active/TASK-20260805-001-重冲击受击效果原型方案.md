# TASK-20260805-001 重冲击受击效果原型方案

- Owner：Codex `/root`
- Status：active
- Stage：C++ 与摆锤 V1 接入已实现；UHT 与 Demo 模块多轮增量编译通过，最终一轮 4 个动作成功。完整 DLL 链接、PCA/DA/Blueprint 装配、自动化和 PIE 等待用户安全关闭 Editor 及人工创建 PCA
- Created：2026-08-05
- Updated：2026-08-05

## 目标与验收

- 目标：实现玩家与追猎者共享的“预接管 → 真实 Chaos 接触 → 受控全身物理飞行 → 环境落地 → Downed”原型，并让摆锤以 PCG 可放置 Actor 的自包含接口驱动它。
- 本阶段验收：代码、PCA/DA/Blueprint 装配、自动化、PIE 和用户画面验收全部有真实证据；当前只完成代码与编译阶段，不因编译通过提前标记完成。
- 非目标：不实现倒地起身动画、伤害/死亡、网络复制，不修改现有 Physics Asset 或两个 AnimGraph。

## 范围与并行边界

- 允许修改：共享重冲击类型/接口/组件/调参/测试、玩家与追猎者适配、磁力中断，以及摆锤预测接入和对应实施文档。
- 只读范围：主角/追猎者 Blueprint 与动画资产、两份 Physics Asset、Level0 和 PCG Population；本增量没有修改这些二进制资产或关卡。
- 并行边界：摆锤策划线程已完成并明确停止写入后，本任务取得一次串行接入窗口，只修改 `APendulumHazard` 与其 Tuning DataAsset C++，没有修改 Level0、机关 Blueprint 或 PCG 资产。
- 并行合同：`FHeavyImpactPreparationRequest` + `IHeavyImpactReceiver` V1 已作为当前代码合同冻结；机关只依赖该合同，不直接包含或调用 Character、AI、Camera、磁力、Physics Control 与受击组件内部接口。破坏性变更必须重新串行 review。
- 资产所有权：本任务独占玩家/追猎者 Blueprint、重冲击 PCA 与少量玩法阈值 DA；官方 PCA 是已确认首选。机关任务独占机关 Blueprint、Level0 与 PCG 资产。所有 `.uasset` 串行写入，不允许两个会话合并同一 Package。
- 公共高冲突文件 `Demo.Build.cs`、`DemoHitTags.h` 和全局设置默认冻结；确有必要时先暂停双方并更新 Plan。

## 计划与检查点

- [x] 阅读项目工作规范、当前任务与现有局部 Physics Control 受击实现。
- [x] 用 UE5.8 官方 MCP 核对主角/追猎者真实 Mesh、Skeleton、Physics Asset、组件碰撞与 ABP 装配。
- [x] 用 UE5.8 官方 MCP 批量读取两份 Physics Asset 的刚体、约束、形状、质量倍率和默认模拟模式。
- [x] 用官方 UE5.8 MCP 核对两个 ABP 的 AnimGraph 拓扑、真实父类与骨架装配，并搜索现有起身动画。
- [x] 结合 UE 官方文档与成熟游戏分享，收束效果原型路线、风险和验证门槛。
- [x] 用户确认追猎者先标定，同时明确玩家是同轮必须接入的对象。
- [x] 写入 `DOC/DailyPlan/2026-08-05-重冲击物理受击效果原型实施计划.md`，定义共享核心、AI/玩家增量、文件白名单与代码增量审计门槛。
- [x] 补充其他机关并行开发的合同冻结、文件/资产所有权、串行 Editor 窗口、集成门禁和分增量回退策略。
- [x] 写入首轮代码稿；用户审阅后确认其 A 首撞、暂缓 PCA、回测试原点三项方向不符合目标，已在稿首明确标记作废，禁止其他 AI 按旧稿 review。
- [x] 用户确认：从 B 的预接管/真实 Chaos 接触开始；优先使用官方 PCA；新重冲击模块为权威；倒地与起身保留实际击飞落点；旧局部受击可先停用再重构；原始 Physics Asset 暂不动。
- [x] 官方 MCP 恢复后，实时复核 PCA/Profile、两套角色资产与碰撞通道，并按已确认方向整体重写拟实现代码稿：`claude/docs/2026-08-05-重冲击物理受击B路线拟实现代码审阅稿.md`。
- [x] 阅读并处理 `2026-08-05c` 最新审阅：PCA API 形状通过；修正旧组件三处接线原子暂停、玩家 Jump 绑定、落地 Capsule 高度、Downed 零空转/再次受撞、拒绝日志限频和销毁参数说明。
- [x] 按用户要求保留旧追猎者受击源码、调参资产、蒙太奇字段、回调和状态成员；仅暂停默认子对象构造、`Configure`、Hit Delegate 三处运行时装配，并在 B 稿第 13 节保存逐字恢复文本与顺序。
- [x] 用户明确授权按 B v1.1 完成共享核心、追猎者、玩家、资产装配和构建；需要用户操作的部分只记录待办，不写绕过逻辑。
- [x] 完成全工作区基线 commit、内部工蜂 push、远端哈希核验并确认工作区干净：`1c50616c8f19cfa5daa62d39fd626c1c11ff7310`。
- [x] 实现共享类型、接口、Tuning、权威组件、玩家/追猎者/磁力适配、摆锤预测接入和自动化测试源码。
- [x] UHT 与所有本增量 `.cpp` 编译通过；最终用 UE5.8 官方 `-Module=Demo -NoLink -NoEngineChanges` 完成 8 个编译动作，并修正一次 `FBodyInstance` 非导出内联链接依赖。
- [ ] 完成 `UnrealEditor-Demo.dll` 链接；当前 Editor 保持 Live Coding Session 并占用 DLL，禁止杀进程或临时 DLL 绕过。
- [ ] 人工创建并 Compile 两份 Physics Control Asset；双 MCP 均无 PCA Factory/Compile 入口。
- [ ] 用官方 MCP 创建两份 HeavyImpact DataAsset、装配两个角色 CDO、编译/精确保存并回读角色与摆锤 Blueprint。
- [ ] 运行 `Demo.Physics.HeavyImpact.*`、相关回归、PIE 矩阵与用户画面验收。

## 当前证据

- 主角使用 `SK_SciFITrooper_Man_03`、其独立 Skeleton/Physics Asset 和官方 `ABP_Unarmed`；追猎者使用 `SK_Sci_Fi_Character_08_Full_01`、另一套 Skeleton/Physics Asset 与自定义 `ABP_Pursuer_Locomotion`。
- 两者都有 17 个主要人体刚体，骨架主干语义相近但刚体分布不同：主角有 `neck_01` 刚体而无 `spine_02` 刚体；追猎者相反。
- 两份物理资产所有形体质量倍率均为 1、模式均为 Default，且手脚使用较大的 Box；尚未为重冲击做艺术化质量与环境接触筛选。
- 主角颈部约束存在自由扭转风险；追猎者头部直接约束到 `spine_03` 且角限位很紧，分别容易出现颈部失控与上身过硬。
- 两个 ABP 均为 UpToDate 且已有 `DefaultSlot`。主角末端还有 Control Rig/脚部 IK；首版应避免让移动动画和 IK 在空中继续驱动物理目标。
- 当前局部 Physics Control 仅属于追猎者、只接收 `AttackProjectile`，并会全量销毁 Controls/BodyModifiers；不能与新权威 PCA 同时 Active。它不会在本增量被删除或重写，只暂停三处角色装配。
- 项目内未找到真正的 ragdoll 起身动画，只有 Jump Recovery Additive；首版应先验证击飞和落地，不把找动画放到关键路径。
- 推荐首版在追猎者上标定：全身刚体接管位移，Physics Control 只以有最大扭矩上限的 Parent Space 角驱动保持“肌肉张力”，不启用把身体钉回 Capsule 的 World Space 位置驱动；环境碰撞继续参与轨迹和姿势结果。
- 首版冻结命中瞬间的动画目标、禁用移动/AI 权限；身体在真实倒地点稳定并进入 Downed/Recovery，不得回到受击前测试位置。起身动画可由用户补充，恢复桥必须围绕最终 pelvis 位置设计。
- 已确认优先使用每套骨架独立的官方 `UPhysicsControlAsset`；少量项目 DataAsset 只保存冲击预接管超时、稳定判断、相机和恢复阈值。新共享重冲击组件是 Physics Control 唯一权威；旧局部受击完整保留为 dormant rollback path，后续是否恢复或重构轻受击由效果验收决定。
- 代码实施采用 Increment 0/1/2/3 白名单；本任务只为每个增量准备独立 diff、构建、官方 MCP 资产回读和 PIE 证据包，随后暂停并交由用户安排的另一位 AI 审计。本任务不创建或代写审计报告。
- 并行实现只允许依赖冻结的 V1 接口。文本文件按白名单隔离；Blueprint/DataAsset/Level 采用单写者。回退只逆向本增量精确 hunk 或独占资产，不使用整工作树重置，也不恢复另一任务拥有的 Package。
- 实现阶段新增了独立 `PreparationVolume`，复用摆锤现有 60 Hz Timer 重试 ETA 请求；同一半摆每个接收者独立去重，真实 `SourceComponent` 固定为 Bob，未增加角色冲量。
- 全身响应组件只在 Prepared/Simulating/Settling 与 Downed 延迟睡眠的一帧启用 Tick；Downed 后共享组件和 PhysicsControl 均停 Tick，真实动态刚体接触可重新唤醒。
- 构建时发现 `D:\UE5_8` 自身已有 OIT 源码改动，UBT 同时尝试重链 `UnrealEditor-NetCore.dll`；本任务没有修改或清理任何引擎文件，后续完整构建必须在用户关闭 Editor 后再次确认引擎构建边界。

## 验证与下一步

- 验证：`git diff --check` 通过；UHT 和所有新增/修改游戏模块源码多轮编译通过，最终一轮 4 个动作成功。完整链接受运行中 Editor/Live Coding 与 `UnrealEditor-Demo.dll`/`UnrealEditor-NetCore.dll` 占用阻断，自动化与 PIE 尚未执行，Content/Config 当前零改动。
- 下一步：用户安全关闭 Editor 并通知后，先完成隔离的项目/引擎构建边界核查与完整链接；重启后由用户创建两份 PCA，随后用官方 MCP 完成其余 DA/CDO/Blueprint 装配，用户点击 PCA Compile，最后执行自动化与 PIE。
