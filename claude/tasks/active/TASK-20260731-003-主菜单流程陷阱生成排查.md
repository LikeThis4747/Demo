# TASK-20260731-003 — 主菜单流程陷阱生成排查

- Owner：Codex / 当前会话
- Status：done（根因已定位，用户已修复并 PIE 验证通过）
- Stage：已验收
- Created：2026-07-31
- Updated：2026-08-03

## 目标与验收

- 目标：定位从主菜单进入 `L_Game` 后 PCG 场景能生成、玩家与追猎者能就位，但陷阱资源不再生成的原因。
- 验收：以当前源码、`L_Game` 中 GameplayPopulator/Generator 的真实实例属性、相关 DataAsset 与一次主菜单到游戏关卡的 PIE 日志形成可复核根因链，并给出最小解决方案。
- 非目标：本轮不修改关卡、蓝图、DataAsset、配置或 C++；不重构 Population 系统。

## 修改范围

- 允许修改：仅本任务卡，用于记录排查证据。
- 共享/潜在冲突：工作区已有主菜单、GameFlow、`L_Game`、`Level0` 与配置未提交改动，全部只读核对。
- 并行拆分/依赖：优先通过 UE5.8 官方 MCP 读取 Actor UPROPERTY、蓝图 CDO 与 DataAsset；必要时用本地 UE Editor MCP 补充 PIE 日志。
- 用户已授权范围：排查问题并提出解决方案；未授权落盘修复。

## 计划与检查点

- [x] 阅读当前 Population 与 GameFlow 真实源码，确认触发时序和引用要求。
- [x] 回读 `L_Game` 中 Generator、GameplayPopulator 与 Profile 的真实属性和引用。
- [x] 从 `L_MainMenu` 进入 `L_Game` 跑 PIE，核对 Seed 请求、生成完成事件与 Population 日志。
- [x] 区分根因、次要警告和最小修复方案，等待用户确认。

## 验证

- 命令/场景：官方 MCP 属性读取、引用检查、主菜单到游戏关卡 PIE 与日志。
- 结果：根因一为 `L_Game` 的 `BP_GameplayPopulator_C_1.Generator=None`，其 BeginPlay 直接返回，运行时地刺和磁性物体实例均为 0。`PopulationProfile` 有效，地刺/磁性物体两条规则及资源依赖均完整。根因二为 `RuntimeLevelGenerator_1.TriggerMode=BeginPlay`，而正式 `BP_ZeroEscapeGameMode` 又调用 `GenerateFromRequest`；主菜单 Seed 6956 的实测日志先生成默认 Seed 15339，再生成 6956，发生重复生成。
- 用户操作（时机、步骤、预期结果）：若用户授权，在 `L_Game` 只改两个实例字段：`BP_GameplayPopulator_C_1.Generator=RuntimeLevelGenerator_1`；`RuntimeLevelGenerator_1.TriggerMode=Manual`。保存关卡后从 `L_MainMenu` PIE，预期只出现一次菜单 Seed 的 `ZE_PCG_RESULT`，随后出现 Populator 放置日志与 `BP_SpikeTrap`/`BP_MagneticProp` 实例。
- 用户验收：通过（2026-08-03，用户确认陷阱与资源正常生成）

## 阻塞、风险与下一步

- 阻塞：无。
- 风险：`L_Game` 与相关蓝图尚未提交，必须以当前编辑器实例值为准。
- 下一步：用户已于 2026-08-03 确认从主菜单进入 L_Game 后陷阱与资源正常生成。本卡完成。
