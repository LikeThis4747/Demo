# TASK-20260727-006 — 追猎者最小 ABP 只读审查

- Owner：Codex 当前会话
- Status：active
- Stage：实现并验证完成，等待用户验收
- Created：2026-07-27
- Updated：2026-07-27

## 目标与验收

- 目标：完整核对现有追猎者 AI 移动闭环、`BP_Pursuer` 装配、新角色 Mesh/Skeleton/动画资产与现有 AnimBP，给出与当前项目一致的最小 ABP 手动创建步骤。
- 验收：明确现有 AI 权威与接入边界；确认目标素材是否已有 ABP、动画与 Skeleton 兼容风险；步骤包含资产位置、节点/状态机、角色装配和 PIE 验证。
- 非目标：不修改源码、蓝图、AnimBP、第三方素材、DataAsset、关卡或项目配置；不创建/重定向动画资产；不改变当前 Physics Control 任务范围。

## 修改范围

- 允许修改：仅本任务卡。
- 共享/潜在冲突：读取 `PursuerCharacter`、`PursuerAIController`、`PursuerConfig`、`BP_Pursuer` 与动画资产；不修改它们，避免与 `TASK-20260727-001` 重叠。
- 并行拆分/依赖：依赖 UE Editor MCP 在线，只读核对 C++ 与真实 UE 资产；以实时源码/资产覆盖旧记忆。
- 用户已授权范围：用户先要求完整阅读项目并提供创建最小 ABP 的步骤；随后明确回复“允许你做”，授权仅修改 `ABP_Pursuer_Locomotion`，创建 `GroundSpeed` 和 EventGraph 速度计算连线。

## 计划与检查点

- [x] 读取当前任务记忆、架构规范、工作流及追猎者相关活跃任务卡。
- [x] 审查追猎者 AI/Character/DataAsset/Build.cs 与相关配置。
- [x] 只读核对 `BP_Pursuer`、目标 Mesh、Skeleton、AnimSequence、AnimBP 和引用关系。
- [x] 输出最小 ABP 创建、装配与验证步骤。
- [x] 新增 `GroundSpeed` 并连接带 Pawn 有效性保护的速度计算图。
- [x] 编译、保存并回读变量、节点和连线。

## 验证

- 命令/场景：源码只读检索；UE Editor MCP 资产/蓝图/属性只读检查。
- 结果：现有 C++ Timer 状态机、`MoveToActor`、CharacterMovement 和 Level0 NavMesh 均已存在；日志证明追击攻击时机与 Physics Control 受击链运行。`BP_Pursuer` 已引用 `SK_Sci_Fi_Character_08_Full_01` 和 `ThirdPersonIdle`。Full_01、Idle、Walk、Run 共同引用素材包的 `UE4_Mannequin_Skeleton`，Full_01 另有专属 Physics Asset；无需为最小 locomotion 重定向。项目中没有该 Skeleton 的 AnimBP/BlendSpace/Montage。
- 实施结果：在 `/Game/ZeroEscape/Enemies/Animation/ABP_Pursuer_Locomotion` 新增非实例可编辑 Float 变量 `GroundSpeed`（分类 `Locomotion`）。EventGraph 使用 `BlueprintUpdateAnimation -> Branch -> Set GroundSpeed`，条件来自 `Is Valid(Try Get Pawn Owner)`，数值来自 `Vector Length XY(Get Velocity(Try Get Pawn Owner))`。
- 自动验证：AnimBP 编译状态 `UpToDate`，0 error、0 warning；保存日志记录目标包保存成功（Saved 1, Failed 0）；回读为 EventGraph 7 节点、7 条边，`GroundSpeed` 存在且不可实例编辑；AnimGraph 仍只有输出姿势根节点，未修改。
- 用户操作（时机、步骤、预期结果）：审查完成后给出 UE5.8 中文界面的逐步操作；用户自行执行前先确认目标 Mesh。
- 用户验收：待验收

## 阻塞、风险与下一步

- 阻塞：无；当前 `BP_Pursuer` 引用足以确认目标为 Full_01。若改选 Full_02 或 Without Arms，需要重新做 Skeleton 兼容审查。
- 风险：Full_02 与 Without Arms 使用各自 Skeleton，不能套用本步骤；最小 ABP 必须包含与未来攻击蒙太奇一致的 Slot；替换 Mesh 后仍需重新验收 Physics Asset 约束、胶囊对齐和局部受击表现。
- 下一步：等待用户验收；验收后再讨论是否把现有 Blend Space 接入 AnimGraph。
