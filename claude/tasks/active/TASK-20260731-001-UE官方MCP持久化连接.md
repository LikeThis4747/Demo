# TASK-20260731-001 — UE5.8 官方 MCP 持久化连接

- Owner：Codex（当前会话）
- Status：active
- Stage：待用户验收
- Created：2026-07-31
- Updated：2026-07-31

## 目标与验收

- 目标：让 Codex 与 CodeBuddy 都通过项目级配置发现 UE5.8 官方 MCP，并验证 UE 重启后的会话恢复行为。
- 验收：两个客户端首次加载后都能调用 `list_toolsets`；UE 重启后，在原任务中再次调用并确认是否自动重新初始化会话。
- 非目标：不修改 UE C++、资产、引擎插件、端口或自动启动设置；本阶段不实现额外的重连适配层。

## 修改范围

- 允许修改：`D:\UE5projects\Demo\.codex\config.toml`、`D:\UE5projects\Demo\.vscode\mcp.json`、本任务卡、对应当日计划。
- 共享/潜在冲突：Codex 与 CodeBuddy 分别读取不同项目配置，但共同访问 `127.0.0.1:8000/mcp`。
- 并行拆分/依赖：先完成配置与语法验证，再由用户重载客户端并重启 UE 做同任务复测。
- 用户已授权范围：按已展示的最小配置方案同时支持 Codex 与 CodeBuddy。

## 计划与检查点

- [x] 确认 UE 官方 MCP 自动启动且端点可握手。
- [x] 写入 Codex 项目级 Streamable HTTP 配置。
- [x] 写入 CodeBuddy/VS Code 工作区 HTTP MCP 配置。
- [x] 完成 TOML、JSON 与官方端点验证。
- [ ] 用户完成两个客户端首次重载及 UE 重启复测。

## 验证

- 命令/场景：TOML/JSON 解析；MCP `initialize`、`tools/list`、`list_toolsets`；两个客户端首次加载；同任务内重启 UE 后再次调用。
- 结果：TOML 与 JSON 均解析成功；在 Demo 根目录执行 `codex mcp list` 时 `ue58-official-mcp` 为 enabled；官方端点 `initialize` 返回 HTTP 200，`tools/list` 返回 `list_toolsets`、`describe_toolset`、`call_tool`，`list_toolsets` 返回 23 个工具集；`git diff --check` 通过。
- 用户操作（时机、步骤、预期结果）：配置验证完成后按交付步骤操作；预期两个客户端都出现 `list_toolsets`、`describe_toolset`、`call_tool`。
- 用户验收：待验收

## 阻塞、风险与下一步

- 阻塞：当前 Codex 任务的工具表不会因文件写入而热刷新。
- 风险：若客户端在 UE 重启后继续复用旧 `Mcp-Session-Id`，项目级配置只能解决持久注册，不能独自解决同任务热重连。
- 下一步：完成静态与直连验证，然后交由用户执行首次重载和 UE 重启 A/B 验证；失败后再讨论重连适配层。
