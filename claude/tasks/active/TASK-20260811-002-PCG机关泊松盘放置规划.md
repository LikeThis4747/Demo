# TASK-20260811-002：PCG 机关与资源分层泊松放置规划

- Owner：Codex `/root`
- Status：Active（技术实现完成，等待用户体验验收）
- Stage：代码、资产迁移、构建、自动化与正式地图接入验证已完成；等待密度、分布、空间净距和通行体验验收
- Created：2026-08-11
- Updated：2026-08-12

## 目标与验收

- 从完整游戏循环出发，规划摆锤、地刺、冲锤、制导发射器与磁力资源物的 PCG 放置。
- 保持一个 `AZeroEscapeGameplayPopulator` 总控，在内部按“机关先、物理资源后”执行两个阶段。
- 两层使用独立数量预算、最小通路间距、确定性随机流和欠填统计；资源不能替代机关。
- 每个合法 1x2 高厅优先放一个摆锤，并计入机关目标。
- 直接替换旧 `OneEveryNCells + MaxCount` 规则链，不保留新旧双轨、兼容适配器或旧随机序列。
- 用户确认方案后先展示拟实现代码；未获明确实现授权前不修改功能代码或 UE 资产。

## 本轮实现范围

- 允许修改：任务卡与当日计划；已确认方案列出的 Population/Generator C++、现有 PCG 测试，以及原位迁移 `DA_Population_Default`/编译相关 Blueprint。
- 只读：当前 PCG、Population、五类对象代码，正式设计文档和 `DA_Population_Default`。
- 不触碰：其他任务产生的 UE 资产、Level0、追猎者动画和无关功能文件。

## 实现授权与基线

- 2026-08-12 用户明确授权按已展示方案开始实现，并允许提交当时全部工作区改动，不受其他任务阻塞。
- 完整基线：`d026668bb0befea6db89ba1131d2534093a2fc7f`；已推送内部工蜂 `origin/main`、远端哈希一致，基线后工作区为空。

## 已核验事实

- 当前正式高厅是 `HighCeilingRoom_Level0_1x2`。
- 项目正式设计把磁力物定义为逃脱、投掷、压制和防御所需的核心物理资源，并要求危险与资源分别通过生成验证。
- 现有 `AZeroEscapeGameplayPopulator` 已承担统一 Owner、Actor 登记和原子回滚，适合作为唯一填充总控，不需要再建两个 Manager。
- 官方 UE5.8 MCP 本轮连接正常，已回读 `/Game/ZeroEscape/Generation/Population/DA_Population_Default`：
  - 地刺：旧规则约每 4 个候选一处、最多 8 处、每处两个、间距 300 cm、仅直道、避开出生/出口邻格、Z=0。
  - 磁力物：旧规则约每 4 个候选一处、最多 8 处、每处一个、不限直道、不避开邻格、Z=50 cm。
- 旧 `FZeroEscapePlacementRule` 在 C++ 中只被 Profile、GameplayPopulator、旧预算 Policy 和对应测试消费，可在实现时直接替换。
- 三个新增机关的 Blueprint/Class 已存在；本任务只规划 PCG 选位，不修改其行为、伤害或表现。

## 稳定架构决策

- [x] 空间生成、流程对象、玩法填充保持清晰分工；Population 只消费最终 Plan。
- [x] Population 内部分成机关阶段与资源阶段，但仍是一套 Actor/Profile/Policy/Spawn 事务。
- [x] 机关层包含摆锤、地刺、冲锤、制导发射器；资源层当前只包含磁力物。
- [x] 机关层先执行；资源层只避让机关实际操作范围，不能抢机关配额或合法点。
- [x] 两层分别使用有限候选位置上的泊松盘式图距离拒绝采样。
- [x] 使用 `HazardPopulationPlacement` 与 `ResourcePopulationPlacement` 两个确定性随机域；资源调参不能洗牌机关。
- [x] 高厅摆锤为结构必放内容并计入机关目标。
- [x] 不增加每层配额、每类硬上限、动态 Director、通用注册表、策略 UObject、每类采样器或新 Manager。
- [x] 不继续保留泛化 `bStraightCorridorOnly`；地刺按实际资产覆盖和进入方向枚举合法候选。
- [x] 主路线资源空窗首轮纳入 Seed Sweep 观测；没有稳定“追逐区”语义和阈值前，不先建分区配额框架。
- [x] 用户确认可以按分层架构继续进入正式实施规划。
- [x] 普通机关改为“类型先、位置后”的加权采样，避免候选多的地刺/冲锤吞掉发射器稀缺位置；不增加每类保底。
- [x] 密度面积使用普通格与高厅可走格，楼梯仅作为图连接；跨层冲突只使用实际操作格。
- [x] 玩家、追猎者、Exit 只按实际占用/操作范围避让，不保留泛化邻格开关。
- [x] 资源候选空间足够大，不增加 `MinimumResourcesPerMap` 第二数量控制；低于密度目标记录回归告警，但不让整轮 Population 失败。
- [x] 资源先按整栋通行图的泊松间距选锚点格，再在格内扣除真实占地半径的安全矩形中均匀随机 X/Y，不再固定地板中心；本轮不额外改变磁力物朝向。
- [x] 用户确认 Population 支持 Easy/Normal/Hard 分档；只分密度、间距和普通机关权重，资产 Class 与装配数据全难度共享，三档初值可先相同。
- [x] 用户确认并授权实现级数据结构、失败语义和验证范围。
- [x] 首验参数已落盘：用户连续否决代表 Seed 仅生成 10 处与 20 处机关的两组过稀初值，并要求难度主要调整种类而非总量。Easy / Normal / Hard 机关密度最终为 26 / 28 / 30，间距统一为 2；地刺/冲锤/发射器权重为 `5:3:2` / `1:1:1` / `2:3:5`。资源 10/100 格、间距 3；磁力物占地半径 75 cm。最终平衡仍待体验校准。

## 旧代码取舍

### 直接替代/删除

- `FZeroEscapePlacementRule`、`UZeroEscapePopulationProfile::Rules`。
- `OneEveryNCells`、`MaxCount`、旧逐规则 Shuffle/Spawn 链。
- `EPopulationPlacementBudgetResult` 与旧 `Evaluate`。
- `bStraightCorridorOnly` 和旧随机调用序列兼容。

### 迁移

- 地刺每处两个 Actor、中心间距和 Z。
- 磁力物 Class、Z 落点与格内安全取点所需的真实占地半径。
- 冲锤、发射器经验证的装配偏移。
- `DA_Population_Default` 资产路径。

### 保留

- Populator 唯一 Owner、`SpawnedActors` 与原子回滚。
- PopulationProfile DataAsset、PlacementPolicy 文件职责与现有测试文件。
- RuntimeLevelGenerator 对最终 Plan 的权威所有权。

### 删除前审计

- `GetGeneratedOrdinaryGameplayCellWorldTransforms` 先用官方 MCP 查 Blueprint/资产调用；迁移调用后再删除，不留永久包装器。

## 拟实施文件

唯一可能新增：

- `Source/Demo/Private/PCG/Population/ZeroEscapePopulationPlacementPolicy.cpp`

拟修改：

- `Source/Demo/Public/PCG/Population/ZeroEscapePlacementTypes.h`
- `Source/Demo/Public/PCG/Population/ZeroEscapePopulationProfile.h`
- `Source/Demo/Public/PCG/Population/ZeroEscapeGameplayPopulator.h`
- `Source/Demo/Public/PCG/ZeroEscapeRuntimeLevelGenerator.h`
- `Source/Demo/Private/PCG/ZeroEscapeRuntimeLevelGenerator.cpp`
- `Source/Demo/Private/PCG/Population/ZeroEscapePopulationPlacementPolicy.h`
- `Source/Demo/Private/PCG/Population/ZeroEscapeGameplayPopulator.cpp`
- `Source/Demo/Private/PCG/ZeroEscapeGenerationCore.h`
- `Source/Demo/Private/PCG/Tests/ZeroEscapePopulationPlacementTests.cpp`
- `Source/Demo/Private/PCG/Tests/ZeroEscapeGenerationContractTests.cpp`
- `/Game/ZeroEscape/Generation/Population/DA_Population_Default`

## 验证计划

- 自动化：高厅必放、两层预算独立、资源调参不改变机关、类型合法性、两层间距、操作范围冲突、格内随机边界与非格心分布、确定性、分层欠填和原子回滚。
- Seed Sweep：两层 target/actual、每类候选/实际、拒绝原因、楼层分布、主路线/支路资源数与主路线最大无资源间隔。
- PIE：摆锤完整摆幅、地刺正面覆盖、冲锤全行程、导弹触发/发射通道、磁力物可用性、导航与玩家/追猎者通行。
- 体验验收：地图危险和物理资源都达到合适密度；各自不挤成团；资源靠近机关时形成反制玩法而非妨碍机关。

## 风险与门禁

- 摆锤 79° 目标摆幅和 84° 硬限位在正式 1x2 高厅内净距紧，必须实测。
- 初始密度与间距不在讨论阶段凭空固定，应先看正式地图多 Seed 候选统计。
- 官方 MCP 已核对当前磁力物的 `MagneticBody` 为带实体碰撞且影响导航的可移动 `StaticMeshComponent`；随机位置必须使用碰撞占地缩进后的格内安全区域，不能简单对格心叠加无边界偏移。
- 2026-08-12 官方 MCP 已确认相关 Population/三类物理机关资产干净；Level0 仍 Dirty，场景内手工摆位只作为装配参考，不作为 PCG 权威数据。
- 官方 MCP 图读取与二进制字符串检索均未发现旧普通格 Transform 查询的 Blueprint 调用；正式删除前再复核一次，不保留无调用的兼容接口。
- 正式修改 C++/资产前必须满足内部 Git 完整基线提交、推送和远端核验门禁，并与其他任务 Owner 协调干净工作区。
- 当前有其他任务的 UE 资产修改，不得纳入本任务。

## 下一步

1. 用户在 `L_Game` 多跑几个 Seed，观察地图是否“尽量均匀地满起来”，并分别反馈机关过空/过密、资源过空/过密。
2. 检查摆锤摆幅、冲锤完整行程、发射器正面触发/弹道、地刺覆盖和玩家/追猎者通行；这些画面与手感不由自动化替代。
3. 根据体验只调三档密度、间距和权重；先不增加分区配额、每层上限、每类保底或动态 Director。
4. 参数稳定后再执行 Easy / Normal / Hard 正式 Seed Sweep，冻结统计阈值并完成用户验收归档。
