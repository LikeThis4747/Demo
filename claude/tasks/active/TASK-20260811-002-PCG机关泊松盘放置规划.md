# TASK-20260811-002 — PCG 机关泊松盘放置规划

- Owner：Codex `/root`
- Status：active
- Stage：方案已形成，待用户确认
- Created：2026-08-11
- Updated：2026-08-11

## 目标与验收

- 目标：基于当前 Runtime PCG 的真实候选位置与可通行结构，为摆锤、冲锤和制导/预判抛射机关规划“候选位置上的泊松盘拒绝采样”接入方案。
- 验收：明确候选数据来源、地图规模与密度换算、最小间距语义、随机种子、机关类型分配、失败降级、职责归属、拟修改文件和验证指标；方案经用户确认后才进入代码预览或功能实施。
- 非目标：本轮不修改 PCG、机关 C++、Blueprint、DataAsset、Level0、伤害、机关行为或表现；不引入柏林噪声、Weighted Sample Elimination、每层配额或固定全图上限。

## 修改范围

- 允许修改：本任务卡；必要时新增 `claude/plans/` 下的讨论方案稿。
- 共享/潜在冲突：只读当前 `Source/Demo/Private/PCG`、`Source/Demo/Public/PCG`、三个机关 C++/DataAsset、PCG 与 Population 资产；不修改当前脏的 `/Game/Levels/Level0`、追猎者动画及轻受击任务文件。
- 并行拆分/依赖：依赖当前多层 PCG 生成结果能够提供合法候选位或足够的布局语义；依赖三个机关现有 Blueprint/Class 可供生成系统引用。
- 用户已授权范围：用户已确认选择候选位置上的泊松盘拒绝采样，并要求开始规划方案、可参考网络资料；该授权不包含功能代码或资产落盘。

## 计划与检查点

- [x] 阅读当前 PCG、Population、可通行图和三个机关真实代码/配置。
- [x] 确认候选位置应由哪个现有阶段生成，以及是否需要新增候选描述类型。
- [x] 规划地图规模到期望机关数量的换算和最小间距语义。
- [x] 规划离散候选点上的泊松盘拒绝采样流程、随机种子和降级路径。
- [x] 列出拟新增/修改文件、职责、并行冲突和验证指标。
- [ ] 向用户展示完整方案，等待明确确认。

## 验证

- 命令/场景：只读源码、项目记忆、UE 资产回读和公开算法资料；本轮不构建、不运行 PIE。
- 结果：已通过源码、官方 UE5.8 MCP 和现有资产回读确认：正式高厅为 1×2；当前 Population 仅有地刺/磁性物体旧规则；三个机关的根节点、局部轴、现用 Tuning 和 Level0 实例高度已核实。已形成 `claude/plans/2026-08-11-PCG机关泊松盘拒绝采样方案.md`；未构建、未运行 PIE、未修改功能文件或 UE 资产。
- 用户操作（时机、步骤、预期结果）：方案确认前无编辑器操作。
- 用户验收：待验收。

## 阻塞、风险与下一步

- 阻塞：无；官方 UE5.8 MCP 本轮连接正常。
- 风险：当前摆锤目标摆幅已提高到 79°、硬限位 84°，接入正式 1×2 高厅时必须沿房间长轴摆放并实际复核极限净距；不能复用早期 18° 横扫短轴的旧摆位结论。
- 待确认：高厅是“每个合法高厅优先放一个并计入全图目标”，还是与大量墙面候选完全等概率抽样。方案推荐前者。
- 下一步：向用户展示方案；用户确认后进入拟实现代码，不直接落盘功能修改。
