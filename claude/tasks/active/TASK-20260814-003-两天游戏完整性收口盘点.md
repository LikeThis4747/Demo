# TASK-20260814-003 — 两天游戏完整性收口盘点

- Owner：Codex `/root`
- 任务类型：纯文档规划
- 实现文件写入权：不占用
- Status：active
- Stage：建议已收敛，已补充 HUD/两小时教学方案，等待用户确认两天范围
- Created：2026-08-14
- Updated：2026-08-14

## 目标与验收

- 目标：基于当前正式策划、开发排期、项目记忆、活跃任务与可核实的实现/验证证据，给出只剩两天时保证 Demo 完整可玩的最小收口清单，并把 HUD/新手教学限制在两小时内。
- 验收：在对话中区分“必须补齐”“有余力再补”“立即冻结”，列出两天安排、整局验收路径与明确停止条件。
- 非目标：本轮不修改 C++、Blueprint、DataAsset、关卡、配置、音频资产或正式设计文档，不执行功能实现。

## 修改范围

- 允许修改：仅本任务卡。
- 只读输入：`.ai-context` / `memory-bank`（经 Memory MCP）、`DOC/Design/GAME_DESIGN.md`、`DOC/Design/DEVELOPMENT_SCHEDULE.md`、相关活跃任务卡、必要的源码/配置/验证记录。
- 并行边界：不占用唯一实现文件写入权，不修改其他会话或用户拥有的文件。
- 用户已授权范围：询问当前游戏完整性缺口与两天优先级；未授权功能落盘。

## 计划与检查点

- [x] 读取热记忆、错误记录、当前上下文与架构规则。
- [x] 核对正式游戏循环、排期与进度记录。
- [x] 汇总活跃任务和已知 P0 风险，区分功能缺失与验收缺失。
- [x] 输出两天收口清单、时间盒与整局验收标准。

## 验证

- 方式：只读文档、配置、源码、官方 UE5.8 MCP 资产回读与现有运行日志交叉核对。
- 结果：正式 GameMode 已装配玩家、追猎者、出口、GameState、Controller、HUD 与结算界面；日志存在 `L_MainMenu -> L_Game`、PCG/Population/开局成功和追猎者实际攻击证据。当前出口开局即激活，HUD 只绘制准星，主菜单没有操作/目标说明，源码未接入正式声音；官方 MCP 回读玩家蓝图与追猎者资产的运行时最大速度均为 450 cm/s，玩家最大生命为 1000，而追猎者攻击为 18/30。基础动态导航并非完全未运行，但同一正式一局跨层持续追逐仍缺验收。
- HUD/教学补充：官方 MCP 确认主菜单默认即为 `Seed 12345 + Normal`，正式 GameMode 的 `HUDClass` 为 `/Script/Demo.ZeroEscapeHUD`；实际键位为 `WASD/鼠标/Space/右键抓取与持有/左键投掷/E 爆裂状态/Esc 暂停`。现有 `12345/Normal` 日志在 Game Setup 成功约 2 秒后即出现追猎者 Heavy 命中，当前工作态 Population 为 45 处机关、16 个资源，因此该 Seed 适合做“推荐第一局”，不适合在场内停住玩家阅读长教程。
- 用户验收：待用户确认收口范围。

## 阻塞、风险与下一步

- 当前风险：同一正式一局的真实追猎者跨层持续追逐仍缺验收；首轮 Development 打包、正式地图 Cook 与整局回归尚未闭合；当前另有 PCG 机关节奏实现任务占用唯一实现文件写入权。若把 `12345` 直接称为教学关而不检查安全开局，会让新人尚未读完提示就遭遇追击与机关压力。
- 收口建议：不建独立教学地图、不做教程状态机。保留 `12345/Normal` 为推荐第一局；把完整玩法说明放在主菜单现有设置面板（可改名“玩法与难度”）和暂停界面，进局只做生命/目标 HUD、短时操作提示及常驻简短键位。新增目标链若获准，只保留“支路收集物 -> 出口解锁”；暂停守场、新机关、复杂教学和 UI 美化。
- 两小时教学时间盒：15 分钟定文案与显示层级；30 分钟主菜单/暂停静态说明；35 分钟扩展现有 Canvas HUD；30 分钟编译、资产编译保存与一轮 `12345` PIE；10 分钟修字和边界检查。超时先删动态分步教程、退出方向箭头和动画，不删生命、当前目标与基本操作说明。
- 下一步：等待用户确认是否采用该两天削减方案；确认后先协调爆裂投掷任务释放/缩减实现写入权，再进入正式实施计划与基线门禁。
