# TASK-20260724-001 — 静态网格批量体检

- Owner：Codex / root
- Status：active
- Stage：分步验证（构建通过，等待 UE/MCP 实测）
- Created：2026-07-24
- Updated：2026-07-24

## 目标与验收

- 目标：为现有 UEEditorMCP 增加只读的静态网格批量体检动作，一次读取指定 Content 目录下网格的局部 Bounds、尺寸、Pivot 相对 Bounds 的位置和相对 300 cm 网格的比例。
- 验收：动作可递归扫描 `SciFiHydroLab` 静态网格目录；FloorA/WallB1 的结果与编辑器实测尺寸及角点/端点 Pivot 一致；不修改任何素材资产。
- 非目标：不修改运行时 PCG、不自动生成素材适配、不引入 Socket、不替代最终视觉拼接与碰撞/通行验证。

## 修改范围

- 允许修改：`Plugins/UEEditorMCP/Source/UEEditorMCP/Public/Actions/EditorActions.h`、`Plugins/UEEditorMCP/Source/UEEditorMCP/Private/Actions/EditorActions.cpp`、`Plugins/UEEditorMCP/Source/UEEditorMCP/Private/MCPBridge.cpp`、`Plugins/UEEditorMCP/Python/ue_editor_mcp/registry/actions.py`、本任务卡及对应 DailyPlan。
- 共享/潜在冲突：只扩展 Editor-only 插件；不修改 `Source/Demo/PCG`、`Content` 或第三方素材包。
- 并行拆分/依赖：无需并行；依赖 UE 关闭后重新编译插件，随后重启 UE 与 MCP 验证新动作。
- 用户已授权范围：批量检查功能已完成；用户另行确认新增独立的 `editor.set_static_mesh_component`，用于给当前关卡中恰好拥有一个 `UStaticMeshComponent` 的 Actor 批量绑定同一个 Mesh。拟实现代码已在对话中展示，并已明确允许落盘、编译。

## 计划与检查点

- [x] 核对现有 `editor.list_assets`、动作注册与 Python schema 结构。
- [x] 收敛到只读最小版本：目录扫描 + Bounds/Pivot/Grid Units。
- [x] 在对话中展示拟实现代码并取得源代码落盘授权。
- [x] 修改四个既有插件文件。
- [x] 关闭 UE 后构建 `DemoEditor Win64 Development`。
- [ ] 重启 UE/MCP，对 `SciFiHydroLab` 执行批量检查并抽查 FloorA/WallB1。
- [x] 重启 UE/MCP，批量读取 HydroLab 312 个 Static Mesh；0 个加载失败。
- [x] 检查 HydroLab Overview 与 Demonstration；统计 2592 个 Actor、2110 个 StaticMeshActor 的真实素材使用情况。
- [x] 形成 PCG 结构素材尺寸与 Trim 对应表。
- [x] 从 Demonstration 提取高频结构/Trim 邻近组合，并记录走廊段风格一致性与未来房间 Style Palette 扩展约束。
- [x] 在 `DOC/Outputs/MCP/` 记录批量体检工具的目的、位置、方案、输入输出、验证与边界。
- [x] 在 Level0 重搭 600 cm 地板宽度的双格 L 形结构：10 Floor、10 Ceiling、14 Wall、14 WallTrimG、6 PillarC；Floor/Ceiling Trim 暂停使用。
- [x] 在 Level0 创建并分组 18 个 `PCG_StyleB_*` 空 StaticMeshActor；位置和旋转按 300×600 开放走廊及候选 Trim 偏移布置。
- [x] 新增并编译 `editor.set_static_mesh_component`；不修改现有属性工具，不自动保存其他 Dirty Package。
- [x] 使用新动作按 7 组绑定 Level0 的墙、地板、天花板和 Trim；18/18 个 Actor 回读验证成功。
- [x] 用户完成首轮视觉检查：Floor Trim 与 Floor 表面重叠产生明显 Z-fighting；WallTrimG 正常；CeilingBTrim1 未贴合天花板并留下空心区域。当前组合判定不通过，Level0 未保存。
- [x] 重新打开供应商 `Demonstration`，按实际接触关系提取直边 Floor/Ceiling Trim 与拐角组合；确认 `TrimB + TrimD` 为地板外接边条、`CeilingBTrim1` 不是墙顶填缝件、WallH 转角采用 45° 倒角。
- [x] 根据示例证据修正 Level0，并加入 90 度拐角；旧的 18 个错误样例已删除，新结构 54/54 个 Actor 创建、绑定和分组成功，等待用户视觉验收后再保存。
- [x] 下沉 CeilingC 的尝试未改善漏光，且在天花板外表面引入 Z-fighting；走廊与房间共 19 块 CeilingC 已全部恢复到 Pivot Z=305。
- [x] 改用已验证的 `WallTrimG` 作为墙顶压条：从现有 22 段墙内侧 Trim 派生同 XY/Yaw 的顶部实例，Pivot Z=278.28，顶部穿入 CeilingC 约 1 cm；22/22 创建、绑定和分组成功，等待视觉验收。
- [x] 在走廊末端新增 900×900 cm（3×3 格）小房间：600 cm 入口、9 Floor、9 Ceiling、10 Wall、10 WallTrimG、3 边界 PillarC 和 1 根中央绕行柱；42/42 个 Actor 创建、绑定和分组成功。

## 验证

- 命令/场景：C++ 构建；`editor.inspect_static_meshes` 扫描 `/Game/SciFiHydroLab`（实际目录以资产注册表为准）；对照用户已测的 FloorA 150×300×4、WallB1 约 11×300×300。
- 结果：`DemoEditor Win64 Development` 构建成功；Python action registry 语法检查通过；本任务文件范围 `git diff --check` 通过。`editor.inspect_static_meshes` 已进入 MCP（此前动作总数 150），扫描 `/Game/SciFiHydroLab` 得到 312/312 个网格、0 个加载失败。新增 `editor.set_static_mesh_component` 后再次完整冷编译成功，插件编译、链接与 Target Metadata 均通过；等待重启后确认动作总数与关卡实例写入结果。FloorA-L 均约 150×300×3.75 且 Pivot 一致；普通 Wall/Ceiling/Trim 大量采用 300 cm 主长度。
- 素材表：`DOC/Design/PCG/SCIFI_HYDROLAB_MODULE_TABLE.md` 已记录尺寸、Pivot、Demonstration 使用次数、首版候选与暂缓项。
- 用户操作（时机、步骤、预期结果）：源代码获准后，请用户关闭 UE；构建完成后再重新打开工程。无需逐个手工测 Pivot。
- 用户验收：待验收

## 阻塞、风险与下一步

- 阻塞：自动邻近统计可证明 Trim 被重复使用，但密集示例关卡中同一 Trim 并非严格一对一绑定结构件；最终固定偏移必须用 Level0 代表组合实拼确认。
- 风险：Bounds/Pivot 数据只能证明几何规格，不能证明材质接缝、碰撞或玩家通行；这些仍需按尺寸分组后抽查代表件。
- 下一步：用户在 Level0 检查墙顶压条是否遮住漏光且没有新增 Z-fighting，并确认 900×900 房间、600 入口和中央绕行柱的尺度；通过后保存并进入 PIE 通行验证，再重新规划 PCG 代码。
