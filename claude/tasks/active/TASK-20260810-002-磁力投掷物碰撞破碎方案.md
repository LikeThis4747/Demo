# TASK-20260810-002 — 磁力投掷物碰撞破碎方案

- Owner：Codex `/root`
- Status：active
- Stage：方案已确认；用户已授权完整 Git 基线和首个 Geometry Collection 手工制作，尚未授权 C++/Blueprint 运行逻辑实施
- Created：2026-08-10
- Updated：2026-08-10

## 目标与验收

- 目标：形成当前磁力物“正式投掷后第一次有效碰撞才破碎，碎片随后全部消失”的第一阶段正式实施方案，并给后续“大物体破碎后保留一块可再次投掷的小磁力物”保留清晰扩展路径。
- 验收：方案必须基于当前 C++、官方 UE MCP 蓝图/静态网格回读和 UE5.8 Geometry Collection 真实接口；必须列出状态 Owner、完整文件/资产范围、资产制作步骤、失败回退和 PIE 验证矩阵。
- 非目标：本轮不修改 C++、Blueprint、Geometry Collection、材质、关卡、配置或第三方素材；不运行构建或 PIE；不实现可重复使用的大/小磁力物链。

## 修改范围

- 允许修改：
  - `claude/tasks/active/TASK-20260810-002-磁力投掷物碰撞破碎方案.md`
  - `DOC/DailyPlan/2026-08-10-磁力投掷物碰撞破碎实施计划.md`
  - `DOC/README.md` 中本计划的索引
- 共享/潜在冲突：工作区已有制导机关 DataAsset 改动，以及轻受击调研任务卡和拟实现方案文档，归属其他工作，不读取为本方案授权、不修改、不提交。
- 并行拆分/依赖：当前方案依赖既有磁力抓取与投掷闭环；不依赖轻受击是否实施，也不修改已冻结的 HeavyImpact。
- 用户已授权范围：用户已授权把当前全部已确认改动形成并推送完整基线，并在该门禁通过后于 Level0 测试区手工制作首个 Geometry Collection；尚未授权 C++、Blueprint 运行逻辑、材质或正式关卡装配实施。

## 计划与检查点

- [x] 只读核对 `ThrowHeldObject`、普通放下、抓取阶段和 `AttackProjectile` 当前边界。
- [x] 通过官方 UE MCP 回读 `BP_MagneticProp` 父类、组件、质量和 `SM_crate4` 真实引用。
- [x] 只读核对 UE5.8 `UGeometryCollectionComponent` 的初始速度、显式解簇和 Remove On Break 支持。
- [x] 确定第一阶段状态机、职责、文件/资产范围、失败回退和验证矩阵。
- [x] 写入正式 DailyPlan 并更新文档索引。
- [x] 用户确认当前全部改动保留，并授权完整 Git 基线提交、推送和远端核验。
- [ ] 基线门禁通过后，逐步指导用户在 Level0 空地用临时 `SM_crate4` 制作 `GC_MagneticCrate4_P0`。
- [ ] Geometry Collection 资产通过外观/碎片数量验证后，再由用户决定是否授权 C++/Blueprint 运行逻辑。

## 验证

- 命令/场景：官方 UE MCP 只读回读；本地源码与 UE5.8 引擎头文件只读检查；Markdown 路径、标题和 Git diff 复核。
- 结果：方案证据已核对；本轮没有产生可运行功能，因此不存在构建、Blueprint 编译或 PIE 通过结论。
- 用户操作（时机、步骤、预期结果）：实施获批后，用户可在 Fracture Mode 中参与 `SM_crate4` 的切片外观确认；预期是 12～16 块可读碎片，不出现大量细小刚体。
- 用户验收：待验收

## 阻塞、风险与下一步

- 阻塞：当前全部改动归属已由用户统一确认；正在执行全工作区基线 commit、push 和远端核验，完成前不创建 Geometry Collection。
- 风险：Geometry Collection 若在抓取阶段直接参与模拟，会扩大碰撞和 Physics Handle 变量；方案已通过“完整静态网格保持到命中后才替换”避开该风险。破碎回调若立即销毁原 Actor，可能影响同帧目标受击监听；方案改为下一帧替换。
- 下一步：基线门禁通过后继续中文版 Fracture Mode 的逐步资产制作；代码实施仍需后续单独授权。
