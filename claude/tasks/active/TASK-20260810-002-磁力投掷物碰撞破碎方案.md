# TASK-20260810-002 磁力投掷物碰撞破碎方案

- Owner：Codex `/root`
- Status：active
- Stage：P0 冲击感修正版 C++ 已落地，构建、官方资产回读和隔离碎片散开验证通过；等待用户完成 Level0 投掷命中 AI 的力量感验收
- Created：2026-08-10
- Updated：2026-08-11

## 目标与验收

- P0 目标：只有 `ThrowHeldObject` 正式投掷后的首次合格 Blocking Hit 才把完整磁力物替换为 Geometry Collection 碎片；碎片在 2~3 秒内移除，6 秒为异常兜底。
- 交互目标：抓取、拉取、持有、普通放下和中断释放均不碎；正式投掷命中角色时保留现有 Light 受击，再于下一帧替换。
- 资源目标：P0 碎片全部不可抓取并消失；“大物体破碎后留一块可再投的小物体”保留为 P1。
- 完成门槛：C++ 构建、Blueprint 编译/保存、官方 MCP 回读、完整 PIE 边界矩阵和用户手感验收全部通过。前三项及隔离碎片运行验证已通过；玩家完整投掷链和边界手感尚待验收。

## 当前真实基线

- 已有资产：`/Game/ZeroEscape/Interaction/Magnetism/Destruction/GC_MagneticCrate4_P0`
  - 1 个根、18 个叶子碎片。
  - 原 `MI_crate4` 材质正常；骨骼调试颜色已关闭。
  - Remove On Break：Post Break `0~0s`，Removal `2~3s`。
- 已有轻受击实现：`UMagneticObjectComponent` 已独占正式投掷的碰撞快照、`AttackProjectileBody`/Tag、Hit 通知、CCD、投掷者过滤和 Timer。
- 设计修订：破碎组件不再独立绑定 `OnComponentHit` 或控制 CCD；只订阅 `UMagneticObjectComponent` 新增的通用投掷命中原生委托。
- 功能写入前基线 `5988854d30ca234b77d27ea85eb580c6830550a3` 已完整提交、推送并与 `origin/main` 对齐。
- 初版 P0 落地提交为 `559223d64edc158b4d7bcdfb5d38c9defae31740`；本次力量感修正的干净基线为 `ba630a5c80e0e4f0dbc9946736b9869149a95fc9`。

## 实际实现文件范围

### 修改 C++

- `Source/Demo/Public/Components/Magnetism/MagneticObjectComponent.h`
- `Source/Demo/Private/Components/Magnetism/MagneticObjectComponent.cpp`
- `Source/Demo/Public/Actors/Magnetism/MagneticPrototypeProp.h`
- `Source/Demo/Private/Actors/Magnetism/MagneticPrototypeProp.cpp`
- `Source/Demo/Private/Components/Magnetism/ElectromagneticGrabComponent.cpp`
- `Source/Demo/Demo.Build.cs`

### 新增 C++

- `Source/Demo/Public/Components/Magnetism/MagneticThrowBreakComponent.h`
- `Source/Demo/Private/Components/Magnetism/MagneticThrowBreakComponent.cpp`
- `Source/Demo/Public/Actors/Magnetism/MagneticFractureActor.h`
- `Source/Demo/Private/Actors/Magnetism/MagneticFractureActor.cpp`

### UE 资产

- 修改 `/Game/ZeroEscape/Interaction/Magnetism/BP_MagneticProp`
- 新增 `/Game/ZeroEscape/Interaction/Magnetism/Destruction/BP_MagneticFracture_P0`
- 原则上不再修改 `GC_MagneticCrate4_P0`；仅在 PIE 证明资产参数有问题时另行调整。

## 已完成检查点

- [x] 只读核对磁力抓取、正式投掷、普通释放和候选查询边界。
- [x] 通过官方 UE5.8 MCP 回读 `BP_MagneticProp` 的父类、组件、质量、碰撞和网格引用。
- [x] 只读核对 UE5.8 Geometry Collection 初始速度、显式解簇和 Remove On Break 接口。
- [x] 在 Level0 测试区完成 `GC_MagneticCrate4_P0`，实际为 1 根 + 18 叶子。
- [x] 用户完成一次落地破碎、材质显示和碎片消失测试。
- [x] 读取最新未归档审阅报告，采纳 2 项阻塞和适用于当前基线的高回报建议。
- [x] 根据已落地 Light 系统修订职责：正式投掷事务单一 Owner，破碎为委托消费者。
- [x] 在拟实现文档补齐碎片碰撞、冲量门槛、8 秒监听硬上限、6 秒清理兜底、失败回退和竞态矩阵。
- [x] 用户审阅修订版并明确授权运行时代码与资产接线。
- [x] 将实施前全部已确认改动完整 commit、push，并核验远端包含与工作区 clean；基线为 `5988854`。
- [x] 实现共享正式投掷事务、破碎消费者、GC 替身 Actor 和 Blueprint 接线。
- [x] `DemoEditor Win64 Development` 构建通过。
- [x] 官方 UE5.8 MCP 完成 Blueprint 编译、保存和关键属性回读。
- [x] 隔离运行时确认显式解簇以及约 2.06 秒后的 Remove On Break 清理。
- [x] 检查旧实现残留：运行时代码不存在破碎侧独立 Hit、独立碰撞快照或旧 DeferredDisarm 路径。
- [x] 根据用户对初版“命中 AI 后像停住再下落”的反馈定位原因：初版在 next-tick 重读了已经过 Chaos 阻挡求解的速度，且解簇后没有主动分离速度。
- [x] 合格 Hit 当帧冻结修正后的继承运动：以真实接触方向和 `NormalImpulse / 质量` 估算损失的法向速度，默认保留 60%，最大继承线速度 5000 cm/s。
- [x] 将最小破碎冲量首值提高到 `5000 kg·cm/s`，避免低能量轻擦也被表现成强破碎。
- [x] Geometry Collection 解簇后增加 `350 cm/s`、半径倍率 `1.25` 的受控径向速度，仅用于分开相邻碎片；整体前进仍由命中运动继承。
- [x] 修正版 `DemoEditor Win64 Development` 构建通过；官方 MCP 回读 5000 / 0.6 / 5000 / 350 / 1.25 / 6 / 8，两个 Blueprint 以 warnings-as-errors 编译且未产生脏包。
- [x] 隔离 PIE 日志与画面确认：原材质保持、碎片在空中解簇并散开，随后按 Remove On Break 清理；临时测试 Actor 与 Level0 写盘变化已精确移除/恢复。
- [ ] 用户在 Level0 验收真实抓取 -> 投掷 -> 合格碰撞，以及低冲量、薄墙、角落、角色命中和多物体边界。
- [ ] 用户验收通过后归档审阅报告、实施计划与任务卡。

## 关键决策

- 完整 Static Mesh 保持到合格命中，下一帧才生成 GC 替身；Geometry Collection 不参与抓取。
- `UMagneticObjectComponent` 是 Hit 通知、CCD、临时碰撞身份和恢复快照的唯一 Owner。
- Light 窗口仍为 2.5 秒；破碎监听最晚 8 秒结束，避免长期潜伏后随机碎裂。
- 最小破碎冲量默认 5000 kg·cm/s；低于门槛不消费。
- 合格 Hit 当帧冻结继承运动，不在 next-tick 重读可能已经归零的刚体速度；冲量/质量只用于补回缺失的前进分量，不改变横向和角速度来源。
- 碎片使用 350 cm/s 的受控径向速度分离。这是本项目的表现参数，不是 UE 的固定破碎规则，也不是对目标施加第二次伤害冲量。
- 碎片 ObjectType 为 PhysicsBody，但仅 Block WorldStatic/WorldDynamic，Ignore Pawn/Camera/PhysicsBody；不触发受击、不可抓取、不影响导航。
- `SafetyLifetimeSeconds = 6s`，大于 Remove On Break 最长 3 秒并保留 3 秒异常余量。
- Build.cs 只追加 `GeometryCollectionEngine`，不新增 `Chaos`。

## 风险与回退

- 最大技术风险是 Light 与破碎重复控制同一个 Hit/CCD 状态；修订版已通过共享正式投掷事务消除双 Owner。
- 高速薄墙隧穿由共享事务现有 CCD 负责，并纳入贴墙、斜射薄墙和近地投掷验证。
- 替身 Spawn 失败时保留原 Actor、完整 Disarm、转为 `BreakFailed`，不循环报错。
- 碎片碰撞若仍干扰角色或其他道具，只在 `AMagneticFractureActor` 的单点碰撞契约中调，不在 BP 建第二份配置。
- 最小回退只撤掉破碎组件、替身 Actor 和接线；现有抓取与 Light 链保持不变。

## 下一步

1. 用户在 Level0 重点验收“正式投掷命中 AI”：完整物体应在接触处立即替换，碎片继续沿撞击方向运动并向外散开，不应先悬停再垂直落下。
2. 再补验真实输入链和边界：低冲量、薄墙、角落、角色命中与多物体竞态。
3. 若门槛、继承比例、碎片分离速度或停留时间需要调整，只修改当前单一参数来源，不恢复 next-tick 重读速度或旧的双 Hit/CCD 方案。
4. 验收通过后归档审阅报告和计划，并关闭本任务；P1“保留一块可再次投掷的小磁力物”另立任务。
