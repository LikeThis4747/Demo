# TASK-20260817-002：Level0 测试地图蓝色全息传送门

- Owner：Codex `/root`
- 类型：实现（C++ + Blueprint + Level0 摆放）
- 范围：出口视觉与光团达标提示；不改 PCG/WFC、楼梯灯、磁力拾取和追猎逻辑。
- 目标：在不改出口触发逻辑的前提下，用蓝色不透景能量面、动态噪声边缘与蓝色 PointLight 做 Level0 传送门预览；实际游戏出口仍支持光团达标后短闪一次。
- Level0 测试摆放：在 `PlayerStart_0` 前方约 600cm 直接放置 `Portal_Test_PlayerStart`，门框约 450x300cm，全息面约 400x250cm，适配测试场地。
- 修改文件：
  - `Source/Demo/Public/GameFlow/ZeroEscapeExitVolume.h`
  - `Source/Demo/Private/GameFlow/ZeroEscapeExitVolume.cpp`
  - `Source/Demo/Private/GameFlow/ZeroEscapeGameMode.cpp`
  - `Content/ZeroEscape/GameFlow/BP_ZeroEscapeExitVolume.uasset`
  - `Content/Levels/Level0.umap`
- 视觉资产：`Content/ZeroEscape/Portal/` 下的传送门材质资产。
- 验证：官方 UE MCP 回读 Level0 仅保留出口蓝图原有组件；GoalVisual 为竖直椭圆蓝色动态边缘，PortalSurface 为不透景深蓝能量面，蓝色点光源已保留；PIE 已启动供验收。
- 风险/后续：Level0 实例是快速验收用的静态视觉预览，不承担正式出口触发逻辑；Portal 2 风格的火焰撕裂边缘仍以本次最小材质调整为限，等待用户验收反馈，不标记完成。
