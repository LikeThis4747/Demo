# TASK-20260723-004 — 运行时 WFC 素材预览筛选

- Owner：Codex / root
- Status：active
- Stage：素材初筛（等待精确尺寸/Pivot 与通行复核）
- Created：2026-07-23
- Updated：2026-07-23

## 目标与验收

- 目标：在独立 UE 5.7.4 C++ 工程中，以 Demo 的低成本渲染基线检查两个科幻环境包是否适合《零号逃亡》的运行时 WFC 模块库。
- 验收：逐包记录模块覆盖、网格/Pivot/朝向/90°旋转、简单碰撞与持铁板通行、静态网格独立性、关闭高级渲染后的表现、版本/作者/来源/许可限制，以及项目自有 Blueprint 包装层适配结论。
- 非目标：本任务不修改 Demo 代码、Config 或 Content，不把候选素材迁入 Demo，也不实现 WFC 算法。

## 修改范围

- 允许修改：`D:\UE5projects\DemoAssetsPreview\` 内的预览工程文件；本任务卡；通过 Memory MCP 维护当前任务恢复摘要。
- 共享/潜在冲突：只读参考 `claude/artifacts/asset-selection/README.md`；与 `TASK-20260723-002` 的未来生成模块契约相关，但不修改其预计代码或资产路径。
- 并行拆分/依赖：先对齐预览工程配置并验证启动，再一次检查一个素材包；筛选结论将作为 PCG/WFC 模块契约的输入。
- 用户已授权范围：允许修改 `D:\UE5projects\DemoAssetsPreview`；允许在 Demo 内创建任务卡和更新记忆；当前不授权修改 Demo 代码或资产。

## 计划与检查点

- [x] 读取素材筛选标准与 Demo 架构/渲染基线。
- [x] 对比 Demo 与 DemoAssetsPreview 的项目配置。
- [x] 将预览工程 RendererSettings 与 HardwareTargetingSettings 同步到 Demo 基线，同时保留预览工程自己的地图、模块、ProjectID 与令牌。
- [x] 完成 DemoAssetsPreviewEditor 构建验证。
- [ ] 启动/重启 DemoAssetsPreview，确认配置生效并等待 Shader 稳定。
- [ ] 逐包检查 `SciFi Corridor Environment` 与 `Sicka SciFi Interior 2`，避免同时混入导致依赖归因不清。
- [ ] 与用户共同查看代表性地图、模块、碰撞和第三人称持板通行表现。
- [ ] 输出逐包结论：可用 / 条件可用 / 不建议，并列最小迁移候选集合。

## 2026-07-23 初筛证据

- `CorridorEnvironment`：79 个 uasset、3 张地图、18 个静态网格；其中 7 个是贴花载体。结构件主要只有门、门框、单块地板、墙、屋顶、立柱和顶部块，未发现命名明确的转角、T 形、十字或房间模块。
- `CorridorEnvironment`：`Showcase_A/B` 的旧 Matinee 对象在 UE 5.7.4 中产生外部容器加载告警；`Showcase_B` 还引用压缩包中不存在的 `战舰1`～`战舰5`。静态网格可继续单独检查，但示例地图交付不完整。
- `CorridorEnvironment`：二进制只读线索显示门、地板、墙、屋顶、立柱等 10 个网格包含 AggGeom/Convex 简单碰撞数据；门框未发现简单碰撞元素线索，需编辑器复核。
- `Sicka_Scifi_Interior2`：275 个 uasset、8 张地图、66 个静态网格；官方说明其中 48 个环境资产，但本地清单显示多数是道具/设备/管线/装饰，建筑壳主要是 3 个地板件、3 个墙件、楼梯和一套门，未发现专用 T 形/十字/房间模块。
- `Sicka_Scifi_Interior2`：主地图对 Lighting、Props、RenderSetup 子关卡存在重复流送引用告警；可绕过主地图直接使用静态网格。62 个非全息网格均有简单碰撞数据线索，仍需编辑器显示碰撞复核质量。
- 两个压缩包均只包含 Content 资产，没有供应商 C++/运行时插件；适合被项目自有 Blueprint 包装层引用，但不代表自身已构成完整 WFC 模块库。
- 初步建议：两者都不作为 WFC 主结构包；Sicka 可保留为工业科幻房间装饰/道具候选，Corridor 最多保留门、墙面、贴花等少量视觉件。

## 2026-07-23 可变空间假设与第二轮候选

- 用户当前偏好（允许未来调整）：类似 P5 印象空间的“宽走廊主干 + 小房间短支路”，陷阱布置在可控必经路；主要目标按 A/B/C 向终点 D 推进，或用 10 取 6 的阈值避免临近终点时返回起点。
- 回头路预算：进入小房间后允许一段短回撤，用柱、陷阱或物理物体形成与追猎者周旋的事件；不鼓励长期兜圈找材料或多次重复长路段。
- 该假设下，资产优先级调整为：宽直廊、L/T/X 路口、可控门洞、少量小房间/大厅、统一网格与简单碰撞；大型开放房间和复杂环路降级。
- 第二轮商店页预评分：SF Corridors 88/100；SciFi Base Kit Vol 1 86/100；AURUM Sci-Fi Tunnel Kit 83/100；JUPITER 80/100；Modular SciFi Hallways 77/100。均为下载前页面评分，价格、账户许可、实际宽度/Pivot/碰撞和 UE 5.7.4 表现仍需独立工程实测。
- 下载优先级：先测试 SF Corridors；若示例宽度或房间不足，再测试 SciFi Base Kit Vol 1；AURUM 作为更轻量、碰撞与 LOD 说明更完整的走廊备选。

## 验证

- 命令/场景：逐项比较 `DefaultEngine.ini` 的 RendererSettings 与 HardwareTargetingSettings；构建 `DemoAssetsPreviewEditor Win64 Development`；随后在预览工程内检查资产和 PIE。
- 结果：两个配置段已逐项匹配；`DemoAssetsPreviewEditor Win64 Development` 构建成功；已完成磁盘资产清单、运行日志、官方商品页与碰撞数据线索初筛。UE Editor MCP 当前未连接预览工程，因此尺寸、Pivot、正方向和实际通行仍未审计。
- 用户操作（时机、步骤、预期结果）：预览工程成功启动且 Shader 稳定后，与用户共同查看每包 Overview/Demo Map 和代表性模块。
- 用户验收：待验收

## 阻塞、风险与下一步

- 阻塞：UE Editor MCP 当前未连接预览工程；精确尺寸、Pivot、正方向、90°拼接和持板通行仍需编辑器检查。
- 风险：两个压缩包若同时导入会混淆缺失依赖与兼容问题来源；必须逐包验证。仅构建成功不能证明素材可用。
- 下一步：先决定是否按初筛结论停止深挖这两个包；若保留 Sicka 作装饰包，再只复核其地板、墙、门和代表性道具的尺寸/Pivot/碰撞。

## 2026-07-23 Demo 软件 Lumen 决策与实施

- 用户已将 `SF_Corridors` 导入 Demo，并明确授权修改 Demo 渲染配置与重启编辑器。
- 决策：运行时 WFC 不采用 Lightmass 关卡烘焙；先采用软件 Lumen，使用 Mesh Distance Fields；反射采用 Screen Space；硬件光追与 VSM 继续关闭。
- 本次修改范围：`D:\UE5projects\Demo\Config\DefaultEngine.ini` 与本任务卡；不修改 C++、Blueprint 或第三方 Content。
- 验证检查点：重启后确认无“Lumen 已启用，但没有光线追踪数据”提示；等待 Mesh Distance Fields/Shader 稳定；在 `SF_Corridors` 代表性场景执行无硬件光追性能基线。
- 实施结果：`DefaultEngine.ini` 已设置 `r.GenerateMeshDistanceFields=True`、`r.DynamicGlobalIlluminationMethod=1`、`r.ReflectionMethod=2`；`r.RayTracing=False` 与 `r.Shadow.Virtual.Enable=0` 保持不变。
- 重启验证：Demo 编辑器已正常关闭并重新启动；UE Editor MCP 恢复连接，World 与 Asset Registry 均 Ready。启动日志确认运行值为 Mesh Distance Fields `1`、Lumen GI `1`、Screen Space Reflections `2`，并明确记录硬件光追因 `r.RayTracing=0` 而关闭。
- 未完成验收：MCP 自动打开第三方 World 时触发 `crash_prevented` 并安全中止，编辑器未崩溃；需用户手动打开 `/Game/Assets/SFCorridors/Maps/SF_Corridors`，确认红字消失、等待距离场/Shader 稳定并观察画面。
