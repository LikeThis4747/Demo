# TASK-20260722-001 — 准星 HUD 诊断与 Lyra 对照

- Owner：Codex / root
- Status：active
- Stage：原因已确认，讨论修复方案
- Created：2026-07-22
- Updated：2026-07-22

## 目标与验收

- 目标：查明当前准星 HUD 未显示的实际链路原因，并说明 Lyra HUD 的实现分层。
- 验收：以 C++、配置、真实 GameMode/关卡/蓝图资产和运行日志为证据，区分已确认原因与推断；给出不落盘的最小修复建议。
- 非目标：本轮不修改功能代码、配置、蓝图或关卡资产，不擅自套用 Lyra 全套架构。

## 修改范围

- 允许修改：本任务卡；`claude/scratch/TASK-20260722-001-inspect-game-mode.py` 只读诊断脚本。
- 共享/潜在冲突：只读检查 `Source/Demo`、`Config`、`Content/ZeroEscape`、`Content/Levels` 与 UE Editor 当前资产；保留现有未提交改动。
- 并行拆分/依赖：无并行修改；资产结论依赖 UE Editor MCP 的当前工程绑定与可读状态。
- 用户已授权范围：阅读 AI 工作指引和项目代码并诊断准星；询问 Lyra HUD 实现。

## 计划与检查点

- [x] 读取启动记忆、架构规则、工作流和现有活跃任务。
- [x] 静态核对准星 HUD、原型 GameMode 和默认地图配置。
- [x] 核对真实 GameMode 蓝图默认值、Level0 覆盖、运行时 HUD 类与日志。
- [x] 对照本机 UE 5.7 Lyra 样例源码与资产，形成分层说明。

## 验证

- 命令/场景：只读源码/配置检索；UE Editor MCP 资产与运行时检查。
- 结果：`Level0` PIE 实际使用 `BP_ZeroEscapePrototypeGameMode_C`；蓝图 CDO 将 `HUDClass` 覆盖为 `/Script/Engine.HUD`，同时将 `PlayerControllerClass` 覆盖为 `/Script/Engine.PlayerController`。原生 CDO 对应值分别为 `/Script/Demo.ZeroEscapeHUD` 与 `/Script/Demo.ZeroEscapePlayerController`。准星类本身仍存在，蓝图覆盖导致其未被实例化。
- 用户操作（时机、步骤、预期结果）：本轮暂不需要。
- 用户验收：待验收

## 阻塞、风险与下一步

- 阻塞：无。
- 风险：修正 HUD 时若遗漏同一蓝图中的 `PlayerControllerClass` 覆盖，专用输入模式仍不会按设计运行；修复后仍需蓝图编译/保存与 PIE 可见性验证。
- 下一步：与用户确认最小修复范围；建议只修正 `BP_ZeroEscapePrototypeGameMode` 的 HUD Class 和 Player Controller Class，不引入 Lyra 全套 UI 框架。
