# TASK-20260815-003 — 摆锤冲锤灰盒替换

- Owner：Codex `/root`
- Status：active
- Stage：用户复测确认摆锤仍穿模；已定位为真实接触后共享 Heavy 立即忽略 PhysicsBody，用户授权所有 Heavy 统一保留现有 0.15s 阻挡窗口
- 任务类型：实现
- 实现文件写入权：Codex `/root` 独占；仅限本卡列出的 Heavy 接触放行实现、测试与任务记录
- Created：2026-08-15
- Updated：2026-08-15

## 目标与验收

- 目标：冲锤使用封闭的 HydroLab 通风罩外观；摆锤保留宽长方体轮廓并换成有法线与粗糙度细节的工业金属材质，同时加宽、略微抬高。
- 验收：冲锤从背面观察不再透空且视觉尺寸贴合 `120 x 280 x 280 cm` 碰撞；摆锤视觉与 `440 x 80 x 150 cm` 碰撞一致，最低点离地约 `55 cm`，侧面不能直接穿行；两个 Blueprint 编译保存，PIE 中运动、碰撞和倒地边界正常。
- 非目标：不改机关 C++ 运动、伤害、冲量、周期、PCG 放置逻辑；不修改 HydroLab 原始第三方资产。

## 修改范围

- 允许修改：`/Game/ZeroEscape/Hazards/Physics/BP_BatteringRamHazard`
- 允许修改：`/Game/ZeroEscape/Hazards/Physics/BP_PendulumHazard`
- 允许修改：`/Game/ZeroEscape/Hazards/Physics/Data/DA_PendulumHazard_Default`
- 允许修改：`/Game/Levels/Level0` 中现有冲锤与摆锤实例（用户要求在 Level0 落地替换）
- 允许修改：`Source/Demo/Private/Actors/Hazards/PendulumHazard.cpp`（仅手工恢复旧 `Prepare` 预测算法）
- 允许修改：`Source/Demo/Private/Components/Physics/HeavyImpactResponseComponent.cpp`
- 允许修改：`Source/Demo/Public/Components/Physics/HeavyImpactResponseComponent.h`
- 允许修改：`Source/Demo/Private/Physics/Tests/HeavyImpactResponseTests.cpp`
- 允许修改：本任务卡与 `DOC/DailyPlan/2026-08-15-摆锤冲锤灰盒替换.md`
- 共享/潜在冲突：工作区现有两份 `StandingImpact` 资产、PPT/README 与其他任务记录不属于本任务；不得修改、暂存或提交。
- 并行拆分/依赖：PCG 技术实现已提交推送并释放实现写入权；本任务接管当前唯一实现窗口，且不进入 PCG、磁力投掷或爆裂投掷文件。
- 并行文档改动（路径/Owner）：`DOC/README.md`、普通磁力投掷 DailyPlan、PCG 实施草案及其任务卡属于其他 Owner，本任务不得修改或暂存。
- 用户已授权范围：补齐冲锤通风罩背面并按当前灰盒冲锤尺寸装配；摆锤保留长方体灰盒轮廓，换工业材质/法线并执行已讨论的加宽和抬高。

## 计划与检查点

- [x] 官方 MCP 回读两个 Blueprint 真实父类、组件树、网格、材质与相对变换。
- [x] 回读两个机关 DataAsset 的当前碰撞尺寸与摆锤高度。
- [x] 确认无 C++ 修改：碰撞继续由 `RamBody` / `BobBody` 管理，美术网格保持无碰撞。
- [x] 记录 HEAD、完整工作区状态与内部 remote；用户明确授权当前资产范围并行落地，不接触现有 C++/磁力投掷实现改动。
- [x] 将 `RamHeadMesh` 换为 `SM_HydroLab_VentB1`，按碰撞尺寸对齐；新增无碰撞薄实体金属背盖，避免单面平面从背后被剔除。
- [x] 将摆锤锤体宽度改为 `440 cm`，应用带法线/粗糙度的工业材质；摆杆和顶部支架换成拉丝金属材质。
- [x] 将 `PivotHeight` 从 `610` 调到 `650 cm`，`BobHalfExtents.X` 从 `110` 调到 `220 cm`。
- [x] Blueprint 编译、保存、属性回读、资产非 Dirty 与 PIE 启动/运行 smoke 均通过。
- [x] Level0 旧实例因历史组件覆盖无法稳定继承新模板，已保留位置、名称与标签并用更新后的同一 Blueprint 原地重建；保存后回读与 PIE smoke 通过。
- [x] 修复加宽后外侧命中准备过晚：保持 440x80x150 外观/碰撞不变，将 `MaximumPreparationLeadTime` 从 `0.08s` 调为 `0.16s`；`PreparationLookAheadDistance=850cm` 保持不变。
- [x] 用户要求停止摆锤改造并完整回退：删除 `PawnStopper`，恢复纯 Engine Cube、无材质覆盖、220x80x150 cm、PivotHeight 610 cm、MaximumPreparationLeadTime 0.08s；Level0 原位重建为单一纯 Cube 实例。
- [x] 精确提交并推送当前纯 Cube 的 Blueprint、DataAsset 与 Level0 基线，工蜂远端核验为 `b320f057d50f7587d1baf347b42cb96ab239f4bf` 后才首次修改 C++。
- [x] 未使用 Git 恢复文件；仅在当前 `PendulumHazard.cpp` 中手工恢复 `d819a1c2` 的发送端 Prepare 表面间距、相对闭合速度与帧自适应预测代码。
- [x] 保持 `BobBody` 对 Pawn 的 Overlap、`MaximumPreparationLeadTime=0.08s`、Cube 尺寸和接收端 Heavy 状态机不变；本轮未实现后续分层碰撞方案。
- [x] 完整构建 `DemoEditor Win64 Development` 成功，正式链接 `UnrealEditor-Demo.dll` 并写入 `DemoEditor.target`；源码 diff 仅恢复 Prepare 相关逻辑并保留后来新增的 PCG 初始相位功能。
- [x] 用户 A/B 复测仍穿模；源码确认真实 Hit 后立即调用 `ReleaseCommittedPhysicsBodyContact()`，使角色 Mesh 对摆锤/冲锤共同使用的 `ECC_PhysicsBody` 当帧转为 Ignore。
- [ ] 按用户授权删除真实接触后的立即放行调用和无用辅助函数；不新增显式来源配置，所有 Heavy 统一复用现有 `PhysicsBodyReleaseDelaySeconds=0.15s`。
- [ ] 保持 Prepare、冲量比例、Heavy 状态机、摆锤/冲锤资产和碰撞通道不变；完成构建与 Heavy 自动化后提交供用户复测。

## 验证

- 命令/场景：Blueprint 编译；DataAsset/组件属性回读；L_Game 或隔离测试关卡 PIE。
- 结果：冲锤改造保留；摆锤改造已按用户要求全部撤回。Level0 摆锤当前为纯 Engine Cube、无材质覆盖、220x80x150 cm 碰撞/外观、610 cm 支点、0.08s 原预测窗口，且只有一个实例；Blueprint、DataAsset、Level0 均已保存并非 Dirty。
- Prepare A/B 结果：发送端已恢复为基于接收组件表面间距、角色相对速度和当前帧时长的旧预测算法；首次构建因运行中的编辑器占用 DLL 而只完成编译，用户关闭编辑器后重跑完整构建成功。
- 用户操作（时机、步骤、预期结果）：实现后请从冲锤背面绕行确认无透空，再让玩家站在摆锤侧边和倒地位置确认不可穿过且不会夹住身体。
- 用户验收：待验收

## 阻塞、风险与下一步

- 阻塞：无；当前进入统一 0.15s 接触阻挡的最小修复。
- 风险：`VentB1` 原点位于一侧，视觉必须补 `X` 偏移；背盖法线方向错误会被背面剔除；材质图集在基础 Cube 上可能因 UV 产生不理想分区，需编辑器画面复核。
- 下一步：摆锤和冲锤分别实测首次接触后的 0.15s 阻挡是否消除穿模，同时确认冲锤未恢复持续顶墙。
