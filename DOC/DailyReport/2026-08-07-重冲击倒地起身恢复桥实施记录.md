# 2026-08-07 重冲击倒地起身恢复桥实施记录

## 已完成

- 新增 `UHeavyImpactAnimInstance`，以 Pose Snapshot 保存最终倒地物理姿势，并由 AnimBP 布尔分支平滑交给 `DefaultSlot` 动态起身 Montage。
- 扩展共享 `UHeavyImpactResponseComponent`：正躺/趴倒判断、最终骨盆锚点、0/20/40/60 cm 安全 Capsule 找位、路径 Sweep、空间不足定时重试、Montage 完成/中断/看门狗和 AnimInstance 重初始化安全收尾。
- 将假阳性回滚快照与真实命中恢复基线拆分；真实 Chaos 命中提交后立即丢弃受击前 Actor Transform，恢复路径不会返回受击前位置。
- 玩家和追猎者共用同一恢复状态机；各自 DataAsset 只提供对应骨架的正躺/趴倒动画与调参。
- 使用 UE5.8 官方 MCP 完成并回读玩家项目 AnimBP 副本、追猎者 AnimBP、玩家 AnimClass 与两份 HeavyImpact DataAsset。官方 `ABP_Unarmed` 和四条源 AnimationSequence 均未修改。
- 旧追猎者局部 Physics Control 受击实现保持原样和停用状态，没有删除。

## 验证证据

- 实施基线：`22803de9260afe56dd3877d37921233e280ba59c`，实施前本地/远端一致且工作区干净。
- 完整构建：`DemoEditor Win64 Development -Module=Demo -NoEngineChanges` 成功；未编译或修改 UE5.8 引擎。
- 自动化：`Demo.Physics.HeavyImpact.*` 共 5/5 通过，0 failed、0 skipped、0 warning、0 error。
- 蓝图：玩家/AI 两个 AnimBP 以 warnings-as-errors 编译成功；父类、Target Skeleton、Pose Snapshot、Bool Blend、DefaultSlot 拓扑和玩家 Mesh AnimClass 均由官方 MCP 保存后回读。
- 最终短 PIE：玩家实际实例为 `ABP_Player_Unarmed_HeavyImpact_C`，追猎者实际实例为 `ABP_Pursuer_Locomotion_C`，两者父类均为 `HeavyImpactAnimInstance`；本轮没有 HeavyImpact 初始化、PCA、Skeleton、Slot 或 Montage 错误。
- PIE：无输入运行与两次仅限 PIE 世界的临时 AI 定点都未产生摆锤真实 Chaos 接触，因此没有把 `committed → Downed → recovery completed` 视觉闭环标记为已验证。两次 PIE 均正常停止且没有保存 Level0。

## 遗留与用户验收

- 玩家与 AI 分别测试正躺、趴倒、墙边、墙角、完全堵塞、解除堵塞、倒地再次受撞和起身中断。
- 重点观察 Pose Snapshot 交给 Montage 的第一帧、正反面 yaw、最多 60 cm 找位的滑动感、起身末尾 0.30 秒混回 Idle，以及起身后玩家控制/AI 追逐是否恢复。
- 本轮 PIE 仍记录到机关预测未形成真实接触和极低帧时序警告；它们属于现有机关命中覆盖问题，不作为起身闭环通过证据。
- 第一次为完整构建关闭 Editor 时发生一次 `EditorExit` 阶段的 UnrealEd/Slate 访问冲突；第二次正常关闭成功，未发现与 HeavyImpact 运行代码相关的调用栈。
