# 2026-08-05 Level0 常驻物理摆锤实现记录

## 完成

- 新增 `APendulumHazard` 与 `UPendulumHazardTuningData`，使用 World-to-Body Physics Constraint、Chaos 自由摆、23° 主轴硬角限位、5° 次轴有限摆动和最低点有限补能；没有角度伺服、物体类别门槛、受击逻辑或 PCG 依赖。
- 创建 `/Game/ZeroEscape/Hazards/Physics/BP_PendulumHazard` 与默认 DataAsset；蓝图仅装配原型网格，玩法逻辑全部留在 C++。
- 读取正式 `HighCeilingRoom_Level0_1x2` 配方，在 Level0 空白区 `(52000,30000,0)` 重搭 36 件独立 HydroLab 高厅并放置两盏同款灯；原参考房间不动。
- 摆锤放在新高厅中心 `(52000,30300,0)`、Yaw `90°`，沿 600 cm 宽度横扫；入口保留一个 20 kg 现有磁性箱供人工投掷。

## 验证

- 完整构建：`DemoEditor Win64 Development` 成功，`UnrealEditor-Demo.dll` 正常链接。
- 资产：Blueprint 父类、DataAsset 引用、参数、组件父子关系、无 Tick、无玩法图均经 UE5.8 官方 MCP 回读；Blueprint 编译和精确资产保存成功。
- 房间：36/36 结构件创建成功，联合包围约 `614×1214×758 cm`；代表性地板、顶板、上下墙均为 Static、`BlockAll`、`QueryAndPhysics` 并参与导航。
- 自由摆：补能 0 的中线样本为 `213.57 → 195.12 → 179.25 → 164.71 → 151.37 cm/s`，确认重力释放、约束、中线检测和被动衰减均真实运行。
- 补能：按实测损失将单次上限设为 `20 cm/s`；后续稳定样本主要为当前 `203–209 cm/s`、补 `13–19 cm/s`，接近目标时只补 `4.8–9.1 cm/s`。
- 碰撞：20 kg 磁性箱在最低点被推出约 `76 cm` 并旋转约 `8°`；摆锤当次中线速度降到 `164.81 cm/s`，只恢复 `20 cm/s`，没有立即回到脚本轨迹。
- 空间：18° 目标摆幅下侧墙余量约 69 cm；23° 几何硬限位下约 25 cm；首轮运行没有接触墙面或地面。

## 遗留与验收边界

- 2026-08-05 用户确认 Level0 物理摆锤“算还行”，原型初步验收通过；地牢房型、玩家与追猎者通行、Pawn 受击和 PCG 投放仍未验收，任务继续保持 Active。
- 自动测试没有覆盖 2000 cm/s 高速投掷、30/60/120 FPS 对照或 Pawn 受击联调；若真实高速磁性物穿透，先讨论是否给现有磁性物开启 CCD，不新增隐藏纠偏框架。
- 原型外观暂用引擎基础 Sphere/Cylinder/Cube；最终锤头、摆杆和支架网格需用户选定后再替换。
