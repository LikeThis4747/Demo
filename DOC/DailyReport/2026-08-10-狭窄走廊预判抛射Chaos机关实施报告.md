# 2026-08-10 狭窄走廊预判抛射 Chaos 机关实施报告

## 状态

- 已完成六文件 C++ 重构、默认 DataAsset、两个 Blueprint、Level0 转角测试布局和 Demo-only 正式链接构建。
- 未运行 PIE，也未完成画面可读性、移动目标命中率、玩家躲避、连续反弹或薄墙穿透验收；当前状态是“等待用户运行验证”，不是功能验收完成。
- 类名与资产路径继续保留 `ThrustGuidedHazard` 以避免不必要迁移；运行实现已不再使用推进器或发射后制导。

## 实施结果

### 发射前预测与机械瞄准

- 首个进入 Trigger 的角色被锁定，默认预警 `0.55 s`，以 `0.05 s` Timer 更新目标位置和速度。
- 使用 UE5.8 `UGameplayStatics::SuggestProjectileVelocity` 求固定速度低弧线，并在移动目标预测中同时收敛飞行时间、目标点、真实质心出生点和发射方向。
- Muzzle 只表示炮口出口；`ProjectileSpawnPoint` 表示 Chaos 胶囊质心。弹道求解、出生检查、SpawnTransform 和最终冲量使用同一质心起点。
- 机械限制相对 BeginPlay 缓存的固定中性轴分别约束 Yaw、向上 Pitch 和向下 Pitch；炮管按 `90°/s` 的角速度限制真实转动，发射帧沿实际炮管方向出射。
- 数学无精确解、目标预测未收敛或机械限角时不会静默取消：依次退化为当前位置、Closest、机械限角或固定中性前向方案，并以日志区分来源。

### 单次冲量与全程 Chaos

- 删除弹体原生 Thruster、飞行中目标引用、Powered 控制、姿态转矩和 Actor Tick。
- 弹体在 BeginPlay 完成唯一权威胶囊、质量、重力、阻尼与 CCD 设置后，只调用一次 `AddImpulse(Mass * LaunchVelocity)`。
- 默认固定速度由 `ReferenceRange=600 cm`、`PreferredLaunchAngle=18°` 和世界重力推导，约为 `1000 cm/s`；质量降为 `25 kg`，轨迹速度与碰撞力度可分别继续调试。
- 第一次有效 Blocking Hit 只切换到自由物理阶段、应用碰后阻尼和触发表现事件；不写回 Transform、线速度或角速度。之后的撞墙、撞地、撞道具、反弹和休眠继续由同一 Chaos 刚体处理。
- `bEnableHeavyImpactPreparation=false` 时准备碰撞体始终关闭，不绑定 Overlap、不启动 Timer，也不读取准备窗口字段；首轮只验证弹道和物理表现。

### 出生与堵膛

- 最终质心位置以完整胶囊 Overlap 做权威出生检查；可选短距离 Margin Sweep 不再从 Muzzle 中心扫一个不存在的完整胶囊姿态。
- BeginPlay 已存在的 Launcher/WorldStatic 结构重叠会禁用错误装配；运行时出生体积被堵则消费本发、触发堵膛表现，不生成重叠弹体，也不伪造对堵塞物的直接冲量。
- 弹体视觉网格的碰撞改为 `NoCollision`，唯一权威碰撞体是 `ProjectileBody` 胶囊：半径 `22 cm`、半高 `45 cm`、`QueryAndPhysics`、CCD 开启。

## Blueprint 与 DataAsset 回读

- `/Game/ZeroEscape/Hazards/Physics/BP_ThrustGuidedHazardLauncher`
  - 新组件链包含 `AimPivot -> Muzzle -> ProjectileSpawnPoint`；`BarrelMesh` 已挂到 `AimPivot`。
  - `Muzzle` 相对位置 `(100,0,0)`，质心出生点再向前 `55 cm`。
- `/Game/ZeroEscape/Hazards/Physics/BP_ThrustGuidedHazardProjectile`
  - 不再存在 Thruster；`ExhaustVisualRoot` 直接挂到 `ProjectileBody`。
  - `BodyMesh` 与 `NoseMesh` 为 `NoCollision`，权威胶囊保留 Hit 通知和 CCD。
- `/Game/ZeroEscape/Hazards/Physics/Data/DA_ThrustGuidedHazard_Default`
  - 预警 `0.55 s`，参考射程 `600 cm`，参考角 `18°`，瞄准高度 `50 cm`。
  - Aim 更新 `0.05 s`，Yaw `30°`，Pitch 上/下 `18°/15°`，转速 `90°/s`，回退仰角 `8°`。
  - 出生 Margin `10 cm`，胶囊 `22/45 cm`，质量 `25 kg`，碰后阻尼 `0.08/0.15`，HeavyImpact 准备关闭。
- 两个 Blueprint 以警告视为错误编译通过；两个 Blueprint、DataAsset 与 Level0 保存后均回读为非 Dirty。

## Level0 转角测试布局

- 在低天花板 L 形走廊西端新增南半幅端墙，世界 Bounds 约为 `X 49296.25～49300 / Y 30300～30600 / Z 0～300`；北侧约 300 cm 通路保留，不把走廊封成死路。
- 转角 Launcher 移到 `(49300,30400,150)`、Yaw `0°`，装在端墙上朝东迎击；实例遗留的 `TriggerAnchor.X=1100` 已改为 Local `(600,200,0)`。
- Trigger Box 为 `500/260/140 cm`，世界范围约 `X 49400～50400 / Y 30340～30860 / Z 10～290`。它不会伸到转弯前竖廊，却覆盖北侧 300 cm 通路的完整可通行胶囊中心范围；默认第一次触发位置到质心出生点约 10 m。
- 原出口灯移到 `(49600,30800,300.2)`，其碰撞 Bounds 约为 `Y 30672～30931`，离开默认弹道。
- 300 cm 顶高与 `18°` 机械上限的粗略净空仍只有约 20～24 cm；这是作者布局约束，必须由用户 PIE 实测弹体网格、天花板内表面和反弹表现。

## 构建与静态验证

- UHT 与 Demo `-NoLink` 编译通过。
- 用户关闭 Editor 后完成 `DemoEditor Win64 Development` 正式链接；`Binaries/Win64/UnrealEditor-Demo.dll` 于 2026-08-10 18:58:09 更新。只构建 Demo 项目模块，没有重新编译 UE5.8 引擎。
- `git diff --check` 通过；六文件只有一处权威 `AddImpulse`，无 Thruster 组件、ProjectileMovement、Powered 阶段或飞行中目标控制残留。
- 两轮独立静态复核未发现剩余 blocker/high。此前发现的 Trigger 初始重叠时序、固定中性轴限角插值、Deferred Spawn Scale、Blueprint 回调重入、非零弹道阻尼和二次根稳定性均已修正。
- 当前 Editor 日志未发现本机关 Blueprint 编译 Warning/Error。启动期缺少分析器 DLL、Android SDK、UnifiedError 自测输出和一次旧 MCP Session 错误均与本机关实现无关。

## 待用户 PIE 验收

1. 转角后是否先看见预警、炮管和实体弹体，再形成命中或擦身而过的结果。
2. 默认 9～11 m 触发后继续迎面移动时是否有较高命中可能；预警期急停、后退、横移是否确实能躲。
3. 600～700 cm 近距静止专项是否命中，确认真实质心起点没有固定偏移。
4. 下游物理道具能否真实阻挡或改变弹体路线；首碰后是否只剩 Chaos 反弹与休眠。
5. 连续发射薄墙专项是否仍有穿透；CCD 只能降低风险，未实测前不能写成已解决。
6. 25 kg、约 10 m/s 的碰撞力度是否适合轻受击或普通物理推动；第一轮不以重受击门槛倒逼速度。

最终 Git 提交哈希以本轮推送回报为准。
