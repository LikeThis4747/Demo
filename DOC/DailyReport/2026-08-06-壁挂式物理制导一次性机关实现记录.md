# 2026-08-06 壁挂式物理制导一次性机关实现记录

## 完成

- 新增 Launcher、Projectile 与调参 DataAsset 共六个 C++ 文件，完成一次性触发、预警、真实 Thruster 推进、小角度制导、首碰失导和推进结束后纯 Chaos 运动。
- 新增两个 Blueprint、默认调参 DataAsset、物理材质和简易机身材质；Blueprint 只负责组件与资源装配，EventGraph 为空。
- 在 Level0 的 `PCG_Test/Room900` 中放置正面墙与拐角墙两个测试实例，归入 `Hazards/Physics/ThrustGuidedHazardTest`。
- Review 按用户决定取舍：V1 保持独立；采纳准备请求结构校验和轴约定；HeavyImpact 预测公共层留待三个机关稳定后再统一评估。

## 验证

- C++ 无链接编译与正式 Demo 模块 DLL 链接均通过。
- 官方 UE MCP 回读父类、空 EventGraph、CDO 引用、组件、DataAsset 与物理材质配置；两个 Blueprint 以警告视为错误编译通过。
- 两个 Muzzle 前 200 cm 无阻挡，完整发射路线分别约 730/760 cm 后命中房间远端墙。
- 正常关闭 UE 编辑器后，只执行带 `-Module=Demo -NoEngineChanges` 的 `DemoEditor Win64 Development` 构建；结果成功、目标已最新、0 个编译动作，没有编译引擎。
- 局部材质编译辅助工具返回 0 error/0 warning，但产生一次 handled ensure；后续官方资产验证与最终 Demo 构建通过。

## 遗留

- 按用户要求未运行 PIE。玩家/追猎者触发、命中率、首碰失导、多次反弹、视觉反馈和手感由用户次日验收。
- 当前不接 PCG Population；后续需先形成能表达墙面朝向、触发区和发射净空的生成契约。
- 功能稳定后再评估是否把三个物理机关重复的 HeavyImpact 预测流程抽为公共层。

## 交付

- 本记录与功能代码、资产、Level0 修改和 Review 归档由同一次完整项目提交承载；提交后核验内部工蜂远端分支与本地一致。
