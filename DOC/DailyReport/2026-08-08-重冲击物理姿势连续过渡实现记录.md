# 2026-08-08 重冲击物理姿势连续过渡实现记录

## 已完成

- 在现有 HeavyImpact 状态机内部加入 `PreparingPose` 恢复阶段，不扩张公开状态，也不改变机关冲量合同。
- `UHeavyImpactAnimInstance` 新增准备姿势 Snapshot、起身 Sequence、显式采样时间和 Alpha；所有引用在失稳、重试、二次受撞、完成、AnimInstance 重建与 EndPlay 路径中幂等清理。
- `UHeavyImpactResponseComponent` 在落地减速后恢复后台 AnimGraph 求值，同时保持全身刚体模拟权重为 1；只逐步提高 ParentSpace 角向强度和最大扭矩，阻尼保持 Landing 配置，线性驱动仍为零。
- 最终交接从与 Sequence Evaluator 相同的动画时间启动动态 Montage；看门狗按剩余动画长度计算。
- 玩家与 AI AnimBP 均接入“物理快照 → 起身首帧 → 显式 Alpha → 布尔门”分支，并保留现有最终 Downed Snapshot 混合。
- 两份 HeavyImpact DataAsset 配置准备时间 `0.40 s`、初始控制比例 `0.30`、正/反面采样时间 `0.0 s`。

## 验证证据

- 实施基线：`df833aa896624d5d3a0436b7cc1d59f520f0871b`；实施前本地/远端一致且工作区干净。
- C++ 首轮构建：`DemoEditor Win64 Development -Module=Demo -NoEngineChanges` 成功，8 actions；未编译或修改 UE5.8 引擎。
- 关闭 Editor 后最终构建：同一 Demo 模块命令成功，Target is up to date、0 actions；编辑器保持关闭。
- 自动化：`Demo.Physics.HeavyImpact.*` 5/5 通过，0 failed、0 skipped、0 warning、0 error。
- 官方 MCP：两个 AnimBP warnings-as-errors 编译成功并保存；准备 Snapshot 名称、Sequence/Time/Alpha 连接、布尔门和现有最终快照链均逐节点回读正确。
- 两份 DataAsset 四个新增值保存后回读一致；四条起身动画、玩家 BP、官方 `ABP_Unarmed`、Level0 与机关未修改。
- 短 PIE 无 HeavyImpact 初始化、PCA、骨架、Slot 或 Montage 新错误。临时玩家定点只触发既有预测过迟和假阳性回滚，没有形成真实 Chaos 命中，因此未将视觉闭环标为通过；PIE 已停止且未保存关卡。

## 待用户验收

- 是否能在落地减速时看到肩、脊柱、髋或腿逐渐收拢，而不是先像雕像一样静止。
- Montage 开始时是否消除整个人闪切，同时保持真实击飞距离和最终倒地点。
- 墙边肢体是否仍服从碰撞；墙角无空间时是否持续躺倒并在空间恢复后重试。
- 准备中再次受撞是否回到物理；起身后玩家控制、相机和 AI 追逐是否恢复。

首轮只调整 `RecoveryPosePreparationSeconds`、`RecoveryPoseInitialControlScale` 和正/反面采样时间；不同时改冲击力、机关速度、PCA 拓扑或动画资源。
