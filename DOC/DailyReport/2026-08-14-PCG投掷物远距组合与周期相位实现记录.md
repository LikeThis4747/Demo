# 2026-08-14 PCG 投掷物远距组合与周期相位实现记录

## 已完成

- 发射器占用从“自身格 + 正前方格”收敛为仅自身锚点格；正前方仍保留开放方向、出生点/追猎者/Exit 排除和运行时弹体净空检查。
- 新增发射器远距组合软评分：通行图距离 1 不加分、2 加 `0.75`、3 加 `1.5`；没有搭档不扣合法性、不触发回滚。
- 正式发射器权重由 `2/3/5` 调为 Easy/Normal/Hard `3/4/6`；转角奖励 `1.5`，组合压力最多 `0.5`。
- 周期相位由公开 Seed、机关类型和锚点确定性派生；同组只做有限候选择优，未设置最低相位间隔或失败分支。
- 摆锤、地刺、冲锤和刺轮均在延迟生成的 `BeginPlay` 前接收相位；玩家触发型发射器不伪造周期。

## 代码规模与范围

- 生产 C++：10 个既有文件，`+354/-46`。
  - Population 摆位/评分与内部结果：`+155/-11`。
  - 运行时生成装配：`+67/-19`。
  - 摆锤、地刺、冲锤三类既有 Actor：`+132/-16`。
- 自动化测试：1 个既有文件，`+202/-6`；其中包含蛇形转角夹具、64 Seed 组合/相位统计和同 Seed 重放。
- 资产：仅修改既有 `DA_Population_Default.uasset`；没有新增资产或代码文件。
- 未触碰投掷物 Actor、弹道、伤害、WFC、公开 Seed、Heavy 或 TASK-20260814-006。

## 验证

- `DemoEditor Win64 Development -Module=Demo -NoEngineChanges`：最终构建成功。
- `Demo.PCG.Population`：`15/15 Success`，0 warning，0 error。
- `Demo.PCG.Runtime.NavigationOperationGate`：`1/1 Success`。
- `BP_PendulumHazard`、`BP_SpikeTrap`、`BP_BatteringRamHazard`、`BP_SpikeWheelHazard`、`BP_GameplayPopulator`：warning-as-error 编译通过，均未产生额外资产脏改。
- 正式资产回读：发射器权重 `3/4/6`；机关密度 `26/28/30`、资源密度 `12/12/12`、机关/资源空白路线奖励 `1.5/1.25` 均保持用户现值。
- L_Game、Seed `12345` 两次运行均得到布局哈希 `3542287860646786879`、机关 `45/45`、发射器 `17`、资源 `19/19`；首个摆锤相位两次均为 `0.592642`，日志中出现发射器组合 `0.75/1.5`，无 Population Failure 或相位回退。

## 待用户验收

- 投掷物出现量是否合适，转角与 2～3 格远距组合是否形成可读配合。
- 同一区域周期机关是否明显摆脱同拍，同时仍保留偶发近似同拍的自然变化。
- 地刺/冲锤第一轮启动偏移与摆锤初始相位的视觉节奏是否需要只调数值。
