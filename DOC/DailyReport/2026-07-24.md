# 2026-07-24 工作日报

## 今日结论

- Demo 由 **UE 5.7.4 升级到 5.8**（源码引擎 `d:/UE5_8`）并编译通过。实际改动远小于预检预期：只有构建设置版本号、`FJsonObject` 键类型两类共约 7 处，磁力与自写 PCG 零改动即过。
- 在 Demo 上启用了 **UE5.8 官方 MCP**（`ModelContextProtocol` + 5 个 toolset），与既有本地 `ue-editor-mcp` **双 MCP 共存**，端口/协议独立、互不干扰。
- 实测摸清了官方 MCP 的能力边界（tool-search 模式、23 toolset），产出并归档了**双 MCP 协同使用规范**，供后续所有 AI 判断"什么任务用哪个"。
- 全局 `mcp.json` 已配好官方 server，重启 CodeBuddy 后原生工具通路生效——AI 现在可直接调用两个 MCP。

## 已完成

### 引擎升级（5.7.4 → 5.8）
- 升级前冻结：Git tag `v5.7.4-final` + 物理副本 `Demo_UE5.7.4_backup`（副本永远关联 5.7.4，不被 5.8 触碰）。
- `DemoEditor Win64 Development` 在 5.8 工具链下 `Result: Succeeded`。实际改动仅两类：
  - 两个 `Target.cs` 的 `DefaultBuildSettings` `V6 → V7`（过配置门槛必需项；`IncludeOrderVersion` 暂留 `Unreal5_7`）。
  - 5.8 把 `FJsonObject` 的 key 从 `FString` 改为 `UE::FSharedString`——共 5 处统一 `Pair.Key → *Pair.Key`（`GraphActions` 1、`MaterialActions` 2、`NodeActions` 2）。
- 追加清除 `UMGActions.cpp:311` 的 C4996 弃用警告：移除 5.8 已无意义的 `REN_ForceNoResetLoaders` 标志，重编零警告。
- 预检里最担心的 K2Node/Kismet/BlueprintGraph 大面 API 破坏**未兑现**（这些 5.7→5.8 稳定）；磁力全 API 已核对、自写 PCG 不依赖官方 PCG 模块，均零改动通过。

### 官方 MCP 启用与实测
- `.uproject` 启用 `ModelContextProtocol` + `EditorToolset`/`PhysicsToolsets`/`UMGToolSet`/`AutomationTestToolset`/`SemanticSearchToolset`（`ToolsetRegistry`/`SemanticSearch` 自动依赖）。编译发现这些 Experimental 插件 dll 引擎源码构建时已编好，**零编译成本**。
- 项目 `Config/DefaultEditorPerProjectUserSettings.ini` 设 `bAutoStartServer=True`，开编辑器即在 8000 端口自动起 server；全局 `mcp.json` 加 `ue58-official-mcp` → `http://localhost:8000/mcp`。
- 用 HTTP 直连走完整 MCP 协议实测：官方开 `bEnableToolSearch=true`，`tools/list` 只暴露 `list_toolsets`/`describe_toolset`/`call_tool` 三个入口；`list_toolsets` 返回 **23 个 toolset**（顶层 C++ 7 个 + `EditorToolset` 内 16 个 Python toolset）。
- 关键修正：官方 `BlueprintTools` **也有完整逐节点建图+连线**（`add_node`/`connect_pins`/`set_node_pin_default`…），推翻了此前"蓝图微操是 ue-editor-mcp 独有"的假设。

### 规范与文档
- 归档 `MCP/capability-notes/dual-mcp-usage-guide.md`（定位、能力矩阵、决策规则、坑与实测来源）+ 两份实测数据附录 `mcp_capability_overview.md`、`mcp_describe_blueprint.md`。
- 更新 `MCP/capability-notes/README.md` 索引，纳入双 MCP 背景与三份新文档。
- `AGENTS.md` 加入双 MCP 协同指引与导航条目；`CLAUDE.md`、`DOC/`、`memory-bank/techContext.md`、`projectbrief.md` 等文档的当前环境版本统一改为 5.8（历史/升级过程记录保留 5.7.4 表述）。

## 验证证据

- 编译日志 `Result: Succeeded`、无警告，生成 `UnrealEditor-UEEditorMCP.dll`（构建临时日志已清理）。
- 官方 MCP `initialize` 握手 `STATUS:200`（protocol `2025-06-18`）；`list_toolsets` 实调返回 23 toolset 完整清单。
- 重启 CodeBuddy 后 `ue58-official-mcp` 出现在原生工具列表，实调 `list_toolsets` 成功——路径 B 生效。

## 未完成与风险

- **磁力手感 5.8 实测未做**：Chaos 内核可能有细微差异，需用同一 `MagneticGrabTuningData` 在 PIE 对比 5.7.4 抓取/吸取/投掷手感。
- **`Demo.PCG` 自动化 5.8 未重跑**：5.7.4 下 13/13，升级后应重跑确认；`-NullRHI` viewport 整数除零是否在 5.8 复现也待验。
- `IncludeOrderVersion` 仍是 `Unreal5_7`（backward-compatible），彻底对齐 5.8 可升 `Unreal5_8`，但可能引入头文件顺序错误，待其余验证稳定后再做。
- 官方 MCP 是 Experimental（`NoRedist`、API 可能再变），定位为辅助工具，不进核心不可替代工作流。
- 官方 MCP HTTP/SSE 使用坑：同一 session 连续多请求可能中途中断，批量枚举时每请求用独立 session 最稳（已写入规范）。

## 下一步优先级

1. 在正常视口重跑 `Demo.PCG` 自动化，确认 5.8 下仍 13/13。
2. 磁力关卡 PIE 手感对比 5.7.4。
3. 需要时按 `dual-mcp-usage-guide.md` 决策规则，用官方 MCP 补齐 ue-editor-mcp 空白能力（语义搜索、自动化测试、物理资产等）。
4. PCG 大改（效果不对）另开分支进行，与升级解耦，避免混淆问题来源。

## AI 使用与边界

- 本轮 AI 完成引擎升级编译修复、官方 MCP 启用与实测、双 MCP 规范产出与文档统一；升级决策、副本冻结与编辑器重开由用户操作确认。
- 编译成功与协议握手只是工程检查点；磁力手感与 PCG 自动化的运行时验收尚未完成，不据此声称"升级已全部验收通过"。

**状态：Demo 已在 5.8 编译通过，双 MCP 平台就位并出规范；磁力手感与 PCG 自动化的 5.8 运行时验收待做。**

## 追加：PCG Grid-WFC V3.2 实施（跨至 2026-07-25 凌晨）

### 已完成

- 在修改前提交并推送内部工蜂快照：`667c49e39e3d70970a398d04f61bab6df20f004d`，远程为 `git@git.woa.com:shiqiqiwang/Demo.git`。
- 将旧 Graph/Socket/Portal/A*/带回溯 WFC/Catalog 链原子替换为纯值 Grid-WFC：600 cm 逻辑 Tile、完整 16 种 Opening Mask、加权最小熵与确定性 N/E/S/W 传播。
- 删除旧 `ZeroEscapeLayoutSolver`、旧测试与死配置，不保留转发壳；保留并简化 Seed、Difficulty、Flow、K-of-N、确定性签名和 Runtime HISM 职责。
- 必要路线改为构造性中央主干与 2×2 Objective 房双入口短回路；可选第三入口不承担可解性。该结构保证合法 Profile 不依赖幸运 Seed，也无需 A*、WFC 回溯、重试或降级。
- 创建项目自有 `/Game/ZeroEscape/Generation/Presentation/DA_Presentation_SciFiHydroLab`，以测得的固定 Pivot 修正直接绑定 HydroLab 的 Floor/Ceiling/Wall/WallTopTrim/Pillar；未修改第三方 Mesh。

### 验证

- UE 5.8 `DemoEditor Win64 Development` 完整构建成功。
- 新 `Demo.PCG` 13/13 通过；Seed Sweep 覆盖 3 难度 × 3 Flow × 32 Seed，共 288/288 成功。补齐 HydroLab Pivot 旋转断言后的最终报告：`Saved/Automation/PCG-V32-FinalAudit-20260725-0050`。
- Generator Blueprint 编译为 UpToDate，0 error / 0 warning。
- `/Game/Levels/L_PCG_RuntimeTest` 正常 NewWindow PIE 成功：Seed 12345、Normal、EscapeOnly 生成 27 个逻辑 Cell、444 个结构实例和 5 个 HISM 组；Harness 成功将玩家从 Staging 传送到生成区。
- 自动化与 PIE 证明运行时生成链、确定性和 Harness 传送可用；尚不代表正常材质效果、接缝、碰撞、净空与玩家 Start→Exit 已验收。

### 遗留与直接方案

- PIE 发现 `MI_HydroLab_Ceiling01`、`MI_HydroLab_Wall01`、`MI_HydroLab_Trim01`、`MI_HydroLab_Floor03` 缺少 Instanced Static Mesh Usage，运行时会回退为默认材质。
- 四个实例共同继承 `/Game/SciFiHydroLab/Materials/Parents/M_HydroLab`。最小直接方案是只在这个根材质启用并保存 `Used with Instanced Static Meshes`；因其属于第三方素材，必须先取得用户许可，不建立材质副本、映射或运行时绕过。
- 获准修复后需要重跑 NewWindow PIE，并由用户完成视觉、碰撞、净空和 Start→Exit 实际走通。旧 `DA_LevelModuleCatalog` 资产暂保留到用户验收后再讨论删除。
