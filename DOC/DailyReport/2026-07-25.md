# 2026-07-25 工作日报

## PCG V3.2 材质门禁关闭

- 用户授权直接修改 HydroLab 共同根材质 `/Game/SciFiHydroLab/Materials/Parents/M_HydroLab`。
- 仅启用并保存 `Used with Instanced Static Meshes`；四个材质实例无需逐个修改，未创建材质副本、映射、Runtime 绕过或永久作者化脚本。
- 一次性 UE 编辑器脚本回读 `before=0 → after=1`、`saved=1`，随后已删除。首次尝试因 UE 5.8 Python 不暴露 `post_edit_change()` 而在保存前失败，确认资产未改后移除该非必要调用，第二次成功。

## 验证

- 全新正常渲染编辑器进程打开 `/Game/Levels/L_PCG_RuntimeTest`，NewWindow PIE 成功运行。
- `ZE_PCG_RESULT`：Seed 12345、Normal、EscapeOnly，27 Cells、444 Instances、5 HISM，`success=1`。
- Harness Teleport 与 TransferToGeneratedStart 均成功。
- 本轮日志不再出现 `MI_HydroLab_*`、`InstancedStaticMeshes`、Usage Flag 或 Default Material 相关警告。
- 当前 PIE 窗口保持运行，等待用户检查材质观感、接缝、碰撞、净空和 Start→Exit 实际走通；在此之前不标记 PCG 玩家验收完成。

## 工作方式决定

- 对“已确认必要、只改一项且可回退”的同类素材兼容设置，后续可直接处理并在完成后通知用户。
- 该授权不自动扩展到改变外观、几何、碰撞语义或批量修改第三方素材；这些仍需先说明影响。

## PCG V4 全图 Grid-WFC 路线重构

- 重新暂存当前工作区并创建实施前快照 `b8a8c6e30b5f5e7c73c382e64c62c0e4d1706e78`，推送到内部工蜂 `origin/main` 后核验本地/远端一致。
- 读取并落实最新代码审计：删除固定中央主干、Objective Gate、Optional Envelope 与事后孤岛剪枝；普通 Grid 全部进入 16 OpeningMask Simple-Tiled WFC。
- 实现最低带权 Shannon 熵观察、加权无放回候选顺序、Domain Trail 与有界 chronological backtracking。
- 实现 Count、MaxConsecutive 与 Connected 三项纯值约束；Connected 使用每格“中心 + N/E/S/W”五节点展开图和迭代 Tarjan 关节点传播。
- 完整候选由 Grid 做唯一产品层验收；路线超限可拒绝当前叶并继续同一决策栈回溯。`NoValidWfcSolution` 表示完整树已穷尽并立即停止；仅 `SolverBudgetExhausted` 使用同请求 Seed 派生的下一个确定性子流。
- 将真实 `/Game/ZeroEscape/Generation/Data/DA_LevelGenerationProfile` 迁移到 V4：24×16 Grid、48–72 非空格、最大连续贯通 4、路线 64+24、整局候选/回溯预算 100000/25000、最多 10 次确定性尝试，三档权重均为 12000/100/100/80/25/5。迁移后由第二个全新 UE 进程从磁盘逐字段复核。
- 测试按职责拆为 Contract 与 WFC/Layout 两个 CPP，新增低预算多尝试、根固定点、根完成叶、活动决策叶、展开图 Connected、真实资产精确字段等边界夹具。

## V4 验证与遗留

- UE 5.8 `DemoEditor Win64 Development` 最终构建成功。
- `Demo.PCG` 共 19 项，19/19 成功；真实 HydroLab Pipeline Smoke 同时验证 V4 Profile、预算、路线和三档完整权重。
- 3 难度 × 3 Flow × 32 Seed 共 288/288 成功：Solve Attempts P50/P95/Max=1/3/7，Candidate Attempts=341/5954/17193，Backtracks=3/5005/15009，Planning=23.145/233.470/622.386 ms。
- 正常编辑器直接打开 `/Game/Levels/L_PCG_RuntimeTest`，显式以 `SelectedViewport`（非 NewWindow、非 Simulate）运行；`ZE_PCG_RESULT schema=4 success=1`，48 Cells、798 Instances、5 HISM，Harness 传送成功，三项日志断言全部通过，随后正常停止 PIE。
- 独立终审结论为“无代码阻断”。当前仍不能代替玩家验收：需在主视口抽查至少 10 个 Seed，检查路线观感、长直路、T/十字/转弯、碰撞、净空、接缝、导航与 Start→Exit 实际走通；室内灯继续作为独立增量评估。
- 本轮实施代码尚未创建第二个提交或推送；等待用户验收和下一步指令。测试 Harness/测试资产仍保留，未来合并到正式 GameFlow 或删除必须另行确认。
