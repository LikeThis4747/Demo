# 2026-08-05 重冲击物理受击实现记录

## 完成

- 在实施前将完整工作区基线 `1c50616` 推送到内部工蜂并核验远端一致。
- 实现玩家/追猎者共享的重冲击请求、接收接口、角色专属 Tuning、Physics Control 权威状态组件与自动化测试源码。
- 玩家接入身体输入门控和真实命中后的磁力中断；追猎者复用现有 PhysicsControl，并保留旧局部受击全部源码/调参/回调，只暂停三处运行装配。
- 摆锤新增独立 Query-only 预测球，复用现有 60 Hz Timer 计算相对接近速度和 ETA；真实动量仍只来自 Bob 与模拟 Mesh 的 Chaos 接触。
- 预测距离、最低重冲击接近速度、预计接收者最大速度和最大发送 ETA 已纳入现有摆锤 Tuning DataAsset，不硬编码到角色或 PCG。默认预测体积按玩家/AI 迎面移动和 0.5 秒低帧率窗口校验。

## 验证

- `git diff --check` 通过，当前没有 `Content/**` 或 `Config/**` 改动。
- UHT 与本增量所有游戏模块源码、自动化测试源码编译通过；`-Module=Demo -NoLink -NoEngineChanges` 多轮增量编译全部成功，最终一轮为 4 个动作。
- 首次链接发现的 `FBodyInstance` 非导出内联依赖已修正；随后没有新的代码或 UHT 错误。
- 完整 DLL 链接仍未完成：运行中的 Editor 保持 Live Coding Session 并锁定 `UnrealEditor-Demo.dll`；已有引擎 OIT 改动还令普通全目标构建尝试重链同样被锁定的 `UnrealEditor-NetCore.dll`。
- 双 MCP 均已只读确认没有创建并 Compile `UPhysicsControlAsset` 的入口；没有新增 Editor-only 或运行时绕过代码。

## 遗留与验收边界

- 用户需先安全关闭 Editor，让后续完整链接不覆盖未保存的编辑器状态；本任务不会杀进程、使用 Live Coding 或临时 DLL。
- Editor 重启后需由用户人工创建两份 PCA，并最终点击各自的 Compile；其余 DA/CDO/Blueprint 装配优先由官方 MCP 完成。
- 自动化、Blueprint 编译/保存、PIE、玩家与 AI 的画面效果和墙边/斜坡/角落边界均尚未验证，本记录不把代码编译等同于功能完成。
- 玩家首版在真实倒地点进入 Downed，尚未接起身动画；用户提供动画后再做当前落点的恢复桥。
- 详细文件、PCA 配置与恢复步骤见 `claude/docs/2026-08-05-重冲击物理受击实施记录与人工待办.md`。
