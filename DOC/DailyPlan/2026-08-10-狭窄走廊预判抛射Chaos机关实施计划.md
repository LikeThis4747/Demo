# 2026-08-10 狭窄走廊预判抛射 Chaos 机关实施计划

> 状态：用户已验收当前机关基础机制版本。当前权威调参为 `ReferenceRange=900 cm / PreferredLaunchAngle=18° / ProjectileMassKilograms=50 kg / ProjectileLifetimeSeconds=8 s`。2026-08-11 用户另行授权第一版外观试装：只修改两个机关 Blueprint，用 UE 基础几何、SFCorridors 现成材质与 `SM_barrel3` 改善机械支撑和投射物轮廓；不改 C++、DataAsset、Level0 或轻受击。基础机制验收版提交为 `f7ae4f44f427b562ec87cfd4a0ce9a109a873747`。

## 1. 目标

把现有“短时推进、飞行中制导”的一次性壁挂机关改成：

1. 角色进入触发区后锁定首个目标并预警。
2. 发射器在预警期预测移动交点并机械转动炮管。
3. 发射瞬间从弹体真实质心施加一次冲量。
4. 离膛后不再追踪、推进或纠偏，始终由 Chaos 处理重力、碰撞、反弹和休眠。
5. 默认正面路线可读且有较高命中可能；预警期急停、后退、横移和下游物理资源阻挡均为有效应对。

第一轮关闭 HeavyImpact 准备，只验收弹道、可读性和 Chaos；通过后再独立决定轻/重受击。

## 2. 文件范围

### C++

- `D:\UE5projects\Demo\Source\Demo\Public\Data\Hazards\ThrustGuidedHazardTuningData.h`
- `D:\UE5projects\Demo\Source\Demo\Private\Data\Hazards\ThrustGuidedHazardTuningData.cpp`
- `D:\UE5projects\Demo\Source\Demo\Public\Actors\Hazards\ThrustGuidedHazardLauncher.h`
- `D:\UE5projects\Demo\Source\Demo\Private\Actors\Hazards\ThrustGuidedHazardLauncher.cpp`
- `D:\UE5projects\Demo\Source\Demo\Public\Actors\Hazards\ThrustGuidedHazardProjectile.h`
- `D:\UE5projects\Demo\Source\Demo\Private\Actors\Hazards\ThrustGuidedHazardProjectile.cpp`

### UE 资产

- `/Game/ZeroEscape/Hazards/Physics/BP_ThrustGuidedHazardLauncher`
- `/Game/ZeroEscape/Hazards/Physics/BP_ThrustGuidedHazardProjectile`
- `/Game/ZeroEscape/Hazards/Physics/Data/DA_ThrustGuidedHazard_Default`
- `/Game/Levels/Level0`：仅移动/旋转本机关转角 Launcher 与 Trigger，并保持磁力碎裂和其他房间改动

### 2026-08-11 外观试装增量

- `/Game/ZeroEscape/Hazards/Physics/BP_ThrustGuidedHazardLauncher`
  - 基础几何固定墙板、轴承和随动 U 形托架；视觉组件一律 `NoCollision`。
  - 优先直接引用 `/Game/Assets/SFCorridors/Materials/MI_MetalChrome` 与现有预警发光材质，不修改第三方资产。
- `/Game/ZeroEscape/Hazards/Physics/BP_ThrustGuidedHazardProjectile`
  - `BodyMesh` 使用 `/Game/Assets/SFCorridors/Meshes/SM_barrel3` 及其 `/Game/Assets/SFCorridors/Materials/MI_barrel3`。
  - 实施值为 `0.78` 等比缩放、Relative Z=`-40.04 cm`，按网格底部附近的原点修正到权威胶囊中心；隐藏旧 `NoseMesh`，保持视觉网格 `NoCollision`。
- 本增量不修改本节上方历史列出的 C++、DataAsset 和 Level0；不新增发射时假弹显隐逻辑。

### 记录

- `D:\UE5projects\Demo\claude\tasks\active\TASK-20260806-001-物理制导一次性机关实现规划.md`
- `D:\UE5projects\Demo\DOC\DailyReport\2026-08-10-狭窄走廊预判抛射Chaos机关实施报告.md`（实施后）
- 项目记忆仅在实施与验证结果稳定后通过 Memory MCP 更新

## 3. 并行边界

- 用户正手工制作 `GC_MagneticCrate4_P0`，可能保存 `Level0.umap`；磁力 C++、Blueprint 运行逻辑和材质不在本任务范围。
- 本任务先完成六个机关 C++ 文件和 Demo 构建，再处理机关 Blueprint/DataAsset。
- Level0 装配最后执行；保存前重新回读当前 Level0 和磁力资产状态，只改两个机关实例，禁止覆盖磁力碎裂 Actor/资产。
- 轻受击方案文档、Review、任务卡和未来代码均不属于本任务。

## 4. 实施步骤

1. 将当前全部已确认改动形成完整基线 commit，核对内部 origin，push 并确认远端 main 包含该提交；提交后状态必须干净。
2. 完整重写 Tuning DataAsset C++ 字段与 `IsConfigured()`。
3. 重写 Launcher：固定中性炮轴、真实质心起点、最多八次收敛、独立 Yaw/Pitch、机械转速、同步发射采样、结构化出生检查。
4. 重写 Projectile：删除 Thruster/Tick/目标/Powered 控制；同一刚体一次质心冲量；首碰只切阶段和阻尼。
5. 第一轮关闭 HeavyImpact 准备；保留的开启路径改成带重力预测。
6. 执行 Demo 模块 `-NoLink` 编译；只解决本任务引入的错误。
7. 正常关闭 UE Editor，只完整链接 Demo 模块，不编译 UE 引擎。
8. 重启 Editor，优先使用 UE5.8 官方 MCP 更新/回读 Launcher、Projectile 和 DataAsset；Blueprint 以警告视为错误编译保存。
9. 在重新回读 Level0 后，把转角 Launcher 改到玩家转弯后正面尽头墙，按真实玩家胶囊首次 BeginOverlap 位置校正 Trigger。
10. 不代替用户执行最终手感 PIE；记录构建、资产回读和当时仍待用户验收的边界。
11. 形成完整最终提交并推送内部工蜂，核验远端；不得夹带仍在编辑且未确认稳定的磁力资产快照。
12. 用户 PIE 后续：将 `ReferenceRange` 从 `600 cm` 调至 `900 cm`，把推导初速从约 `1000 cm/s` 提高到约 `1225 cm/s`。
13. 将 `ProjectileMassKilograms` 从 `25 kg` 调至 `50 kg`；质量只改变动量和接触，不改变冻结弹道速度。
14. 新增 `ProjectileLifetimeSeconds=8 s`，弹体进入 `Ballistic` 后使用 Actor LifeSpan 定时清除，不启用 Tick。
15. 重新执行 Demo 模块构建、DataAsset 回读与工作区范围检查；用户随后已验收当前基础机制，剩余边界作为后续专项复核。
16. 2026-08-11 外观试装先回读两个 Blueprint、`SM_barrel3` Bounds 和 SFCorridors 材质，再更新任务卡并建立完整 Git 基线。
17. 仅在 Launcher Blueprint 增加纯视觉固定底座、轴承和随动 U 形托架；保留原生 `AimPivot/Muzzle/ProjectileSpawnPoint` 和碰撞合同。
18. 仅在 Projectile Blueprint 把视觉主体替换为居中的 `SM_barrel3`，隐藏旧圆锥鼻部，保持唯一权威胶囊。
19. 两个 Blueprint 以 Warning 视为 Error 编译、保存和逐字段回读；由用户在 Level0 决定外观与机械转动是否通过。
20. 2026-08-11 已完成资产试装、编辑器重启后的 Level0 实例回读和一次临时发射验证；Level0 未纳入改动，最终画面仍待用户验收。

## 5. 首轮参数

- `ReferenceRange=600 cm`，`PreferredLaunchAngle=18°`，按 World 重力推导约 `1000 cm/s`。
- `TargetAimHeightOffset=50 cm`。
- `WarningSeconds=0.55 s`，`AimUpdateIntervalSeconds=0.05 s`。
- `MaximumAimYaw=30°`，`PitchUp=18°`，`PitchDown=15°`，`AimTurnSpeed=90°/s`。
- 胶囊半径/半高 `22/45 cm`，质量 `25 kg`，出生 Margin `10 cm`。
- `bEnableHeavyImpactPreparation=false`。

### 已完成的用户 PIE 后续调参

- `ReferenceRange=900 cm`，`PreferredLaunchAngle=18°` 保持不变，按默认世界重力推导约 `1225 cm/s`。
- `ProjectileMassKilograms=50 kg`。
- `ProjectileLifetimeSeconds=8 s`；从正式发射开始计时，到期整体销毁弹体 Actor。
- 首轮不同时修改抛射角或阻尼，避免无法归因“更快、更重”分别带来的手感变化。

## 6. 验证

### 静态与构建

- 六文件不再引用 Thruster、Projectile Tick、飞行中目标、Powered 状态或姿态转矩。
- `IsConfigured()` 覆盖全部新字段和关系。
- Demo `-NoLink` 与 Demo-only 完整链接成功；不编译 UE 引擎。
- Blueprint 父类、组件树、编译状态和 DataAsset 字段逐项回读。
- Level0 只修改目标机关实例；磁力碎裂资产与 Actor 保持原样。

### 基础验收后的专项复核

- 近距静止目标不再有固定质心起点偏差。
- 9～11 m 触发后继续前进时能看见预警、实体和尾迹，并有较高命中可能。
- 预警期急停、后退、横移可以躲。
- 转角取得炮口视线后才触发。
- 下游物理资源由真实弹体撞击。
- 墙、顶、地面和后续接触始终由同一 Chaos 刚体处理。
- 薄墙连续多发专项决定 CCD 是否仍需后续调整。

## 7. 风险与停止条件

- 当前磁力工作和本任务共享 Level0；若用户正在保存或 Level0 在机关装配前再次改变，先回读合并，不覆盖。
- 官方 MCP 连接失败时立即报告，不静默改用本地 MCP。
- Blueprint/DA 字段迁移导致资产无法安全编译或回读时暂停，不用本地 Python 绕过。
- CCD 不是绝对穿透保证；没有 PIE 证据不得写成已解决。
- 任何新出现且归属不明的脏文件都会阻止最终提交，先协调 Owner。
- SFCorridors 的贴图按原网格 UV 制作，在 UE 基础几何上可能出现拉伸；第一版优先使用较均匀的 `MI_MetalChrome`。若仍不合格，停止扩大修改，不直接改第三方材质，改为另行申请项目自有材质实例。
- `SM_barrel3` 原点位于底部附近，不能只换 Mesh 而不居中；缩放后必须回读可视 Bounds，确保整体仍在 `22/45 cm` 权威胶囊内。
