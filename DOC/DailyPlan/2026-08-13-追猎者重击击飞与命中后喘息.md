# 2026-08-13 追猎者重击击飞与命中后喘息

## 已确认目标

- 跑跳下砸和近距离挥斧都改为玩家现有 Heavy Impact 重受击：成功命中后远距离击飞、倒地并起身，不再提交追猎者攻击侧的轻受击。
- 可见斧头不作为唯一命中体；保留现有近战 Sweep 与下砸落点范围作为宽容候选查询，再由短时隐藏真实物理球接触玩家后提交 Heavy Impact。
- 成功命中后追猎者停住，等待玩家完成 Heavy 恢复，再提供可调额外喘息；恢复追逐后仍受可调攻击冷却限制。
- 保留跑跳攻击起跳前对移动玩家的一次性落点预测，空中不持续修正，玩家仍有躲避机会。

## 实施范围

- `APursuerCharacter` 持有一个平时关闭碰撞的 `AttackImpactBody`；只在命中候选成立后短时启用真实 Chaos 模拟。
- `UPursuerAttackComponent` 负责 Heavy 准备、真实接触提交、单次伤害、击飞增强、落空恢复、起身等待、额外喘息与成功冷却。
- `UPursuerConfig` 提供刚体速度/质量/半径、近战与下砸击飞速度、成功冷却、起身后喘息和最大等待时间。
- `DA_Pursuer` 初值：成功冷却 5 秒、起身后额外喘息 0.75 秒、异常最大等待 8 秒；跑跳伤害 30，近战伤害 18。
- 玩家 `BP_ZeroEscapeCharacter` 的最大生命值调整为 1000；运行时初始生命由 `HealthComponent::BeginPlay` 同步到最大值。

## 验证

- `DemoEditor Win64 Development` 完整构建成功。
- `Demo.Combat.PursuerAttack.PredictionAndBallistics` 与 5 项 `Demo.Physics.HeavyImpact.*` 自动化测试共 6/6 通过、0 警告。
- 玩家 Blueprint 重载后回读 `maxHealth=1000`。
- 用户已在 PIE 中看到并确认真实击飞效果；本次不再改变当前命中预测与击飞参数。
- 修正攻击隐藏刚体停用顺序：先退出物理模拟，再切为 `NoCollision`，避免 `AttackImpactBody` 的无效模拟配置警告。
- 最新无人操作 PIE 中多次落空，未产生新的 `AttackImpactBody` 物理配置警告；真实命中后的视觉手感以用户已完成的 PIE 观察为准。
