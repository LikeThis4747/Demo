# 2026-08-18 玩家跳跃能力削弱

## 目标

降低玩家跳跃高度和空中横向移动距离，不加入跳跃冷却。

## 范围

- `/Game/ZeroEscape/Characters/BP_ZeroEscapeCharacter`
- `CharacterMovement.JumpZVelocity: 400 → 300`
- `CharacterMovement.AirControl: 0.35 → 0.20`

不修改 C++、输入映射、MaxWalkSpeed、JumpMaxCount 或其他关卡/界面资产。

## 实施与验证

1. 先提交并推送当前工作区已确认的既有实现文件，建立基线。
2. 通过 UE5.8 官方 MCP 修改玩家蓝图默认值。
3. 编译、保存并回读两个参数。
4. 在 PIE 中观察跳跃高度、空中横向距离和落地后的正常移动。

## 回退

将两个参数恢复为 `JumpZVelocity=400`、`AirControl=0.35`。

## 当前状态

已实施，待用户体验验收。基线提交 `19d5327fb2582d89c26d66f5157f6f805a9b985b` 已推送；玩家蓝图已编译、保存并回读为 `JumpZVelocity=300`、`AirControl=0.20`；PIE 已启动并停止。
