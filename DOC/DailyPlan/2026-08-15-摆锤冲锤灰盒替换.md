# 2026-08-15 摆锤冲锤灰盒替换

## 目标

- 冲锤保留 HydroLab 圆角通风罩外观，并补成从任意方向观察都不透空的封闭锤头。
- 摆锤保留宽长方体轮廓，换用带法线和粗糙度细节的工业金属材质，同时解决侧穿与倒地夹人风险。

## 实施范围

- `/Game/ZeroEscape/Hazards/Physics/BP_BatteringRamHazard`
- `/Game/ZeroEscape/Hazards/Physics/BP_PendulumHazard`
- `/Game/ZeroEscape/Hazards/Physics/Data/DA_PendulumHazard_Default`
- 不修改 C++、机关行为逻辑或 HydroLab 原始素材。

## 拟装配

1. 冲锤 `RamHeadMesh` 使用 `/Game/Assets/SciFiHydroLab/Meshes/Props/SM_HydroLab_VentB1`，对齐现有 `120 x 280 x 280 cm` 盒形碰撞。
2. 在 `RamVisualRoot` 下增加无碰撞背盖平面，使用 `/Game/Assets/SciFiHydroLab/Materials/MI_HydroLab_Wall02`，只封住原模型的开放背面。
3. 摆锤 `BobMesh` 保留 `/Engine/BasicShapes/Cube`，视觉缩放改为 `(4.4, 0.8, 1.5)`，应用 `/Game/Assets/SciFiHydroLab/Materials/MI_HydroLab_Misc03`。
4. `RodMesh` 与 `AnchorMesh` 应用 `/Game/Assets/SciFiHydroLab/Materials/MI_HydroLab_Floor02`，消除裸灰盒外观。
5. `DA_PendulumHazard_Default`：`BobHalfExtents=(220,40,75)`，`PivotHeight=650 cm`；摆长和质量保持不变。

## 验证与回退

- 编译并保存两个 Blueprint，回读网格、材质、变换与 DataAsset 参数。
- PIE 检查冲锤伸缩、背面封闭、锤头视觉与碰撞对齐。
- PIE 检查摆锤侧面不可穿行、最低点约 `55 cm`、玩家倒地时不被锤体夹住。
- 材质图集若在 Cube UV 上观感不合格，只回退摆锤材质选择，不回退已确认的宽度和高度安全调整。

## 当前阻塞

- 另一实现任务仍有未提交 C++ 与 DataAsset 改动并持有唯一实现写入权；本计划在其提交、推送、远端核验并释放写入权后执行。
# 实施结果（2026-08-15）

- 冲锤：`RamHeadMesh` 已换成 `SM_HydroLab_VentB1` 并按 `120 x 280 x 280 cm` 碰撞盒对齐；新增无碰撞薄实体金属背盖，解决背面透空。
- 摆锤：保持长方体轮廓，锤体完整尺寸改为 `440 x 80 x 150 cm`；锤体使用 `MI_HydroLab_Misc03`，摆杆与顶架使用 `MI_HydroLab_Floor02`。
- 离地：`PivotHeight` 改为 `650 cm`，长度保持 `520 cm`，最低点理论净空为 `55 cm`。
- 验证：两个 Blueprint 编译、保存、属性回读与非 Dirty 检查通过；PIE Simulate 启动运行正常，本次运行未新增 Error。玩家侧穿、绕背和倒地夹体等待关卡内手感验收。
- Level0：发现旧放置实例带历史组件覆盖，已保留原位置、显示名称和摆锤标签，用更新后的同一 Blueprint 原地重建并保存；当前各只有一个实例，保存后回读与 PIE smoke 均通过。
- 重受击时序修复：加宽后外侧接触可能跨过原 `0.08s` 短预测窗口，导致先穿过 Pawn 胶囊再切物理；已将摆锤 `MaximumPreparationLeadTime` 调为 `0.16s`，保留 `850cm` 提前范围及原外观/碰撞尺寸。
- 最终回退：用户要求停止摆锤改造。已删除后续防穿组件并恢复改造前纯 Cube 状态：无材质覆盖、220x80x150 cm、PivotHeight 610 cm、MaximumPreparationLeadTime 0.08s；Level0 原位重建并保存。上条时序修复不再是当前状态。
- Prepare A/B 回退：用户要求先对比旧基线手感。当前纯 Cube 资产先独立提交并推送；随后不使用 Git 恢复文件，只手工恢复 `d819a1c2` 的摆锤发送端 Prepare 预测代码并完整构建、单独提交。保持 Pawn=Overlap、0.08s 数据、接收端 Heavy 状态机和当前资产不变；新的分层碰撞方案延后到用户验证之后。
- Prepare A/B 实施结果：纯 Cube 资产基线 `b320f057` 已推送并核验；`PendulumHazard.cpp` 已手工恢复旧版表面间距、相对闭合速度与帧自适应预测。关闭编辑器后 `DemoEditor Win64 Development` 正式 DLL 链接和目标元数据写入成功；等待用户在原复现点验证，不提前落地新碰撞方案。
- 统一接触窗口修复：用户复测证明旧 Prepare 仍不能消除穿模，根因是共享 Heavy 在首次真实 Hit 后立即把 Mesh 对 `PhysicsBody` 改为 Ignore。按用户确认不增加来源配置，删除即时放行路径，摆锤和冲锤统一复用现有 `PhysicsBodyReleaseDelaySeconds=0.15s`；其余 Prepare、冲量、状态机与资产不变。

## 2026-08-16 摆锤外观恢复

- 用户确认此前穿模与摆锤工业材质无关，授权恢复回退前已经试装的摆锤宽度、高度与材质。
- 只修改 `BP_PendulumHazard` 与 `DA_PendulumHazard_Default`：锤体 `440×80×150 cm`、支点 `650 cm`、锤体 `MI_HydroLab_Misc03`、摆杆/顶架 `MI_HydroLab_Floor02`。
- 摆长、质量、摆幅、阻尼、预测窗口、Heavy、PCG 与关卡逻辑保持不变；Blueprint 编译保存并回读后交由用户 PIE 视觉验收。
- 实施结果：Blueprint warnings-as-errors 编译、两资产精确保存与属性回读均通过，资产非 Dirty；预测窗口仍为 `0.08s`，未带回此前临时的 `0.16s` 调整。当前仅等待用户 PIE 视觉验收。
