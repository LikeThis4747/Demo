# 2026-08-15 PCG 路线、机关与死路能量球分步实现

## 已确认目标

- 用软路线提示和坍缩后图评分，减少长直、锯齿、无意义一格凸起与单一 L 型路线。
- 用有界 log2 权重改善机关覆盖、发射器远距配合、刺轮组合和资源支持，不将质量偏好升成硬禁止。
- 识别有长度、有转弯、不反客为主的奖励支线，在最终端点放置独立能量光团。

## 实施边界

- 保留现有“二维 WFC → 跨层 Plan → 纯值 Population → Spawn”架构；不新建房间/走廊 ID 系统、通用 Manager 或新生产 C++ 文件。
- 硬约束仅限几何/拓扑合法、连通、同格/操作包络冲突、玩家/追猎者/Exit/光团端点保护、Class/Transform/Actor 预算和原子回滚。
- 所有路线质量、支线 3/4/5 中心值、长直/一格凸起、机关覆盖、类型/组合/压力/多样性与资源支持都是软评分。
- 光团本轮只做确定性放置、可读原型与 Spawn；不做能量数值、行走充能、Exit 百分比或 HUD。

## 检查点 A1：坍缩前软引导

1. 在 Generation DataAsset 增加软开口强度、奖励支线长度与早停阈值。
2. 按方向感知最短路生成主路/支线开口偏好；超长直线平方增费，每格反向的锯齿额外增费。
3. 偏好只乘合法 OpeningMask 的正权重，不 Ban 候选；兜底使用独立 salt。
4. 关闭 Editor 完整构建，迁移/回读 Generation Profile，运行 WFC 自动化。

## 检查点 A2：图分析、选优与奖励支线

1. 仅在每棵 WFC 成功树后分析一次；不进入叶验收 `BuildRouteMetrics`。
2. 提取稳定主路 Run、合理转弯、锯齿、长直、度 1/2 支线和可绕过主路边。
3. `OrdinaryDeficit` 仍第一比较；其余密度/主路/支线/替代路/伪凸起量化为整数 micros，质量达 `0.82` 可早停。
4. Planner 先保护候选支线选玩家出生点；若容量不足则回退原出生规则，再过滤冲突支线，不令软目标引发整栋失败。
5. 最终 RewardBranches 进 Plan/Hash，`ZE_PCG_RESULT` schema `6→7`。

## 检查点 B：机关与普通资源

1. 类型比例写为 Easy `5/3/3/2`、Normal `4/4/4/2`、Hard `3/6/4/4`。
2. 把“机关处数/100 格”迁移为“预算单位/100 格”；摆锤/地刺/冲锤/发射器/刺轮成本为 `10/10/10/7/5` 个十分之一单位，初值 `22.0/23.5/24.5`。
3. 机关覆盖从图距离 4 开始按 `0.35x+0.03x²` 增长，单通道上限 `+5 log2`；无已接受机关时为 0。超长空白优先被补足，达到总分上限时可能暂时压缩其他正奖励的排序空间，留给实玩调参。
4. 发射器组合奖励为图距离 `1/2/3+ = 0.25/1.0/1.75`；压力诊断保持按组半径归一。
5. 奖励支线接近区只把正组合 log2 乘 `1.2`；负贡献和压力不放大。
6. 删除普通资源之间图距离 3 硬间距，保留同格/机关占用冲突。

## 检查点 C：死路能量光团

1. 在 Plan 增加独立 `EnergyOrbPlacements` 和 `EnergyOrbs`，不混入 `ResourcePlacements/ResourceStats`。
2. 每条最终 RewardBranch 端点确定性放一个光团，不消费 HazardRng/ResourceRng；前后 Actor 预算都计入。
3. Populator 完整接入名称、Class、加载、Transform、AppendLayer、Spawn 和日志。
4. 原型资产仅有可读 Emissive 光团、低成本 PointLight 和 OverlapOnlyPawn 碰撞，默认无 Tick，不做爆炸表现。

## 验证与回退

- 四个检查点保持代码职责和回退边界独立；本次因同一轮反射迁移与联合回归连续完成，最终作为一个精确范围提交交付。
- 自动化覆盖：软提示不 Ban、同 Seed 稳定、软分析每成功树一次、支线/死路不变式、覆盖限幅下组合仍有效、资源可相邻、随机域隔离、光团统计/预算/Class 失败原子性。
- 性能记录每层 WFC 树数、软分析调用数、整栋 P50/P95；平均 WFC 树数不超基线 `+0.25`，整栋 P95 不超 `2×`。
- 完整构建后重开 Editor，用官方 UE5.8 MCP 迁移/编译/保存/回读 DataAsset 与 Blueprint，最后运行代表 Seed 与多 Seed PIE；仍保留用户玩家手感验收。

## 2026-08-15 实施与验证记录

- A1/A2/B/C 已落盘；没有新增生产 C++ 文件、Manager 或组件体系。
- UE 5.8 完整构建通过；正式 DataAsset 已迁移和回读；能量光团 Blueprint 及材质已创建、编译、保存并回读。
- `Demo.PCG.Population` `16/16` 成功；完整 `Demo.PCG` `43/43` 成功，包含 900 Seed 稳定性和 90 Seed 路线质量压测。
- 初版 90 Seed 路线诊断：奖励支线约 `1.77/层`，一格终止凸起约 `2.11~3.04/层`，替代路线覆盖约 `0.004~0.008`，规划 P95 约 `565~842 ms`；下方“实玩反馈后的最小收敛”已给出最新结果。
- 正式 `L_Game` 独立运行 Seed `12345` 成功：3 层、6 条奖励支线、6 个光团、50 个机关、21 个普通资源；玩家名义速度由正式 Pawn CDO 读取为 `400 cm/s`。
- 初版已知差距：没有稳定达到每层 3/4/5 条支线，替代路线覆盖接近 0；下方收敛提高了支线与替代覆盖，但仍不得描述为已经稳定达到配额或解决多主路问题。
- Level0 仍覆盖旧 Prototype GameMode，本轮正式链路通过独立 `L_Game -game` 验证；路线观感、机关节奏和光团可读性仍待用户从正式入口游玩验收。

## 2026-08-15 实玩反馈后的最小收敛

- 查明原配置把每层 `3/4/5` 条软中心值按容量公式静默压成了 `2/2/2`。Generation Profile 现将奖励支线最短/偏好最长长度设为 `4/7` 格，软开口提示提高到 `+4 log2`；每层普通格软目标设为 `40`，2/3/4 层整栋软下界同步为 `80/120/160`，Easy/Normal/Hard 支线格占比分别为 `0.30/0.40/0.50`，因此有效软中心值恢复为 `3/4/5`。
- 未提示的随机死端/T 路口基础权重由 `80/35` 降为 `40/20`，与最高 `16 倍`的定向提示配合，减少用随机一格凸起冒充支线的收益；仍不 Ban 任何合法形状。
- 奖励支线识别不再要求 Gateway 必须位于算法任选的单条稳定最短主路上；仍须是度 1 终点回溯到真实路口、长度至少 4 格、包含转弯且路径全为普通格。这修正了明显支线因连接到次级路线而漏放光团的问题，不放宽同格/结构占用冲突。
- 90 Seed 新诊断：Easy/Normal/Hard 奖励支线约 `2.452/2.809/3.387` 条每层；一格终止凸起约 `2.521/2.266/2.708` 个每层；替代路线覆盖约 `0.050/0.050/0.057`；规划 P95 约 `564/845/839 ms`。软目标明显改善但仍不承诺逐层达到 `3/4/5`。
- UE 5.8 Development Editor 完整构建成功；除长压测外其余 `42/42` PCG 测试成功。关闭测试进程的 MCP 自动启动后，`PublicSeedStability900` 独立通过 `900/900`，0 警告、0 错误；正常 UE 编辑器已重开。
- 本轮没有提高机关或资源总预算。实玩中的空长走廊属于图覆盖被侧向凸起/可绕机关满足的问题，摆锤无效则源于高厅结构尚未被路线评分优先穿过；两者不能靠继续增加总数或支线参数可靠解决，保留为用户复测后的独立小范围候选。

## 2026-08-15 高厅摆锤主路软奖励

- 目标是让已经必定生成摆锤的高厅更可能被主要通行路线利用，而不是把高厅改成必经点。
- Planner 在现有结构投影中标出高厅可走格；每个成功 WFC 候选统计稳定主路覆盖这些格的比例。
- 该比例以 `0.15` 进入现有候选路线质量分；当本层没有高厅时完全不参与归一化。覆盖率为 `0` 只是得分较低，不会被拒绝、重抽 Seed 或触发硬修复。
- 没有修改 Population 预算、摆锤几何、高厅数量或能量光团合同；“摆锤后另算一条光团支线”仍未实现，避免扩大 Plan/Hash/占用边界。
- 验证：UE 5.8 Development Editor 完整构建成功；人工拓扑锁定“主路覆盖=1 / 主路绕过=0”且两者都合法；完整 `Demo.PCG` `43/43` 成功，包含 `PublicSeedStability900`。
- 新 90 Seed 路线回归：Easy/Normal/Hard 支线约 `2.452/2.755/3.349` 条每层，规划 P95 约 `519/765/775 ms`；与上一版相比没有性能回退。实际摆锤是否更多进入玩家主通路，仍由用户次日多 Seed 实玩验收。
