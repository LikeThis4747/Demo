# 2026-08-17 Level0 测试地图蓝色全息传送门实施计划

## 已确认方案

- 使用现有 `/Game/Assets/SciFiHydroLab/Meshes/Walls/SM_HydroLab_DoorFrame` 作为门框。
- 使用现有 `/Game/Assets/SFCorridors/Materials/MI_hologram2` 作为蓝色全息面材质，并保留蓝色 PointLight。
- 门框约 450x300cm，全息面约 400x250cm，适配测试场地。
- Level0 直接放置 `Portal_Test_PlayerStart`，位于 `PlayerStart_0` 前方约 600cm，方便开局快速找到。
- 正式游戏出口的光团达标闪烁逻辑保留；Level0 实例仅用于视觉快速验收。

## 范围约束

- 不依赖 Level0 的 PCG 运行时终点，不改 PCG/WFC、楼梯灯、磁力拾取和追猎逻辑。
- 先使用现有素材完成可验收原型；Portal 2 风格材质动画与边缘能量不在本轮扩展。
