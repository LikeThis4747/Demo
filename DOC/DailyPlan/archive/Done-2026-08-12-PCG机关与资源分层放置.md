# 2026-08-12 PCG 机关与资源分层放置

> 状态：已完成并归档。2026-08-12 用户确认当前版本验收通过；交付结论见 `DOC/DailyReport/2026-08-12-PCG机关与资源分层放置验收收口.md`。

- 目标：用一套 Population 总控依次规划机关与磁力资源；高厅必放摆锤，普通机关和资源分别按整栋通行图最小间距采样，资源在格内安全区域随机落点。
- 难度：一份共享资产装配；Easy / Normal / Hard 机关总量只做小幅 26 / 28 / 30 分档，三档间距统一为 2，主要用普通机关权重改变构成。
- 非目标：不改机关行为、伤害、初始相位、WFC 布局、Level0、导航资产或追猎者。

## 预计范围

- 新增：`Source/Demo/Private/PCG/Population/ZeroEscapePopulationPlacementPolicy.cpp`。
- 修改：Population 三个 Public 头文件、Generator 头/实现、Population Policy/Populator、随机域及两个现有测试文件。
- 资产迁移：原位更新 `/Game/ZeroEscape/Generation/Population/DA_Population_Default`；不新建兼容资产。

## 顺序与检查点

1. 纯值配置、随机域、通行图、候选与分层采样；构建和纯值自动化。
2. Generator Ready 快照与旧候选接口移除；构建并复核 Blueprint 引用。
3. Populator 统一规划、按实际类型加载、Spawn 和回滚；迁移并保存 DataAsset/BP。
4. `Demo.PCG.Population` 自动化与完整 `Demo.PCG` 回归；正式多难度大样本 Sweep 留作后续回归与参数校准。
5. 代表 Seed PIE 接入验证；用户已完成本轮机关密度、分布和整体可玩观感验收。

## 进入实现前门禁

- 用户确认拟实现代码和难度分档选择，并明确授权实现。
- 处理当前 Level0、动画资产及其他任务文档的工作区归属。
- 完成全部已确认改动的内部工蜂基线 commit、push 与远端核验，确认工作区为空。

## 2026-08-12 实施与验收收口

- 完整实现基线为 `d026668bb0befea6db89ba1131d2534093a2fc7f`，已推送并核验内部工蜂。
- 旧 `OneEveryNCells + MaxCount` 规则链已直接移除；没有保留双轨、兼容包装器或额外 Manager。
- 机关与资源使用独立目标、图距离拒绝采样和随机域；高厅摆锤必放并计入机关目标。
- 普通机关按“类型权重 -> 合法锚点 -> 无冲突方向”抽样；发射器前方格等实际操作格会参与机关间互斥和资源避让。
- 资源先选图上的锚点格，再在扣除占地半径的安全矩形内随机 X/Y；资源参数不会洗牌机关结果。
- Demo 模块正式链接成功；`Demo.PCG.Population` 8/8、完整 `Demo.PCG` 29/29 通过。
- `L_Game` 代表 Seed 12345 完成一次 Normal 接入 PIE：36 处机关与 13 个资源全部生成，Game Setup 成功且无欠填。
- 用户连续否决 8/100（本 Seed 10 处）与 16/100（20 处）两个过稀初值，并明确难度不应依赖明显增减总数。Easy / Normal / Hard 机关密度最终收口为 26 / 28 / 30，三档间距统一为 2；地刺/冲锤/发射器权重分别为 `5:3:2`、`1:1:1`、`2:3:5`。资源保持 10/100 格、间距 3 格。
- 用户已确认当前“总量接近、构成分档”的版本验收通过；多 Seed 统计与长期难度平衡转为后续回归/调优，不再作为本任务阻塞项。
- 功能提交 `cad49998863d98c604c956b5dd4d593fdfaf0673` 已推送并核验内部工蜂 `origin/main`。
