# 2026-07-27 追猎者最小 ABP 速度变量

> 状态：阶段检查点已归档；GroundSpeed/EventGraph 已验证，正式 AnimGraph、AnimClass 与视觉验收留待动画工作重启。

## 目标

在现有 `/Game/ZeroEscape/Enemies/Animation/ABP_Pursuer_Locomotion` 中新增 `GroundSpeed`，由动画更新事件读取 Pawn 的水平速度，为 locomotion Blend Space 提供输入。

## 范围

- 修改 `Content/ZeroEscape/Enemies/Animation/ABP_Pursuer_Locomotion.uasset`。
- 新增 Float 变量 `GroundSpeed`。
- EventGraph 使用 `Try Get Pawn Owner`、有效性判断、`Get Velocity` 与 `Vector Length XY` 更新变量。
- 不修改 AnimGraph、BlendSpace、`BP_Pursuer`、AI、Physics Control、DataAsset、源码或关卡。

## 验证与回退

- 编译 AnimBP，要求 0 error。
- 保存后回读变量、节点和边，确认 `GroundSpeed` 数据与执行路径完整。
- 若任一步失败，停止后续图修改并报告；不创建替代资产或脚本绕过。

## 实施记录

- 已创建 Float 变量 `GroundSpeed`，分类为 `Locomotion`，不可实例编辑。
- 已建立带 Pawn 有效性保护的水平速度更新图；EventGraph 回读为 7 个节点、7 条连线。
- AnimBP 定向编译通过：`UpToDate`，0 error、0 warning。
- 目标资产已单独保存；AnimGraph、BlendSpace、`BP_Pursuer`、AI、Physics Control、源码和关卡均未修改。
