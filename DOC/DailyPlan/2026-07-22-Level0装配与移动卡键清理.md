# 2026-07-22 Level0 装配与移动卡键清理

## 授权与目标

- 用户已于 2026-07-22 明确授权按已展示方案实施。
- 将四个磁性测试物体从 GameMode 运行时生成迁移为 `Level0` 的持久关卡实例。
- 删除只服务于旧生成器的接口，并清理已证明无效或职责重复的移动输入代码。
- 不调整吸取曲线、投掷、相机、磁力选取、近墙安全算法或输入资产配置。

## 修改范围

- `Source/Demo/Public/GameFlow/ZeroEscapePrototypeGameMode.h`
- `Source/Demo/Private/GameFlow/ZeroEscapePrototypeGameMode.cpp`
- `Source/Demo/Public/Actors/Magnetism/MagneticPrototypeProp.h`
- `Source/Demo/Private/Actors/Magnetism/MagneticPrototypeProp.cpp`
- `Source/Demo/Public/Characters/ZeroEscapeCharacter.h`
- `Source/Demo/Private/Characters/ZeroEscapeCharacter.cpp`
- `/Game/Levels/Level0` 及保存关卡实例实际生成的包
- `/Game/ZeroEscape/GameFlow/BP_ZeroEscapePrototypeGameMode` 仅做继承属性刷新、编译与保存

## 实施步骤

1. 确认 PIE 停止且关卡内不存在同名磁性测试实例。
2. 使用 UE Editor MCP 放置四个 `BP_MagneticProp`，设置名称、位置、缩放、质量与 Outliner 文件夹；回读后保存。
3. 删除 GameMode 的运行时测试物生成器，以及 `AMagneticPrototypeProp::ConfigurePrototype()` 死接口。
4. 将普通移动收敛为 `Triggered -> AddMovementInput`：删除 `ClearMoveInput`、Completed/Canceled 绑定、Character 重复 Flush 与 UnPossessed 重复 Consume。
5. （已被后续根因修订取代）最初保留了 PlayerController 进入 Playing 状态的一次输入清理；用户复现与参考项目核对后，确认这段逻辑会与 PIE 焦点策略形成重复 Owner，后续按下方修订方案移除。

## 验证

- 精确核对 Git 变更，只接受上述源码、计划/任务记录、Level0 和实际关卡实例包。
- 完整构建 `DemoEditor Win64 Development`，不使用 Hot Reload。
- 编译并保存相关蓝图，关闭并重新打开关卡后回读四个磁性实例。
- PIE 验证不再出现运行时重复生成；检查四种质量均可抓取。
- 用 `showdebug enhancedinput` 与 CharacterMovement 数据区分持续前跑时的输入源和物理速度源；无法自动复现的用户按键时序不冒充已通过。
- 最终状态保持“机器验证完成、等待用户手感与卡键复现验收”，未经用户验收不标记功能完成。

## 当日实施结果

- `Level0` 已持久保存四个磁性测试实例；完整关闭并重启编辑器后，名称、Transform、文件夹及 5/20/45/70 kg 配置均可精确回读。
- GameMode 运行时生成器及其死接口已删除；移动输入的重复清理代码已移除，没有新增 Tick、定时器或强制停止兜底。
- `DemoEditor Win64 Development` 完整构建成功；三个相关蓝图编译均为 0 error / 0 warning；SelectedViewport PIE 启停成功，未发现本任务相关配置错误。
- 尚未捕获持续前跑的用户时序，因此不把本轮清理宣称为该 bug 已修复。后续由用户复现时保留调试叠层证据，再决定输入源或 CharacterMovement/碰撞侧的最小修改。

## 输入焦点根因修订（用户已授权）

### 目标与边界

- 保留 `AZeroEscapePlayerController : APlayerController` 及 GameMode 对它的引用，作为未来 Possess、UI 输入模式和相机等玩家会话级功能的扩展点。
- 删除 Controller 在 `BeginPlayingState()` 中无条件执行的 `SetInputMode(GameOnly)` 与 `FlushPressedKeys()`；普通 WASD 和 Enhanced Input Mapping Context 继续由 Character/Pawn 负责。
- PIE 自动聚焦改由项目编辑器默认配置负责；不新增 Tick、Timer、移动清零、强制停止或额外输入 Subsystem。

### 文件、职责与依赖

- `D:\UE5projects\Demo\Source\Demo\Public\GameFlow\ZeroEscapePlayerController.h`：保留空的自定义 Controller 类型，删除错误生命周期接口并同步职责注释。Owner 为玩家会话级扩展点；不拥有普通移动或开局焦点。
- `D:\UE5projects\Demo\Source\Demo\Private\GameFlow\ZeroEscapePlayerController.cpp`：保留翻译单元，删除 `BeginPlayingState()` / `ApplyGameplayInputMode()` 实现并同步职责注释。
- `D:\UE5projects\Demo\Config\DefaultEditorPerProjectUserSettings.ini`：新增项目默认值 `GameGetsMouseControl=True`，供新用户/新环境的 Selected Viewport PIE 自动聚焦。
- `D:\UE5projects\Demo\Saved\Config\WindowsEditor\EditorPerProjectUserSettings.ini`：启动前把当前机器已保存的 `False` 改为 `True`；冷启动后 UE 将等同项目默认的该行省略，当前无 `False` 覆盖。该文件受 Git 忽略，不属于交付变更。
- `D:\UE5projects\Demo\Content\ZeroEscape\GameFlow\BP_ZeroEscapePrototypeGameMode.uasset`：仅把“玩家控制器类（Player Controller Class）”默认值设为 `AZeroEscapePlayerController`，清除蓝图对基础 `APlayerController` 的旧覆盖；不修改蓝图图表。
- `D:\UE5projects\Demo\claude\tasks\archive\TASK-20260722-002-Level0装配与移动卡键诊断.md`：记录授权、检查点和验证证据。
- 依赖方向保持 `GameMode/BP GameMode -> AZeroEscapePlayerController` 类型选择、`AZeroEscapePlayerController -> APlayerController` 空扩展；本轮不修改 GameMode/Character 源码、蓝图图表或输入资产。

### 验证与回退

1. 精确差异检查：`Source/Demo` 中不再存在项目自定义的 `BeginPlayingState`、`SetInputMode` 或 `FlushPressedKeys`；GameMode 仍引用自定义 Controller。
2. 完整构建 `DemoEditor Win64 Development`，不使用 Hot Reload。
3. 冷重启编辑器后确认项目默认与当前用户的有效值均为 `GameGetsMouseControl=True`，并确认 `BP_ZeroEscapePrototypeGameMode` 实际使用 `AZeroEscapePlayerController`。
4. 编译相关蓝图并进行 Selected Viewport PIE：启动无需额外点击即可移动，松开 WASD 后停止；覆盖 Shift+F1、点击输出日志、Alt-Tab 后在失焦期间松键，恢复焦点后保持静止。
5. 机器验证通过后仍保持“待用户按键验收”；若出现回归，只回退上述 Controller、GameMode 蓝图类默认值与项目编辑器默认配置，不触碰 Character、GameMode 源码或磁力/关卡改动。

### 当前实施检查点

- Controller 两个函数及其声明已删除，空的自定义类、独立翻译单元和 GameMode 引用均保留；项目默认为 `GameGetsMouseControl=True`，当前机器无 `False` 覆盖且运行态有效值为 `true`。
- 静态搜索确认项目源码内已无自定义 `BeginPlayingState`、`ApplyGameplayInputMode`、`SetInputMode`、`FlushPressedKeys` 或旧移动清理兜底。
- 中间检查点中，UHT、C++ 编译和独立后缀 DLL 链接成功；首次标准目标链接仅因正在运行的编辑器占用 `UnrealEditor-Demo.dll` 而失败。用户随后安全关闭编辑器，最终标准完整构建已经成功。
- 中间关闭请求只提示未保存的 `Level0`，当时已取消以避免越权保存或丢弃；用户随后自行处理并关闭，冷启动与 PIE 边界验证已经完成。

### 冷启动与运行时验证结果

- 用户关闭旧编辑器后，标准 `DemoEditor Win64 Development` 完整构建成功；三个相关蓝图在冷启动后均为 0 error / 0 warning。
- `BP_ZeroEscapePrototypeGameMode` 的基础 `APlayerController` 旧覆盖已清除；PIE 运行态确认 Player 0 实际使用 `/Script/Demo.ZeroEscapePlayerController`，不是仅依赖静态继承关系推断。
- 编辑器运行态加载的 `GameGetsMouseControl=true`。不点击游戏视口直接按住 W 1.5 秒后，Pawn 从 `(0.0, 0.0, 100.49)` 移动到 `(703.44, 106.78, 97.65)`；松键后的连续回读位置相同。
- 在 W 按下后切到 Codex 窗口、于 UE 失焦期间松键并返回 UE，Pawn 后续连续两次位置均为 `(1102.73, 136.30, 97.65)`，未出现残余移动。
- PIE 已安全停止；运行态只读验证使用的临时 Python 配置与脚本已清理，`Demo.uproject` 和 `Config/DefaultEngine.ini` 无残留差异。
- 机器验证已完成；仍等待用户用真实操作补测 Shift+F1、Output Log 与磁力输入交错。未经用户验收，不将该任务标记为完成。

### 用户验收结果：未解决

- 用户实际操作仍可在按 Shift 后复现“松开全部移动键仍持续前跑”，因此本次输入焦点修订没有解决完整问题。
- Output Log 反复出现 UE 调试命令 `DebugManager.CycleToPreviousColumn/NextColumn`；它与 Shift 明确相关，但目前只有相关性，没有足够证据认定为根因或 UE 引擎 Bug。
- 按用户决定暂停继续调试，不增加强制停步或清键兜底；问题已转入 `DOC/Bugs/BUG-20260722-001-Shift后持续前跑.md` 长期跟踪。
