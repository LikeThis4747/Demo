# 2026-08-06 壁挂式物理制导一次性机关实施计划

> 状态：历史推进/制导方案，已被 `2026-08-10-狭窄走廊预判抛射Chaos机关实施计划.md` 取代。本文中的 Thruster、持续主推力、惯量转矩和旧参数仅作过程追溯，不再作为实现、调参或验收依据。

## 目标

在 Level0 完成一个一次性壁挂机关的最小可玩闭环：

- 第一个进入触发区的 ACharacter 被锁定，不区分玩家或追猎者。
- 预警结束后从 Muzzle 发射真实 Chaos 刚体。
- 可调推进时间内由尾部 UPhysicsThrusterComponent 真实施力并做小角度制导。
- 首次有效阻挡碰撞后立即结束本次全部推进并恢复重力，路线从这一刻起完全交给 Chaos 反弹、摩擦、滑行和休眠。
- 复用现有 IHeavyImpactReceiver 准备合同，不在角色侧补 AddImpulse 或 LaunchCharacter。

## 已确认设计

- 机关按壁挂式理解；正面墙是第一测试基线，拐角墙是第二测试变体。
- 关卡摆位承担主要瞄准，Muzzle 局部 +X 是中心线；制导只补偿路线内横向移动。
- TriggerAnchor 可独立于 Muzzle 摆放和旋转；拐角触发区必须位于目标越过遮挡、已有发射净空的位置。
- 弹体胶囊局部 +Z 是纵轴；UE5.8 Physics Thruster 的局部 -X 是实际施力轴，且 Thruster 必须直接附着模拟刚体。
- 不实现循环发射、对象池、自动复位、自动清理、伤害、联网、PCG 放置或预设的碰撞去抖。

## Review 取舍

- 本次按用户决定独立实现，短暂容忍第三份 HeavyImpact 预测流程；功能稳定后再评估公共层，不新增 UHeavyImpactPredictionComponent、IHeavyImpactSource 或几何工具库。
- 稳定性重构采用固定主 Thruster 与独立真实转矩：Thruster 只沿弹体纵轴提供可调前进力，朝向误差与角速度阻尼计算期望角加速度，再结合刚体惯量施加物理转矩；不旋转喷口、不写 Transform 或速度。
- 发送准备请求前接入 FHeavyImpactPreparationRequest::IsStructurallyValid。
- 在代码顶部和属性注释中明确 Muzzle +X、弹体 +Z、Thruster -X 三套轴约定。

## 文件范围

新增：

- Source/Demo/Public/Actors/Hazards/ThrustGuidedHazardLauncher.h
- Source/Demo/Private/Actors/Hazards/ThrustGuidedHazardLauncher.cpp
- Source/Demo/Public/Actors/Hazards/ThrustGuidedHazardProjectile.h
- Source/Demo/Private/Actors/Hazards/ThrustGuidedHazardProjectile.cpp
- Source/Demo/Public/Data/Hazards/ThrustGuidedHazardTuningData.h
- Source/Demo/Private/Data/Hazards/ThrustGuidedHazardTuningData.cpp

后续创建资产：

- /Game/ZeroEscape/Hazards/Physics/BP_ThrustGuidedHazardLauncher
- /Game/ZeroEscape/Hazards/Physics/BP_ThrustGuidedHazardProjectile
- /Game/ZeroEscape/Hazards/Physics/Data/DA_ThrustGuidedHazard_Default
- /Game/ZeroEscape/Hazards/Physics/Materials/PM_ThrustGuidedHazard

后续修改：

- /Game/Levels/Level0，仅增加独立测试实例，不修改现有摆锤/冲锤逻辑。

明确不修改：

- Demo.Build.cs
- IHeavyImpactReceiver、HeavyImpactTypes 和角色 HeavyImpact 实现
- 玩家/追猎者 Blueprint、AnimBP、PCA 和受击 DataAsset
- PCG Population、既有摆锤/冲锤 C++ 与资产
- 第三方素材包

## 实施顺序

1. 新增 DataAsset、Launcher 和 Projectile 六个 C++ 文件。
2. 执行 DemoEditor Win64 Development 无链接编译；修正 C++/UHT 问题。
3. 编辑器允许正式链接时完成项目 DLL 构建。
4. 官方 UE MCP 创建并配置 DataAsset、两个 Blueprint 和物理材质；Blueprint EventGraph 保持为空。
5. Level0 静态布置正面墙和拐角墙两个测试版本，保存前核对 Muzzle/TriggerAnchor、出生净空和无遮挡关系。
6. 正常关闭 UE 编辑器，执行完整 DemoEditor Win64 Development 构建。
7. 处理审阅报告、任务卡、进度/当日记录与交接，完成完整内部提交/推送/远端核验；PIE 由用户次日执行。

## 强制 Git 基线

- 用户已明确授权实现。
- 实施前完整基线：5983d11ff9c8d9158188e77b627b1e6dbca6fd04。
- 本地 HEAD 与 origin/main 一致。
- origin：git@git.woa.com:shiqiqiwang/Demo.git。
- 首次功能写入前 git status --short 为空。

## 验证矩阵

- 配置缺失、非法数值、非单位 Actor Scale：明确 Disabled，不使用隐藏兜底。
- 道具先进入不消耗机关；两个角色只锁定首个回调。
- 目标在预警期间失效：取消本次预警并恢复 Armed，不消耗机关、不换目标。
- 中心/左右进入：初始方向不得超过最大锥角。
- 首次撞墙、道具、角色、发射器外壳或出生穿透：立即停止 Thruster 和受控阶段，恢复重力并进入 Coasting。
- 同一对象多次真实接触：允许，不加固定冷却或抖动过滤。
- 推进结束：Thruster 与 Actor Tick 关闭，重力恢复。
- 休眠后被外力唤醒：只恢复 Coasting 预测，不恢复制导。
- 玩家与追猎者：同一个 LaunchId 按接收者独立准备，真实 Chaos 接触决定位移。
- 拐角版本：触发时 Muzzle 到目标区域无遮挡，不允许依赖大角度绕墙。

## 当前验收边界

本次 AI 交付要求是：C++ 与资产落盘、Blueprint 编译/保存、Level0 静态布置、关闭编辑器后的完整构建，以及完整内部 Git 提交/推送。用户明确把 PIE、玩家/追猎者运行矩阵和手感验收留到次日，因此本次提交后仍标记“等待用户 PIE 验收”，不伪报玩法完成。

## 实施结果

- 六个独立 C++ 文件已按 Public/Private 镜像目录落盘；没有修改 `Demo.Build.cs`、HeavyImpact 合同、角色受击实现或既有机关。
- 机关已稳定性重构为“首个 ACharacter 锁定 → 预警 → 固定 Thruster 前进 + 惯量感知真实转矩追踪 → 首次阻挡接触立即停推 → 纯 Chaos”。没有速度/Transform 写入、没有 `AddImpulse`、`LaunchCharacter` 或 ProjectileMovement；姿态控制只使用真实物理转矩。
- 已创建 Launcher/Projectile Blueprint、默认调参 DataAsset、物理材质与简易机身材质；两个 Blueprint 的 EventGraph 为空并编译保存。
- Level0 复用 `PCG_Test/Room900`，在 `Hazards/Physics/ThrustGuidedHazardTest` 下放置正面墙和拐角墙两个实例；没有改造原有 PCG 房间尺寸或逻辑。
- Review 中的 `IsStructurallyValid`、完整配置校验和轴约定已采纳；HeavyImpact 公共层按用户决定延后。旧喷口偏转/尾部横向力方案已被固定主推力与独立真实转矩取代。

## 静态验证结果

- Demo 模块无链接编译通过，正式链接生成 `Binaries/Win64/UnrealEditor-Demo.dll`。
- 官方 UE MCP 回读确认 Blueprint 父类、空 EventGraph、CDO 引用、物理材质与组件装配正确；Blueprint 以警告视为错误编译通过。
- 两个 Muzzle 的前 200 cm 静态射线无阻挡；沿完整发射路线分别约 730/760 cm 后命中对面墙。
- UE 编辑器已正常关闭；最终只运行带 `-Module=Demo -NoEngineChanges` 的 Demo 模块构建，结果成功且目标已最新，没有编译引擎。
- 未运行 PIE。局部材质编译辅助工具曾产生一次 handled ensure，但返回 0 error/0 warning；官方资产验证与最终 Demo 构建均通过，运行期行为仍需用户次日 PIE 验收。

## 2026-08-10 稳定性重构结果

- 受控阶段不再用单一总速度 800 cm/s 直接关机：油门按有符号前向速度计算；异常保护分别检查前向/倒飞、侧滑和 950 cm/s 组合总速，避免正常转向误熄火。
- 出生前按完整胶囊做净空查询，并按 ProjectileBody 的真实双边碰撞响应过滤；仅会把实际具备物理碰撞的阻挡体视为出生阻挡。两个现有 Launcher 外壳/炮管已只读确认满足 `QueryAndPhysics + BlockAllDynamic`。
- 第一次阻挡 Hit（包括发射器外壳和 `bStartPenetrating`）统一结束推进；推力归零、Thruster 停用、重力与低阻尼恢复，后续不再追踪。
- 默认参数已调整为质量 60 kg、目标速度 650 cm/s、最大受控加速度 900 cm/s²、受控时间 1.4 s，配合 0.9 s 预警和动态目标前置；参数只作为首轮可见性基线，最终以用户 PIE 画面为准。
- Demo 模块无链接编译与最终完整链接均成功；最终构建只链接 `UnrealEditor-Demo.lib/.dll`，没有重编引擎。两个 Blueprint 以警告视为错误编译并保存，DataAsset/CDO 逐字段回读一致。
- 未运行 PIE，未保存 Level0，未修改 HeavyImpact。画面、命中率、首次接触停推和多次反弹仍待用户验收。

## 现场 Review 修正

- 原方案复用了远离当前机关测试区的 `PCG_Test/Room900`，用户现场 Review 判定位置错误；两个 Launcher 将全部迁出，不在原房间保留测试实例。
- 新房放在 `PendulumTestRoom` 画面右侧，与画面左侧冲锤房、中间摆锤房形成横向三房对照。
- 新房采用 300 cm 高的低天花板 L 形走廊：南侧入口直段后 90° 转向世界 -X；世界范围约 `X=49300..50800`、`Y=29700..30900`。
- 保留正面墙与拐角墙两个发射版本；拐角发射器的触发锚点后移，确保预警发生时目标与 Muzzle 之间仍有可读距离。
- 本轮只改 Level0 摆位和文档，不改机关代码/资产参数；完成后重新提交推送，再释放工作区给起身实现任务。

### 修正结果

- 新房已落在摆锤房画面右侧，采用 300 cm 低顶 L 形轮廓；与左侧冲锤房、中间摆锤房形成三房并排总览。
- 房间 45 个构件与 2 个既有 Launcher 均已归入 `Hazards/Physics/ThrustGuidedHazardTestCorridor`；旧机关测试文件夹已自动消失。
- 两条走廊中心线及两个发射口前 200 cm 均通过静态净空检查；转角触发锚点后移至 1100 cm。
- Level0 已保存并完成官方 MCP 回读；未运行 PIE。
- UE 编辑器已正常关闭；最终 Demo 模块构建 `Succeeded`，目标已最新且 0 个动作，没有编译引擎。
