# 2026-08-10 重冲击提前快照起身交接实施计划

## 目标

- 正常开阔地在身体落地、低速并连续获得短暂地面支撑后，直接从当前物理姿势混入起身 Montage。
- 删除会让睡眠刚体重新唤醒并可见蠕动的物理姿势准备路线。
- 保留物理冲击、最终倒地点、正反面选择、安全 Capsule 找位和玩家/AI 共用恢复状态权威。

## 范围

- 修改 `Source/Demo/Public/Animation/HeavyImpactAnimInstance.h`。
- 修改 `Source/Demo/Private/Animation/HeavyImpactAnimInstance.cpp`。
- 修改 `Source/Demo/Public/Components/Physics/HeavyImpactResponseComponent.h`。
- 修改 `Source/Demo/Private/Components/Physics/HeavyImpactResponseComponent.cpp`。
- 修改 `Source/Demo/Public/Data/Physics/HeavyImpactTuningData.h`。
- 修改 `Source/Demo/Private/Data/Physics/HeavyImpactTuningData.cpp`。
- 修改 `Source/Demo/Private/Physics/Tests/HeavyImpactResponseTests.cpp`。
- 修改玩家与追猎者 HeavyImpact AnimBP、两份 HeavyImpact DataAsset。
- 不修改起身 AnimationSequence、Physics Asset、PCA、机关代码或角色骨架。

## 实施顺序

1. 删除 `PreparingPose` 状态、旧恢复姿势 Snapshot/Sequence Evaluator 数据和对应 Physics Control 驱动。
2. 在 `Settling` 连续低速且有支撑约 0.10 秒时尝试安全 Capsule 方案；成功则立即交接，0.35 秒仅保留为进入睡眠的兜底阈值。
3. 同一交接中保存根重定位后的物理姿势、停止全身物理并启动动态 Montage；用唯一 Alpha 在约 0.22 秒内从 Snapshot 混到 Slot。
4. `Downed` 空间重试只检查安全位置并直接做相同 Snapshot 混合，不再唤醒刚体执行物理整理。
5. 简化两个 AnimBP 为 `Downed Pose Snapshot` 与现有 `DefaultSlot` 的双向混合。

## 验证与回退

- C++：HeavyImpact 自动化契约、DemoEditor `-Module=Demo -NoEngineChanges` 构建。
- 资产：两个 AnimBP warnings-as-errors 编译、两份 DataAsset 属性回读和资产保存状态检查。
- 运行：开阔地玩家/AI 正躺与趴倒、墙边/阻塞重试、起身后控制/追逐、二次受撞；画面仍由用户最终验收。
- 回退：本次实现作为一个完整提交；旧物理姿势准备代码不保留并行运行分支，必要时按提交整体回退。

## 实施结果

- 已删除 `PreparingPose`、恢复姿势 Sequence Evaluator、准备时间/控制比例、相关计时器与开发期切换开关；代码统计为净删除，未保留失效修复分支。
- 正常空间在身体满足最短物理时间、低速且有地面支撑后累计 0.10 秒，即保存当前物理姿势、同步启动动态 Montage，并在 0.22 秒内只用一个 Alpha 混到原有 `DefaultSlot`。
- 空间阻塞才进入无 Tick 的 `Downed` 睡眠；重试时不再唤醒刚体蠕动。墙角搜索允许最多 60 cm 的小范围退让，最终位置必须容纳完整站立 Capsule，退让路径不得穿过阻挡几何。
- 玩家与追猎者 AnimBP 都已简化为 `HeavyImpactDownedPose(A) + DefaultSlot(B) + HeavyImpactRecoveryBlendAlpha`，warnings-as-errors 编译成功；两份 DataAsset 重启回读一致。
- `Demo.Physics.HeavyImpact.*` 5/5 通过；短 PIE 无初始化、AnimBP、骨架、Slot、Montage 或脚本错误；Demo 模块构建成功且未编译引擎。
- 真实摆锤命中没有在无人输入的短 PIE 中自动发生，开阔地/正反面/墙角的最终视觉效果仍等待用户现场验收。

## 验收后增量：同一机关防夹保护

- 用户已确认当前受击起身主链效果可接受并通过视觉验收；本增量只处理“被同一摆锤持续夹住而无法起身”，不重新调整姿势混合或动画。
- 第一次真实碰撞继续由 Chaos 产生冲量。提交后保留约 0.15 秒刚体接触，再令物理 Mesh 临时忽略 `ECC_PhysicsBody`；墙、地面和动态关卡几何仍保持阻挡。
- 恢复完成后记录本次命中的机关 Actor。玩家对同一来源保护 0.75 秒，追猎者保护 3.0 秒；机关仍持续请求时刷新该期限，离开其预测范围后才自然到期。
- C++ 范围：`Source/Demo/Public/Components/Physics/HeavyImpactResponseComponent.h`、`Source/Demo/Private/Components/Physics/HeavyImpactResponseComponent.cpp`、`Source/Demo/Public/Data/Physics/HeavyImpactTuningData.h`、`Source/Demo/Private/Data/Physics/HeavyImpactTuningData.cpp`、`Source/Demo/Private/Physics/Tests/HeavyImpactResponseTests.cpp`。
- 资产范围：`/Game/ZeroEscape/Physics/HeavyImpact/DA_PlayerHeavyImpact` 与 `/Game/ZeroEscape/Physics/HeavyImpact/DA_PursuerHeavyImpact`。不修改 AnimBP、动画、Level0 或机关实现。
- 验证：补充保护参数与生命周期自动化契约；通过官方 MCP 回读两份 DataAsset；关闭 Editor 后仅构建 `Demo` 模块（`-Module=Demo -NoEngineChanges`）；最终由用户复测摆锤夹住及连续命中。

### 防夹增量实施结果

- `HeavyImpactResponseComponent` 在首次真实 Chaos 接触提交后保留 0.15 秒完整刚体接触，随后只把物理 Mesh 的 `ECC_PhysicsBody` 改为 Ignore；`WorldStatic`/`WorldDynamic` 没有改变，恢复外壳时复用既有 Baseline 还原碰撞。
- 组件从真实提交保留来源 Actor 到起身结束。完成恢复后只拒绝同来源请求并刷新期限，未新增机关分支或修改 `IHeavyImpactReceiver` 契约；不同来源仍可在保护期内发起下一次合法重冲击。
- 官方 MCP 保存并回读：玩家 `PhysicsBodyReleaseDelaySeconds=0.15`、`SameSourceProtectionSeconds=0.75`；追猎者对应为 `0.15`、`3.0`。玩家采用 C++/DataAsset 类默认 0.75，追猎者资产保存 3.0 覆盖值。
- 两个角色正常动画态 Mesh 均回读为 `QueryOnly`，所以 Montage 期间不会继续参与 Chaos 刚体夹持，本次没有添加无效的 AnimBP 或额外碰撞分支。
- `Demo.Physics.HeavyImpact.*` 5/5 通过、0 warning、0 error；短 PIE 正常启动/停止且无 HeavyImpact 初始化错误；最终 `DemoEditor Win64 Development -Module=Demo -NoEngineChanges` 构建成功，未编译或修改引擎。
- 用户已验收原受击起身主效果。本增量仍待用户复测摆锤夹持脱离和追猎者同机关连续命中，不在技术验证阶段冒充视觉验收。
