# DOC 目录

| 目录 | 用途 |
|---|---|
| `AI_WORK_GUIDELINES/` | AI 工作规范；入口见该目录 `README.md` |
| `Design/` | 唯一现行策划案（`GAME_DESIGN.md`）、专题设定、开发排期，`PCG/` 下的正式生成规则和素材契约，以及 `UI/` 下的局流程与界面系统文档 |
| `Ideas/` | 前期创意、候选方案与评审记录；按需读取，不加入启动上下文 |
| `DailyPlan/` | 用户确认后的当日实施方案；根目录只放正在推进的，完成后移入 `DailyPlan/archive/` |
| `DailyReport/` | 按日期保存的阶段或日报 |
| `Bugs/` | 尚未解决、暂停调查或等待验收的 Bug 记录与索引 |
| `Outputs/` | 可复用的工具与阶段产出说明；MCP 工具记录放在 `Outputs/MCP/`，PCG 阅读报告放在 `Outputs/PCG/`，动画重定向等制作工作流放在 `Outputs/Animation/` |

新增文档必须放入用途匹配的子目录；没有合适分类时创建名称明确的新目录，并在此表增加一行。`DOC/` 根目录只保留本目录文件。

## DailyPlan 归档约定

DailyPlan 完成或被后续主线取代后，**移入 `DailyPlan/archive/` 子目录并加 `Done-` 前缀**（如 `archive/Done-2026-07-22-xxx.md`），文件顶部保留一行归档 banner：

`> 状态：已完成并归档，仅作历史留痕，AI 无需主动阅读（当前主线为 …）。`

- `DailyPlan/` 根目录只保留正在推进的计划；AI 启动或检索时只看根目录，不主动阅读 `archive/` 内的历史计划，只有追溯特定历史决策时才按需打开。
- 移动文件时同步更新其它文档（任务卡、交接、Bug 记录等）里的路径引用，避免断链。
- 该约定与 `claude/reviews/`（完成报告移入 `reviews/archive/` 并加 `Done-` 前缀）保持一致。

## 局流程与 UI

- `Design/UI/GAME_FLOW_UI.md`：主菜单→PCG 关卡→胜负判定→结算/暂停→重开/选择关卡的完整闭环；已落地并验收（2026-08-03），审美统一打磨排期到最后。

## 当前 PCG 实施计划

- `DailyPlan/2026-08-03-PCG多层关卡正式实施计划.md`：多层 PCG 正式方案；先放完整楼梯和高天花板房间，再逐层运行现有二维 WFC，最后统一检查整栋可达性、楼梯间距和运行时导航。当前待用户批准，尚未进入代码落盘。
- `DailyPlan/2026-08-03-Level0三层楼梯隐藏导航坡面验证.md`：在 Level0 的 V2 贯通三层楼梯间与四座双层楼梯中验证隐藏行走坡面、护栏闭合、Recast 与真实追猎者通行；不改第三方素材与 PCG/WFC 代码。

## 物理受击阶段基线

- `DailyReport/2026-08-10-重冲击阶段验收收口.md`：玩家与追猎者共享重受击的当前权威记录；真实 Chaos 决定位移，Physics Control 维持有限人体张力，Snapshot 混入起身 Montage，并包含最终防夹参数和用户验收边界。
- 2026-08-05、08-07、08-08、08-10 的重冲击实施计划均已完成或被最终路线取代，统一移入 `DailyPlan/archive/`；后续轻受击先讨论共享输入、强度分级和状态仲裁，不直接扩写现有重受击状态机。

## 当前物理机关原型计划

- `DailyPlan/2026-08-10-磁力投掷物碰撞破碎实施计划.md`：当前磁力物保持静态网格抓取，只有正式投掷后的第一次有效阻挡碰撞才替换成短命 Geometry Collection；第一阶段全部碎片消失，后续再讨论大物体保留独立小磁力物。方案已确认，完整 Git 基线通过后先手工制作碎片资产；C++/Blueprint 运行逻辑尚未授权。
- `DailyPlan/2026-08-05-Level0常驻物理摆锤实施计划.md`：在 Level0 独立高厅实现一个不接 PCG 的 Chaos 自由摆锤，以硬角限位保留空间安全边界，并通过中线有限补能抵消实测被动损失；自动验证已通过，待用户手感验收。
- `DailyReport/2026-08-05-Level0常驻物理摆锤实现记录.md`：记录摆锤 C++/资产、36 件独立高厅装配、20 cm/s 补能标定与 20 kg 磁性箱碰撞证据。
- `DailyPlan/2026-08-06-Level0自动周期冲锤实施计划.md`：在摆锤房旁的三段低天花走廊实现自动周期冲锤；复用 HeavyImpact，只以真实 Chaos 接触驱动受击，当前待用户体验验收。
- `DailyReport/2026-08-06-Level0自动周期冲锤实现记录.md`：记录冲锤 C++/资产、低天花测试区、非零求解器冲量、4/4 自动化测试以及最终锤头/推杆装配。
- `DailyPlan/2026-08-10-狭窄走廊预判抛射Chaos机关实施计划.md`：当前权威方案；采用发射前预判、一次质心冲量、离膛后全程 Chaos。基础机制已获用户验收，后续模型更换、表现细节和轻受击接入均为独立增量。
- `DailyReport/2026-08-10-狭窄走廊预判抛射Chaos机关实施报告.md`：当前权威实施、验证与验收记录；最终默认调参为 `900 cm / 18° / 50 kg / 8 s`，验收版提交为 `f7ae4f44f427b562ec87cfd4a0ce9a109a873747`。
- 运行行为以当前 `Source/Demo` 中的 `ThrustGuidedHazard` 六文件和 `/Game/ZeroEscape/Hazards/Physics/Data/DA_ThrustGuidedHazard_Default` 为最终权威。2026-08-06 计划/报告及 2026-08-10 固定推力稳定性重构记录仅作历史追溯，不再作为实现、调参或验收依据。
