# Latest Error

当前无已知 Git 备份错误。

2026-07-29 已在精确核验 `origin = git@git.woa.com:shiqiqiwang/Demo.git` 后完成普通推送；主快照 `2c97789aa6d0c1f5abf119894134e06d8995fcf1` 已到达 `origin/main`，84 个 LFS 对象（约 40 MB）上传成功。

当前已确认但不属于 Git 的问题：Level0 日志缺少 RecastNavMesh；本地 MCP 的 `open_asset_editor` 有 UObjectArray 断言/SEH 崩溃保护记录。当前 C++ 构建、PIE、18 项 Demo.PCG、多 Seed 导航/追猎、连续命中与生命归零失败/同 Seed 重开仍未验证。
