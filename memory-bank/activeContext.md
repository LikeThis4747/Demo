# Active Context — Demo

> 当前任务详情以 .ai-context/current-task.md 为唯一来源；此处只保留迭代焦点与任务索引。

## 当前迭代焦点

Level0 V2 的中央三层塔与 A/B/C/D 四座双层楼梯已完成隐藏坡面、护栏收口、Recast 和真实追猎者双向通行验证；当前只等待用户手动玩家实走验收。通过后进入多层 WFC 数据合同讨论；正式 Exit/死亡/重开闭环与回归基线仍后置。

## 当前索引

- Level0 V1/V2：V1 静态基线保持；V2 共有 12 段 `ZE_NavOnlyRamp`，中央塔 4 段，A/B/C/D 各 2 段。
- 隐藏坡面：约 393.23×205×8cm、34.90°，宽度收在栏杆内，端点顶面约高出基准 4cm；游戏中隐藏，使用稳定的 `InvisibleWall + QueryAndPhysics`。
- 护栏：四座双层楼梯各一处 21.571cm 尾栏缺口已闭合到约 0.0000003cm；通路接口未封闭。
- 导航：未改 Recast 全局参数；绿色覆盖连续跨过每座楼梯的两跑、转向平台和上下层落脚区。
- 追猎者：真实 `BP_Pursuer` 以 1.2 倍缩放完成中央塔 G0↔F3、A/B G0↔F2、C/D F2↔F3；临时实例已清理。
- 玩家：自动输入不构成可信证据，仍需用户手动实走并确认无明显悬空、卡接缝或转向异常。
- WFC：隐藏坡面属于楼梯宏模块内部固定子件；占地、标高、落脚区、净空、开放接口、栏杆与行走面必须整体建模。正式记录见 PCG 模块表第 11 节。
- 正式一局：Exit、死亡结算与重开仍未完成；完整构建、Demo.PCG 与至少 10 Seed 基线仍待后续重跑。

## 当前边界与风险

- 本轮只修改并保存 Level0，以及任务卡、当日计划、PCG 模块文档和项目记忆；没有修改第三方 HydroLab 资产、蓝图、C++、DataAsset、其他地图或 Recast 全局配置。
- 最终保存重载后 12 段坡面持久化，PIE 已停止、临时测试 Actor 为 0、Level0 非脏；任务仍因玩家人工验收而保持 Active。
- 无专门上楼动画时，约 4cm 的坡面偏差需要玩家视角最终确认；若仍出戏，只迭代 Level0 内对应坡面的高度/端点，不扩大范围。
